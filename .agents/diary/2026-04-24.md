---
name: diary-2026-04-24
type: diary
era: COHORT_N9
status: current
created: 2026-04-24
updated: 2026-04-24
---

# 2026-04-24

## Session — canonical H1–H4 tables + writing handoff

**Context:** Post-reset results needed in a form the writing agent
can consume.

**Done:** H1–H4 VI(k) unanimity tables locked
(`2026-04-24_h1-h4-vi-results.md`). Writing handoff composed
(`2026-04-24_multiscale-task-trace.md`, 729 lines). Era index
refreshed (`2026-04-24_pipeline-status.md`).

## Session — scalar gauntlet: Stage 0b → Stage 4

**Context:** Plan to prove cohort-wide band-specific task persistence
via a single per-(patient, band) scalar + Wilcoxon / FDR gate. Stages:
0b (dmax stability), 1 (14 MSC-era metrics rerun under imcoh_abs),
2 (literature menu: KC, MC, wRF), 3 (implementation), 4 (KC λ-sweep).

**Done:** All four stages executed. Results:
- **Stage 1** — 0/14 metrics pass pre-reg. Best β rb ≈ +0.6, 6–7/9.
- **Stage 3** — 0/8 pass. KC is heights-driven; wRF picks δ only.
- **Stage 4** — per-band λ-signatures differ, 6–7/9 ceiling holds.

**Issues:** n=9 cannot clear q<0.05 FDR m=6 for rb ≈ 0.6 effects.
The scalar-gate framing is fundamentally wrong for this cohort.

**Decisions (user, verbatim):**
*"all the signals were there with the imcoherence framework and the
VI(k) profiles. we just have to find the way to make them emerge and
you are making me losing time."*

- STOP. Scalar-gate framing retired.
- Signal is visible in existing |ImCoh| artifacts (VI(k),
  partition_multiscale, H2d-θ). Surfacing problem, not testing problem.
- Month's scalar work not publishable. Scripts to be archived in the
  reorg (phase 3.7).

**Files touched:** `stage0b/1/3/4_*.py` (to be archived),
`2026-04-24_modular-trace-investigation.md` (STOP directive added),
`feedback_dont_rerun_scalar_tests.md` (memory saved).

**Next:** Compact + reorganize `.agents/`, `.claude/`, scripts.

## Session — scaffolding reorganization

**Context:** Folder structure is mess — bad filenames, no dates, no era
tags, no diary, no hooks, no enforcement. A new sweep keeps repeating
the same mistakes.

**Done:**
- Audits of `.agents/`, `.claude/`, and code organization (3 parallel
  Explore agents).
- Plan approved: 13 phases on `chore/reorg-agents` branch.
- **3.1** scaffolding commit `178eae1` (empty target dirs).
- **3.2** rule docs commit `aaffd11` (renormalization-style, coding-
  rules, naming, frontmatter-schema, never-always, diary README,
  era-map).
- **3.3** (this entry) — post-mortem + backfilled diary for
  2026-04-10, 13, 15, 18, 22, 24.

**Decisions:**
- Renormalization style (head first, body expands) is the global
  writing rule.
- Save "never X / always Y" as feedback memory on first mention.
- Scope: reorganize scaffolding only in this session; return to
  science next session.

**Next:** 3.4 frontmatter to existing files; 3.5 kebab rename;
3.6 archive dead; 3.7 scripts reorg; 3.8–3.13 per plan.
