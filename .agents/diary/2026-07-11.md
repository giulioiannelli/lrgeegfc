---
name: diary-2026-07-11
type: diary
era: IMCOH_ABS
status: current
created: 2026-07-11
updated: 2026-07-11
---

# 2026-07-11

## Session 09:30–10:25 — Results §3 finalized: numbers verified (one fix), 5 figures built + 2 compounds embedded

**Context:** Finalize `results_sec_3.tex` (epileptogenic-tissue propagator markers) the
way §1/§2 were — verify every result holds, build/adjust figures under
`data/preprint/figures/results_section3`, add the missing "spreading-from-a-seed" glass
brain, and embed everything into the paper.

**Done:** All six headline claims re-verified against source CSVs and match to the decimal
(one wording fix). Four existing single-panel figures redirected to the preprint tree and
refactored for compounding; one new 3D seed-spread brain built; two page-fitting compound
floats assembled and embedded in the `.tex` with captions + inline `\FigRef`s.

**Issues / divergences:**
- **One number fix (substantive):** R3.3 "eight patients … AUC 0.90–0.99" overstated —
  the 8 community patients span **0.685–0.985**, only 5 ≥ 0.90 (audit_117
  `detector_lopo_per_patient.csv`). Fixed to "up to 0.99 (five of the eight above 0.90)"
  in both `results_sec_3.tex` and the `R3.3` directive. Figure was already honest.
- **One clarity fix:** dropped the Pat_15 frame collision ("0.93 against the propagator's
  0.61" — 0.61 is all-contacts, 0.57 is the fused detector cited two clauses earlier) →
  "0.93 where the propagator sits near chance".
- **Deviated from "one panel holds all":** five full-width analytical strips can't fit one
  portrait journal page legibly (fonts shrink to ~6 pt at `\textwidth`). Split into TWO
  `figure*[p]` floats — same reason §1/§2 each use two. Flagged to user.
- **ACC dropped** (user Q3): accuracy is misleading at ~9 % SOZ base rate; kept
  AUC + precision@k + calibration.

**Decisions:**
- §3 = two floats: **fig_epi1** = seed-spread brain (a) + relational band-AUC (b) +
  calibrated detector (c) [R3.1–R3.2]; **fig_epi2** = two-populations (a) + scope/ceiling
  (b) [R3.3–R3.4]. Brain = plotly 3D raster panel (a), per user, mirroring §1 fig_trace1a.
- Seed-spread exemplar = Pat_08 (δ, rest_post): seed electrode W, off-shaft SOZ on electrode
  T rediscovered (off-shaft AUC 0.86); node colour/size = strength-residual affinity *rank*
  (raw residuals too small to see), rings = true unseeded SOZ.
- Single-panel scripts expose `draw(target[, letters=False])`; compound imports & delegates
  (single source of truth), like the §1/§2 compounds.

**Files touched:**
- `.agents/preprint/overleaf/results_sec_3.tex` (2 prose fixes, 4 `\FigRef`s, 2 figure floats)
- `.agents/preprint/directives/results_paragraphs/R3.3_two-populations.tex` (kept in sync)
- `scripts/01_compute/figures_embedded/fig_epi_seed_spread_3d.py` (NEW)
- `scripts/01_compute/figures_embedded/fig_epi_section3_compound.py` (NEW → fig_epi1/2)
- `scripts/01_compute/figures_embedded/fig_epi_{a,b,c,d}_*.py` (draw() refactor + preprint OUT)

**Next:** copy the `.tex` + `fig_epi1.pdf`/`fig_epi2.pdf` to Overleaf and compile; optional
polish — tiny "0.81/GBM" label overlap in fig_epi1(c); could also build a single tall
5-panel poster/talk version of the compound if wanted. Env: use
`/home/giulio/Documents/miniconda3/envs/lapbrain/bin/python` (the `conda run` wrapper hits a
cross-compiler activation hook).

## Session (figure-maker) — ρ^coph explainer: the cophenetic tanglegram (committed a557223)

**Juice:** built the missing "what does ρ^coph actually measure" figure. Crop the LRG tree to
one clade, draw its induced sub-dendrogram across rest_pre → task → rest_post as an UNTANGLED
tanglegram; lines parallel = split-rank retained, crossing = reorganised. Contacts tinted by
rest_post sub-clade so the fates read as colour-block patterns. Approved by user ("it was the
right one"), committed + pushed on `audit/cohort-n10-diagnostic`.

**Two scripts (both `scripts/01_compute/figures_embedded/`):**
- `fig_cophenetic_tanglegram.py` — 4-row overview, ONE β patient (Pat_02), small clades (6–10),
  labelled bead-nodes (`W¹⁰` superscripts, unicode). Rows = the 4 fates.
- `fig_cophenetic_tanglegram_class.py` — one big single-fate tanglegram (≥20 contacts), exemplar
  sampled across cohort × 6 bands (seeded, top-k). One PDF per fate. Picked: anchor Pat_08 γ_low,
  trace Pat_06 δ, reset Pat_07 α, reorganised Pat_08 δ.

**Taxonomy = corners of the (a,b,c) cube**, a=ρ(pre,task) b=ρ(task,post) c=ρ(pre,post):
anchor (H,H,H) · trace (L,H,L) · reset (L,L,H) · **reorganised (L,L,L) = new "changes always"
class** added this session. Visual 2×2: fate ⇔ (pre grouped?)×(task grouped?); rest_post rail
is always clean by construction (colours ARE its sub-clades).

**Honesty:** trees untangled by branch rotation (rotation-only, so crossings are genuine);
annotated ρ is LOCAL/subset (≠ whole-tree ρ^coph), clades disclosed-selected by score →
illustrative, NOT a gated test; cohort claim stays the matched-strength gate. At ≥20 contacts
anchor/reset are the *weakest* exemplars (exact rank-preservation across a big clade is rare;
reset ρ_pre,post only 0.45) — reported honestly on-figure. trace/reorganised are crispest.

**Reuse:** promoted `contact_labels` + `split_label` → `lrg_eegfc.utils.probe` (2nd caller);
`fig_hierarchy_chord_network.py` now imports them (dedup). Draft PDFs in
`data/preprint/figures/_drafts/` (gitignored). Env caveat: `conda run` hits a cross-compiler
hook — call the env python directly.

**Next (open, user may want):** contact labels currently repeat on all 3 rails (redundant but
legible at small N) — could drop to rest_post-only for the big figures; finalize out of `_drafts`
+ companion `.md` when the set is locked.

---

## MS-relative scale sweep — α has a real mesoscale; τ_min under-reads it (audit_172)

**Head.** User pushed on the role of τ: on a fully-connected continuous-spectrum
graph is `τ_min = 1/λ_max` really the only scale, or is there a band↔scale
fine-tuning? Ran the referee audit_121 never had — the canonical ρ_sym gate swept
across `s ≡ τ·λ_max` with a **matched-strength null rebuilt at every s**
(audit_172; the un-run "audit_159"). **Verdict: α's trace is genuinely
scale-tuned — it strengthens at a mesoscale `s ≈ 1.5` (cohort gate 0.024 → 0.0098,
z 2.2 → 3.6, LOO 0.0195) on a broad plateau s∈[0.5,2.3], and weakens past Fiedler
(not collapse). β is scale-broad (clears every collapse-free s, best 0.003 @
s≈1.86). δ/low_γ "clear" only into collapse (monotone z, strengthen past Fiedler,
δ = SOZ-anatomy band) → rejected. θ/high_γ null.** So the Methods "τ-sweep is
degenerate / no hierarchy remains to read" is **false for α**, true enough for β.

**Notation fix (user):** `s ≡ τ·λ_max` for the diffusion scale — stop overloading
α (the band). methods.tex uses the same `α ≡ τ·λ_max` collision; flag to fix.

**Anchor.** `s = 1` reproduces the certified audit_150 gate bit-for-bit
(max|Δobs|=1.1e-16, max|Δsurr_mean|=9.7e-17), so every `s≠1` reading is real
scale physics. Engine reuses audit_150's numba swap + seed (BASE_SEED 20260706 +
cell_idx, phase order A→B→task→post); eigendecompose once, D_coph(s) from
eigenpairs. 60 cells × R200 × 16 s in 553 s on the Pool.

**Discipline caught the trap.** "best-p over 16 s" is a cherry-pick — read off the
**collapse-free window (s < Fiedler)** and the **shape** (peak = trace, monotone
climb = collapse), not the min. That's what separates α (genuine) from δ (SOZ
anatomy surviving the ρ_sym differencing at coarse s; MS doesn't rescue it — it's
an anchor, not a task-trace).

**Artifacts.**
- scope `.agents/guides/task-persistence-investigation/2026-07-11_ms-relative-scale-sweep.md`
- `scripts/01_compute/audit/audit_172_ms_relative_scale_sweep.py` (+ `_172b` figure)
- `data/audit/tau_sweep_ms_gate/{per_patient_per_band_s,cohort_gate_s}.csv`,
  `audit_172_scale_sweep_verdict.pdf`, `README.md`
- verdict `.agents/reports/2026-07-11_ms-relative-scale-sweep-verdict.md`
  (updates the 2026-07-07 r1-reexam: α upgraded observed-only → MS-clean + LOO-robust)

**Open (PI / writing calls, not touched):** (1) soften the hardened methods.tex
τ language for α (+ fix the `α ≡ τ·λ_max` symbol collision); (2) whether α's
mesoscale enters the paper as the "α gains a scale though no place" point;
(3) Part B — encoding/inference components over a dense s-grid (audit_157 already
has a 3-point MS version favouring s≈2.6 for β-inference).

---

## Session (talk conductor) — 20-min deck restructured to 25 plain slides + figure worklist

**Juice:** folded the user's hand-written `Slides.md` + a live Canva read into a locked
25-slide structure, rewrote every slide into a plain 7-part format (title · concept ·
on-slide text · speech · figures · refs · Canva status), and produced the figure agent's
worklist in `.agents/talk/PLAN.md`. This is the "main slide-editing" chat; W/F/R/V lanes run
in lateral chats off PLAN.md. Canva reachable via the connector (deck `DAHO6jbUdrY`).

**Structure (25 slides, `slides/NN_*.md`):** intro 01–08 (+ NEW 06 "why it matters —
signatures across scales"), methods 09–11, results 12–23 (NEW 12 tanglegram categories,
14 "who's right", 16 survivors table; the null moved from methods into results as the
referee), conclusion 24–25. Old I-/M-/R-/C- files git-rm'd (content rewritten; history kept).

**Key content calls (user-driven):**
- Null reframed as the results referee: raw ~half-killed by MS + fails drift; Grassmann survives
  MS but drift-vulnerable (p≈0.116); coph β sole dual-null survivor → "it must be multiscale".
- Reveal reorder: our-measure (13) → "who's right?" (14) → nulls/why (15) → survivors table (16).
- Reber 2016 kept, moved to dataset slide 03 as the iEEG-inference precedent (not "higher-order").
- Consolidation (18) keeps reinstatement 10/10 + a laterality bullet (justifies the 6/10).
- Inference→cingulate (21): verified audit_171 — coph localizes (MS q 0.03/0.04, LOO), raw
  placeless; duration control OPEN (length-null demotes to 0.124, likely invalid for a conditional).
  "Detection tied, localization separates" made explicit.
- low-γ ENCODING→cingulate made explicit (whole-brain zero masks a focal cingulate trace, 8/8 q 0.035).
- **Three-axes CORRECTED** (was wrong): time = frequency bands, space = sEEG contact resolution,
  topology = diffusion hierarchy (NOT "τ / heat-flow / hierarchy"). Fixed on slides 07/06/24.
- Tanglegram slide (12) uses ρ^coph classes trace/anchor/reset/**reorganized** (NOT "emergent").

**Figure worklist (PLAN §Figure manifest):** ⭐ real F-agent work = FINALIZE the 4 tanglegrams
(12), BUILD the raw/Grassmann/coph 3-method panel (14), FINALIZE inference bloom (20), LOCATE
low-γ cingulate (22), + 4 RENDER 3D/video (04 connectome, 07 τ-sweep, 17 pairglow, 18 laterality).
~26 COPY rows (existing PDFs → talk/); Canva schematics flagged presenter-built; EXTERNAL (Betzel,
frisbee, sEEG photo) → Lane R.

**Open:** 25 slides / 20 min ≈ 48 s each (tight) — cut valves = slide 23 (epi), then merge 13+14.
Per-slide review/lock with the user still pending.

---

## Scale sweep — Methods fold-in + Part B (encoding vs inference over s) (audit_173)

**Head.** Closed out the τ/scale investigation. (1) **Methods fold-in** (user: "don't
change paper content, this parameter doesn't change much, fold in methods"): replaced
the two FALSE absolutes in `methods.tex` — "such a τ-sweep is degenerate" and "no
hierarchy remains to read" — with the honest robustness note ("across [τ_min, τ*) the
reading is robust rather than tuned; the same bands carry the trace at every scale below
collapse; τ_min is a robustness-checked operating point"). β stays at τ_min, NO new
result, no α-mesoscale claim. Re-paste methods.tex to Overleaf. (2) **Part B (audit_173):**
densified audit_157 (enc/inf arc ρ_sym × s × MS) from 3 τ to |s|=17, reusing the cached
seed-20260511 surrogate eigs (reform-only). Anchors PASS 1e-16 (arm1==103d, sym@1==152).

**Part B verdict.** **Inference-specific (T_infspec_pe) is β-ONLY at every scale** —
clears τ_min (.010) and everywhere (best .003 @ s≈2.1); no other band ever clears →
scale-broad, mildly mesoscale (confirms audit_157 full-curve, vindicates τ_min).
**Encoding (T_learn) is α+β and FINE-favouring** — α strongest s≈0.85 (.010), weakens
monotonically to null past Fiedler (.246 @ s=8). **Scale dissociation: encoding fine,
inference broad/meso** — component echo of Part A's α-meso/β-broad. **δ encoding
strengthens with s (.014→.003) = collapse/anatomy → REJECT.** No paper change (all
components clear at τ_min, same bands across window).

**Artifacts.** `scripts/01_compute/audit/audit_173_arc_scale_sweep_rhosym.py` (+ `_173b`
fig); `data/audit/arc_scale_sweep_rhosym/{arc_scale_null_s,arc_scale_cohort_verdict_s}.csv`,
`audit_173_arc_scale_verdict.pdf`; verdict report Part B section appended to
`.agents/reports/2026-07-11_ms-relative-scale-sweep-verdict.md`. methods.tex edited (2
surgical fixes, §LRG-framework + §Decomposing/Scale-dependence).

**Open (user):** re-paste methods.tex to Overleaf; nothing else pending on the scale axis.

---

## 1/K vs 1/A — is the diffusion distance distinguishable from raw-FC before sparsification? (audit_176)

**Head.** User's gate: migrate the trace pipeline to a raw-FC distance (dropping the LRG
propagator) **only if** 1/K(τ_min) — or at any τ — does not beat the raw distance on nulls
or a principled axis. **Verdict: the propagator is NOT distinctive for the trace at τ_min —
migrate to −logA (raw), NOT 1/A.** MULTISCALE = the dendrogram hierarchy (ρ^coph compares
cophenetic path-heights); ANY raw-distance UPGMA tree has it, so a raw distance does NOT
lose multiscale. (I first hallucinated that "1/A is single-scale / deletes multiscale" —
FALSE and corrected; the pipeline reads the trace at a single fixed τ_min, its
multiscale-ness is the tree not a τ-sweep; user was right.) 1/A SPECIFICALLY is the one bad
raw form (loses α); −logA reproduces the whole diffusion portrait.

**Why 1/K≈1/A on β is mathematically forced.** First-order heat kernel: K(τ)_ij = τ·A_ij
+ O(τ²) off-diagonal. Measured on our FC: **Spearman(offdiag ρ(τ_min), offdiag A) =
0.995–0.997 in every band** — the τ_min propagator is a near-perfect monotone image of A.
The diffusion adds only a ~0.5% rank perturbation (the 2-step-path term); UPGMA amplifies
it to tree-sim 0.76–0.89 (audit_174) — enough to tip α/low_γ near the .05 boundary,
nothing for β at +0.198. This is the Villegas-2025 Wigner-semicircle degenerate regime
(dense graph → single scale).

**audit_176 (new): functional-form robustness of the raw arm** (closes audit_174 open #1).
Canonical ρ_sym MS gate at τ_min on FOUR distances fed identical surrogate draws (R=200,
729s; diff reproduces audit_150): 1/K, 1/A, 1−A, −logA.
- **β CLEARS under all four** (.032/.032/.042/.019, n 6–7/10) → connectivity property,
  not diffusion, not reciprocal-form. Bulletproof.
- **⚠ REFINES audit_174's "α needs the propagator".** α clears under diffusion, 1−A,
  −logA but marginally FAILS under **1/A specifically** (.053). So α's audit_174 raw-fail
  is a **1/A-reciprocal-form artifact** (1/A blows up weak edges, drowns α), NOT
  diffusion-necessity. Honest claim: "α needs a weak-edge-robust distance, of which
  diffusion is one." −logA is α's strongest arm (.0098).
- **−logA reproduces the diffusion's ENTIRE band portrait** at τ_min: α .0098, β .019
  (both STRONGER than diffusion), δ/θ/low_γ/high_γ all fail (AGREES with diffusion band-for-band).
  So the propagator is not doing distinctive work for the trace; a raw log-distance suffices.
- **low_γ** clears only under 1/A & 1−A; fails under diffusion AND −logA. 1/K and 1/A
  DISAGREE here. Status UNRESOLVED — NOT "artifact/anatomy" (user correction; high
  inter-patient fluctuation, largest single-patient trace yet fails cohort-wide). Since the
  chosen migration form (−logA) AGREES with diffusion (both fail), low_γ is not a migration blocker.
- δ/θ/high_γ dead across all forms.

**"At any other τ" clause.** audit_172 (sibling): under diffusion α peaks at s≈1.5
(.024→.0098). But −logA (raw, τ-free) MATCHES that (.0098) at τ_min — so even α's coarse-τ
diffusion gain is reproduced by a raw log-distance. 1/K beats 1/A(reciprocal) here, not −logA.

**What you may claim.** The propagator is NOT load-bearing for the cross-phase trace: a raw
−logA UPGMA hierarchy reproduces α+β with stronger effect and the same band-selectivity.
CONVERGES with audit_175 (sibling, just landed): on the sparse backbone geodesic ties/beats
diffusion and β survives. Re-ground LRG value on LOCALIZATION + band-selectivity, not the
diffusion mechanism. CAVEAT: scope is the TRACE (τ_min); the EPI MARKER uses coarse-τ
diffusion SPREADING (10/λmax) = genuine heat dynamics, NOT raw-reproducible.

**Artifacts.** `scripts/01_compute/audit/audit_176_rawform_robustness.py`;
`data/audit/rawform_robustness/{per_patient_per_band,cohort_summary}.csv` + README.
Memory: `audit_176_rawform_robustness_2026_07_11`. Reuses audit_150/174 machinery verbatim
(only distance fn changes); does not modify either.
