---
name: diary-2026-07-13
type: diary
era: IMCOH_ABS × COHORT_N10 (mst@0.20 recovery)
status: current
created: 2026-07-13
---

# 2026-07-13 — recovery arc: controls ladder, pillar 6, T_learn bug, compact

**Head.** Closed the mst@0.20 recovery pillars: controls ladder (band-selective trace
is cophenetic-unique), pillar 6 (β trace left-lateralized), and caught + fixed a
shared-baseline bug in the enc/inf `T_learn` that had made encoding look falsely null.
State compacted; next = Overleaf conceptual-diffs + slides. Full state in memory
`sparsified_recovery_mst020_2026_07_12.md` + report `2026-07-12_mst020-recovery-arc.md`.

**Done today:**
- **Controls ladder** (`14_controls_ladder_mst020.py`, `data/sparsified_arc/controls_ladder/`):
  raw_fc / strength / clustering / geodesic / **spectral resistance** / cophenetic(τmin,meso)
  × {T_test,T_learn,T_infspec,T_infspec_pe} × matched-strength. **Band-selective trace
  (α/β) is COPHENETIC-UNIQUE** — raw FC fires 4 bands (non-selective), strength degenerate,
  geodesic wrong bands (δ/γl), spectral resistance dead; coph_meso sharpens coph_τmin.
  Encoding readable from raw FC; inference (β) readable from raw FC too → coph value-add
  = **localization** (cingulate, audit_171) not magnitude. Fig `fig_controls_ladder.py`.
- **Pillar 6** (`15_patient_variation.py`, `patient_variation/`): joins mst@0.20 per-patient
  trace × `data/audit/implant_geometry/per_patient_features.csv`. **β trace LEFT-lateralized**
  (B_hemi ρ=+0.685, p=0.029) — left-only implants (02/08/03/07) trace, right-only Pat_15
  null, Pat_06 right-OFC exception; **α NOT lateralized** (ρ=0.16 ns, placeless). Reproduces
  audit_148.
- **T_learn shared-baseline BUG** in `05_enc_inf_arc.py`: was `0.5(ρ(e,p2)+ρ(e2,p))`
  (shares A/B baseline per arm), inconsistent with its own T_test and audit_166. Fixed to
  cross-baseline `0.5(ρ(e,p)+ρ(e2,p2))`. My earlier "encoding = null everywhere" was this
  bug; the controls ladder (correct convention) shows **encoding CLEARS α/β** (matches
  paper §2). Re-run in progress → `encinf_fix.log`; T_infspec_pe (cross-baseline,
  unaffected) stays δ/α/β = not β-only.

**Committed:** 7610694 (scripts 12/13 + 05/06 mst020 + lib). **Uncommitted:** 14, 15,
05 T_learn fix, fig_controls_ladder, pillar6 + preview figs.

**Next (user 3-part, compacting first):** (2) Overleaf conceptual-diff directives per
`results_sec_{1,2,3}.tex` for 3 parallel rewrite agents (→ `.agents/preprint/directives/`);
(3) slides: add sparsification to `11_pipeline.md` + τ-role commentary.

---

## PM — enc/inf fix landed, Overleaf directives + slides propagated (2)+(3) DONE

**Head.** Captured the corrected enc/inf per-scale numbers (encoding α/β recovered,
inference NOT β-only), wrote the four Overleaf conceptual-diff directives, and
propagated the recovery into the talk slides — including the required correction that
**drift is retired**, which had been baked into slides 14/15/21 as a selling point.

**enc/inf fix (T_learn bug) — per-scale verdict (05, mst020, encinf_fix.log done):**
- Encoding `T_learn`: **β 16/16** (p@meso .003), **α 3/16** (clears τ_min .042 + meso .032).
  δ/θ/low_γ 0/16. → encoding RECOVERED (bug had faked it null — "washed out" premise RETRACTED).
  **Band-split (controls ladder):** β encoding = COPHENETIC-UNIQUE (raw FC .053 near-miss
  fails; clustering/geodesic/spectral all fail; only coph .003) = the multiscale-exclusive
  result; α encoding = BASIC-METRIC (raw FC .032 + clustering .010 read it) + marginal
  (α-meso .032→.053 flips across runs). For β the enc=basic/inf=multiscale story INVERTS
  (β encoding hierarchy-only, β inference raw-visible → coph value-add = localization).
- Inference `T_infspec_pe`: **β 7/16** (.005), **α 7/16** (.024), **δ 10/16** (.019) at
  mesoscale — **NOT β-only**. Controls ladder: β inference readable from raw FC too
  (.010) → coph value-add = LOCALIZATION (cingulate, audit_171), not detection.

**(2) Overleaf conceptual-diff directives** (`.agents/preprint/directives/`, kind=writing-directive,
story-level per user — tell the issue + τ-role, let rewrite agents do prose):
- `writing_directive_2026-07-13_sparsified-recovery-OVERVIEW.md` — shared context +
  master number tables (trace gate, controls ladder, enc/inf, epi, laterality) +
  honesty rules + the τ through-line. READ-FIRST for all 3 agents.
- `..._sec1-trace-conceptual-diff.md` — biggest change: dense→sparsified + τ swept;
  promote band-specificity para to the full CONTROLS LADDER (spectral resistance dead);
  convert "spread not noise" → laterality EXPLANATION. Arc + OFC/tissue/replay/SOZ stay.
- `..._sec2-encinf-conceptual-diff.md` — encoding α/β HOLDS (bug was internal); drop
  "inference in β ALONE" (now δ/α/β at meso) → reframe value-add as localization.
- `..._sec3-epi-conceptual-diff.md` — smallest: marker robust to sparsification; τ lifts
  AUC/ranking (multi>single 7-8/10) NOT precision (60% = band-fusion). Everything stays.

**(3) Slides propagated** (`.agents/talk/slides/`): **11** pipeline = +sparsification
(MST+top20%, why: dense propagator degenerate) + τ-sweep as scanner (flipped the old
"no τ-sweep" note; figure needs a backbone panel → task #13). **14** the-nulls = drift
RETIRED, matched-strength sole referee, hands to "is it really multiscale?". **15**
survivors = fully REBUILT from the dead drift-based "2-null survivor table" into the
CONTROLS LADDER (raw/strength/clustering/geodesic/**spectral-resistance**/cophenetic →
only multiscale carves bands). **13** drift-ref removed. **20** epi = τ ranking-not-
precision + mst@0.20 robustness. **21** take-homes "drift survivor"→controls-ladder
survivor. `Slides.md` old outline marked superseded. 05/07 already τ-aligned (left).

**Blast radius note:** the drift discard (2026-07-12) had propagated into the deck as a
headline ("cophenetic β = the drift survivor", "survives both nulls") — all now corrected
to matched-strength + controls-ladder. This was beyond the literal "τ-role" ask but
required for correctness (brutal-honesty: can't sell a retired null).

**Still uncommitted** (data/ gitignored): scripts 14/15/05-fix + figs + the 4 new
directives + 7 edited slide files + this diary. Task #13 (regen preprint + pipeline
figures on settled scheme) remains the open compute item.

---

## Session 2 — `new_results_sec1` figure rebuild (Wave 1: the τ-story)

**Head.** Started rebuilding Results §1 figures on the mst@0.20 multiscale backbone in a
NEW folder `data/preprint/figures/new_results_sec1/` (old `results_section1/` untouched).
Wave 1 (everything whose data already exists) is **done + verified**: 6 main figures + 9
drafts, all reading the τ backbone at the reporting scale s≈5.646. Wave 2 (per-pair spatial
compute) not yet started.

**Foundation (library + shared layer):**
- `src/lrg_eegfc/utils/fc/heat_multiscale.py`: added `linkage_at_scale(ev,V,s)->Z` +
  factored `_linkage_at_tau` shared body (returns the UPGMA tree behind `cophenetic_at_scale`
  so tanglegrams/chord/ribbon/morph can draw the dendrogram at scale s). `cophenetic_at_scale`
  + `linkage_at_scale` added to `__all__`.
- `scripts/01_compute/figures_embedded/new_results_sec1/_common.py`: the backbone+scale layer —
  `tree_at_scale(W,s=S_REPORT,frac=0.20)`, `coph_square_at_scale(...)`, `eig_backbone`,
  `eig_by_phase`, S_REPORT=5.646, SGRID, FIGDIR, band colours, `load_phase`/COHORT/BANDS.
  Validated: recomputed ρ_sym bit-exact vs `ms_mst020/per_patient_scale.csv` (Pat_06/α=0.903,
  Pat_15/β=0.015). **Run from repo ROOT** (loaders resolve root from CWD).

**Wave-1 figures (all in new_results_sec1/):**
- `fig_tau_role` (money): C(τ) dense-1-peak→mst@0.20-ladder + peak-count-vs-density onset +
  τ-resolved 6-band trace with MS null envelope. Honest labels keyed to the s≈5.6 gate
  (β scale-broad, α mesoscale, δ patchy, high_γ coarse-only, θ/low_γ null).
- `fig_tau_dendrogram_morph`: one exemplar (Pat_06 β) dendrogram at s∈{1,5.6,30,90} —
  height dynamic-range 7.2→1.7 decades = multiscale→collapse.
- `fig_encinf_tau_gate`: T_learn/T_test/T_infspec(_pe) gate −log10p vs s per band. Carries
  the corrected encoding result — **β-encoding continuous-multiscale (16/16), α-encoding NOT
  (3/16)** — verified from `enc_inf_arc_mst020/cohort_gate_vs_s.csv`.
- `fig_trace_a_band_violin`: per-band violins @ s≈5.6 + band×scale gate heatmap.
- `fig_perpatient_bands_forest`: per-patient own-null forest @ s≈5.6 + "k/16 scales cleared".
- `fig_trace_c_diffmaps_mst020`: split-half (arm-1 ρ_sym) diff-maps, exemplar **Pat_07**
  (β/α echo, others null — Pat_08 rejected as a low_γ super-tracer that muddied the visual).
- Drafts (agents, verified): 4 taxonomy tanglegrams (trace Pat_05 θ / anchor Pat_10 low_γ /
  reset Pat_10 δ / reorganized Pat_06 δ — re-scanned on the sparsified graph); 3 chord
  networks (Pat_13/06 β, Pat_06 θ) + 2 ribbon trees (Pat_06/05 θ) — cleaner on the sparse
  backbone; graph-tool worked.

**Wave 2 (NOT started):** per-pair ρ_sym^coph at scale on mst@0.20 + region/tissue/SOZ labels
+ R=1000 localizer → fig_trace2 (OFC), tissue-class carrier, **α-SOZ-per-scale investigation**
(the novel item), pairglow 3D (τ as parameter), enc→OFC/inf→cingulate anatomical. Plus the two
remaining Wave-1 adaptations that need new compute: dual-bloom (drift-laden, repoint sources)
and reinstatement (windowed cophenetic on mst@0.20 + add learn phase). All need fresh compute;
time-estimate before launch. Plan: `~/.claude/plans/refactored-baking-mccarthy.md`.

---

## Session — slide 11 pipeline finished (7-panel sparsification sequence)

**Head.** Rebuilt talk slide 11 ("the network-analysis pipeline") end-to-end for the
mst@0.20 arc: sparsification is now a first-class pipeline step, the example sequence
grew 5→7 panels, and the ρ estimator formula was trimmed to one readable line with the
A/B halves labeled as rest_pre split-halves.

- **`scripts/07_figures/gen_pipeline_sequence.py`** rewritten (5→7 panels, dropped the
  fragile tanglegram/audit sys.path imports). New sequence, Pat_05 β, all transparent PNG
  in `data/outputs/figures/talk/pipeline_seq_{1..7}_*.png`:
  1 brain electrodes (nilearn ortho glass brain, 118 contacts) · 2 sEEG timecourses ·
  3 |ImCoh| matrix (dense) · **4 mst@0.20 backbone network** (spring, edges visible via
  rank t_mode + α-floor; density 0.20 printed) · **5 specific heat C(τ)+Ŝ(τ)** with 4
  timescales marked on C(τ) (s=1 · peak1≈8 · valley≈54 · peak2≈372) and a **propagator
  K̂(τ)=e^{−τL̂} inset** at each (diagonal-zeroed, shared PowerNorm — reads as sparse→block
  →fill→near-uniform spread) · 6 backbone cophenetic dendrogram (τ_min, ~11-decade multiscale
  tree) · 7 the ρ glyph (output). Runs in ~6 s (N~118, cheap). C(τ) shows **2 peaks** =
  mesoscale ladder confirmed on the backbone.
- Removed the 5 stale old-numbering panels (seq_1_timeseries … seq_5_rhocoph_tanglegram)
  so the Canva sequence is unambiguous. The ρ^coph tanglegram is no longer a pipeline panel
  (lives as its own explainer, `fig_cophenetic_tanglegram.py`, a557223).
- **`.agents/talk/slides/11_pipeline.md`** rewritten: on-slide text + speech now lead with
  sparsification (dense = one scale; backbone = many) and τ-as-scanner; FIGURES section is
  the concrete 7-panel spec; kept the current τ-swept science from the earlier same-day edit.
- **Formula fix** (user): estimator trimmed to ONE line, `_sym` gone, A/B written explicitly
  as `D^A_pre`, `D^B_pre` = the two rest_pre split-halves (answers "A and B of what" on-slide):
  `ρ = ½[ ρ_S(D_task − D^A_pre, D_post − D^B_pre) + (A↔B) ]`. D^coph unchanged
  (`D^coph_ij = h_{ℒ[D]}(LCA(i,j))`). Both compile-ready; presenter RE-RENDERs + RE-COMPOSEs
  the 7-panel row in Canva.
- **PANEL POLISH (4 user fixes, same session):** (1) panel 1 brain = the 3D translucent-pial
  COHORT overlay (all 10 implants, per-patient colour, `brain3d.pial_mesh`+`spheres_mesh`,
  plotly/kaleido), ONE left view — replaced the flat nilearn glass brain; `--brain patient`
  for single-implant. (2) panel 4 backbone = Fruchterman–Reingold + faint grey web — killed
  the same-probe RED edges (`coloring="probe"`) and the KK hairball-ball; NB mst@0.20 is
  density 0.20 ≈ avg degree 23 (1384 edges) → genuinely an integrated cloud, not sparse.
  (3) panel 6 tree = **normalised** height + ONE illustrative ψ_n(τ) cut colouring the
  partitions (hand-placed `--cut-k 12`; cropped the empty grey trunk). Swept τ + tried
  `optimal_threshold`: **degenerate — Pat_05 β is DELOCALISED** (one giant community at every
  scale until collapse-shatter; consistent with the settled "β trace delocalised, no
  anatomical home"), so the cut is a hand-picked teaching device, NOT the LRG index; the real
  comparison uses full cophenetic distances (no partition — [[feedback_no_partition_metrics_use_rho_coph]]).
  (4) panel 7 glyph = **ρ^coph** (was ρ). graph_tool (sfdp/arf) NOT available in lapbrain →
  fr/kk only for layouts.
- NOT committed yet (talk figures untracked build artifacts; slide md + script + diary staged
  for the next talk commit).

---

## Session — talk results-act REFOCUS to the settled three results

**Head.** The three results settled (`.agents/preprint/established_results/2026-07-13_settled-three-results.md`)
and forced a real refocus of the talk's results act, not a patch. Two mandated changes:
**(a) β trace → OFC is DEAD** (settled 1.8 ✅ — the β trace is DELOCALIZED on mst@0.20, no
anatomical home at any scale; supersedes the whole-graph β→OFC as a degenerate-graph
artifact; OFC survives only as the ENCODING anchor). **(b) the "it must be multiscale /
who's right" spine is retired** — the frame is now **information-DEPTH, not exclusive
detection** (raw FC detects β at p=.024; the multiscale edge is selectivity + scale-shape,
NOT "only multiscale sees it").

**Method.** Two workflows: (1) a 25-agent propagation scan (`wayn3umg5`) → review doc
`REVIEW_2026-07-13_tau-sweep-propagation.md` (still valid for intro/method 5/6/7/9/10/12 +
hubs); (2) a 10-agent parallel refocus (`wgs9ipot9`) rewriting slots 8+13–21 from the
settled doc + plan `REFOCUS_2026-07-13_three-results.md`, one shared vocabulary.

**Deck re-map (slots stable, jobs changed):**
- **13** trace-holds + the scalar-vs-structured-object frame (killed "read three ways/who's right").
- **14** SELECTIVITY (controls ladder MOVED here from 15; "selectivity not exclusivity"; raw fires 4 / coph only α,β / resistance dead). name→talk-slide-14-selectivity.
- **15** SCALE-SHAPE (τ-curves; "only β genuinely multiscale" — β 16/16 .014→.001, α 12/16 .024→.007). name→talk-slide-15-scale-shape.
- **16** KILLED β→OFC → **held · placeless · lateralized** (reinstatement 10/10 p=.001 ⚠️; delocalized ✅; laterality ρ=.685 p=.029 ✅). name→talk-slide-16-beta-held-placeless-lateralized.
- **17** enc/inf setup — encoding co-equal (not a nuisance). **18** inference NOT β-only (δ/α/β; β 7/16 .005, α 7/16 .024; δ inference CUT); enc→OFC / inf→cingulate (⚠️ whole-graph); retitled off "(β only)".
- **19** → the BAND DISSOCIATION (cognition α/β · epilepsy δ/low-γ/β · β the bridge). **20** epi AUCs → δ.83/low-γ.82/β.745 + two populations + fusion + LOPO .87. **8** bet reframed to information-depth (dropped "invisible to"). **21** take-homes aligned.

**Consistency pass (mine):** guardrails clean (no live β→OFC / β-only / exclusive-detection /
drift-as-null — all only in "do NOT say" notes); OFC only on 18, band-dissociation only on 19.
Fixed one cross-slide number: slide 14 cophenetic α .019 (its ladder CSV) → **.024** (settled
source of truth + slide 15).

**Honesty state:** localization is now the WEAK leg — β trace delocalized; enc→OFC/inf→cingulate/
reinstatement/low-γ→cingulate are whole-graph, re-derivation-pending (task #13); tagged ⚠️ on-slide.
Talk leans on selectivity + scale-shape + epilepsy per the settled doc.

**Open / next:** (1) figure rebuilds — τ-curves (15), single-panel selectivity matrix (14),
laterality brain (16), retire fig_before_nulls/fig_whos_right (13/14), regen epi-marker fig AUCs
(20), slide-11 backbone+τ-sweep panels [task #13]. (2) filename mismatches — 14_the-nulls /
15_survivors-table / 16_beta-ofc-consolidation now hold selectivity/scale-shape/anti-OFC content;
needs a git mv wave (cascades to PLAN/Slides/README refs). (3) intro/method housekeeping from the
review doc (5/6/7/9/10/12 numbering+drift + Slides.md/README/PLAN hub re-sync). Nothing committed
this session.

---

## Session 3 — new_results_sec1 Wave 2 (spatial), rebuilt HONESTLY on the localization overturn

**Head:** finished the §1 figure set in `data/preprint/figures/new_results_sec1/` (old
`results_section1/` byte-untouched, git-clean). Wave 2's spatial figures were replanned around
the settled 2026-07-13 verdict: **the β trace is DELOCALIZED at every scale on mst@0.20** — the
whole-graph "β→OFC" was a degenerate-graph artifact (audit_174). Did NOT rebuild the OFC-enrichment
figures as enrichment; rebuilt them as the honest delocalization result instead.

**New figures this session (all mst@0.20, s≈5.6, matched-strength only — drift discarded):**
- `fig_descriptor_bloom_mst020` — raw-edge vs cophenetic patient-coherency bloom. Raw clears
  δ/α/β/low_γ (magnitude-driven, sees the trace broadly); cophenetic clears ONLY α/β. Message =
  cophenetic's contribution is band-SELECTIVITY, not exclusive detection. Sources: `controls_ladder/
  {per_cell,cohort_gate}.csv` (raw_fc + coph_meso, T_test).
- `fig_localization_scale_mst020` — the trace is strong but PLACELESS. System enrichment across
  s∈{1,2.83,5.65}: nothing clears BH in any band; β OFC p=.50→.78→.59 with obs sign-flip
  +91k→+130k→−322k; best trends low_γ→PFC(q.19)/α→insula(q.25)/δ→cingulate(q.44) all fail BH.
  Supersedes fig_trace_b/fig_trace2 OFC. Source: `localization_mst020_s{01.0,02.8,05.7}/system_enrichment.csv`.
- `fig_soz_scale_mst020` — the α-SOZ investigation, honest answer. (a) trace vs SOZ across scale:
  α/β lean weakly INTO the SOZ (ns, p.14–.39); the one BH result is **δ AVOIDS the SOZ**
  (non_soz q=.006 @ meso, 9/10, scale-growing). (b) disease-marker AUC vs scale (epi_arc): δ 0.85 /
  low_γ 0.84 strongest, α 0.67 weakest. ⇒ the SOZ's own scale is a mesoscale δ DISEASE signature,
  not an α cognitive trace. Sources: `localization_mst020*/soz_enrichment.csv` + `epi_arc_mst020/`.
- `fig_reinstatement_learn_mst020` — full rest_pre→task_learn→task_test→rest_post arc, Pat_08 β.
  Per-leaf cophenetic preservation-to-test warms pre(.16)→learn(.29)→[test]→post(.38); cohort
  β 10/10 reinstate @ s=5.6 (gate p=.001). Learn phase ADDED. On-backbone cohort panel uses
  `ms_mst020` ρ_sym (not the dense windowed replay CSV).

**Recon (Explore agent) established the ground truth** before any figure: β delocalized (16_localization_mst020),
enc/inf ANATOMICAL localization on mst@0.20 **NOT done** (only whole-graph; checkpoint says decisive,
"may not survive"), no per-pair ρ_sym vector on disk (only per-system/SOZ aggregates), δ-avoids-SOZ is
the sole BH spatial result. Caveat surfaced: `enc_inf_arc_mst020/config.json` backbone = "percolation (θ*)"
NOT mst@0.20 despite the dirname (feeds Wave-1 `fig_encinf_tau_gate`); band-nature verdicts robust, label misleading.

**CORRECTION (user caught it — my recon agent missed it).** enc/inf anatomical localization AND
the tissue gray/white pair-class were ALREADY computed on mst@0.20 — the recon grep looked for
"encoding/OFC" scripts and missed the pipeline's `17_localization_arc_mst020.py` ("arc") +
`19_tissue_pairclass_mst020.py` ("pairclass"). Both ran today (data on disk). No re-compute, no new
scope report (script 17 carries the 5-point preamble; scope = `2026-06-18_inference-mark-localization.md`).
Built the two remaining figures as pure plotting:
- `fig_encinf_localization_scale_mst020` (from `localization_arc_mst020/system_enrichment.csv`):
  **the enc→OFC / inf→cingulate double dissociation DISSOLVES on mst@0.20.** Encoding→OFC: β p=.156,
  clears 0/16; nothing clears BH for encoding anywhere. Inference→cingulate: the only survivor is a
  sub-threshold **α** lean, p=.008 **BH q=0.06** @ s=16 (7/8), misses BH — and it's α, not β
  (β-cingulate p=.23). Same fate as the trace → R2 anatomy is a degenerate-graph artifact.
- `fig_tissue_class_scale_mst020` (from `tissue_pairclass_mst020/cohort.csv`): **β trace is
  tissue-NON-specific** — gray-gray 14/16, gray-white 16/16, white-white 16/16, non-SOZ 15/16 all
  clear BH (q≈.015); it THINS only over **SOZ-SOZ (1/16)**. α near-miss everywhere (best q=.06),
  δ/low_γ null. Delocalization from the tissue angle + β-level SOZ-avoidance.

---

## Session — talk results-act RESTRUCTURE (9-slot) + methodological Q&A

**Head.** Restructured the talk results act (pipeline-onward) to a **9-slot machine→number→
meaning→discrimination→scale→payoff→closer** arc, driven by the settled results + narrative
audit + a deep methodological Q&A with the user that fixed three real overclaims. Plan saved
to `.agents/talk/2026-07-13_results-restructure-9slot.md` (RESUME ANCHOR; supersedes the
results-act structure of `REFOCUS_2026-07-13_three-results.md`).

**9 slots:** S1 pipeline(done) · **S2 measure ρ_sym (kept standalone, between pipeline &
taxonomy)** · **S3 4-fold taxonomy (kept standalone — user values it; cross-phase axis)** ·
S4 detect≠discriminate (pedestal-vs-peak) · S5 scale-signature (β deep/α single; scale axis) ·
S6 payoff (premises vs inferred order, held, distributed) · S7 one operator (cognition+epilepsy) ·
S8 take-homes · S9 thanks. Setup = machine/number/meaning; two-axes (S2) cashed by S3+S5.

**Three overclaims the user caught + fixed (all in the plan doc):**
1. **Pedestal-vs-peak is the rigorous selectivity** (not "method ladder / who's right"). Raw FC is a
   broad positive pedestal across ALL 6 bands (θ its 3rd-largest, +0.165; clears 4); cophenetic is a
   sharp α/β peak (θ NEGATIVE −0.017). **θ = the internal control → non-circular.** Numbers from
   `controls_ladder/cohort_gate.csv`. New figure to build (trivial). `fig_method_ladder` demoted.
2. **GRAPH CONFOUND:** in `14_controls_ladder_mst020.py:82–92`, raw_fc/strength/clustering run on the
   **DENSE** graph but geodesic/resistance/cophenetic on the **mst@0.20 backbone** → the ladder is
   pipeline-vs-pipeline, can't isolate hierarchy-vs-sparsification. **User is adding a same-graph
   control (simple descriptors on the backbone) with another agent.** Harden S4 after it lands.
3. **DETECTION-vs-FORM:** dropped "inference invisible at the fine scale / climb the hierarchy to see
   it" — raw FC DETECTS β inference (.010). Correct framing: raw detects *that*; the hierarchy shows
   it's a mesoscale **coarse re-grouping** (form). Within-cophenetic mesoscale-emergence (s₁→meso, both
   on backbone) is clean; only raw-vs-coph is dense/sparse-confounded.

**Plus:** localization fully dead incl. enc/inf (enc→OFC β p=.156 0/16; inf→cingulate α q=.06 misses
BH) → DROP "different cortex", the "where" is DISTRIBUTED. Everything scale-tested; anchor scale
claims to external facts (epilepsy bridge, laterality). Guardrails unchanged (no exclusive-detection,
no β→OFC, no β-only inference, no drift, matched-strength only).

**NEXT (post-compact):** lock 9-slot briefs → build S4 pedestal-vs-peak figure → rewrite S2–S9
(S2 measure standalone; S3 taxonomy kept + swap trace exemplar off Pat_06 δ → α/β). Await the
same-graph control before hardening S4's hierarchy-vs-sparsification attribution.

**Not built (superseded by delocalization):** pairglow-3D (a delocalized field = diffuse green).
Existing but not ported to §1: `18_laterality_duration_mst020` (β-left-lateralization = R2/patient-var story).

**Net §1 spatial verdict (all honest, all corroborating):** the β trace has NO anatomical home
(fig_localization_scale), NO functional home — enc/inf dissociation dissolves (fig_encinf_localization),
and NO tissue home — clears gray/white/mixed alike (fig_tissue_class); it thins only over the SOZ core,
where the one BH-significant spatial result is δ-avoids-SOZ (fig_soz_scale). §1's load-bearing story is
band-selective SCALE signature, not place.

**Reinstatement dendrogram fix (user caught "trees show only 2-3 splits").** Root cause: mst@0.20
cophenetic heights `T=1/ρ` are log-distributed over ~4-8 DECADES (rest_post s=5.6 = 5.3 decades,
98% of merges within the outer 1% of radius; a near-disconnected node on the sparse backbone gives
one enormous merge). `plot_circular_dendrogram` maps radius LINEARLY in height (`r = r_outer −
h/h_max·…`), so ~98% of merges crush onto the rim and only 2-3 top splits show. task_learn escaped
(1.96 decades, 0% crushed) → that's why task trees looked bushy and rest trees didn't. Dense-τ_min
heights are ~1.0 decade (0% crushed) → dense fig never hit this. FIX = `_common.log_height_linkage(Z)`
(display-only: remaps `Z[:,2]→log10`, topology + preservation colours untouched — same thing
`set_yscale('log')` does for the rectangular morph/ribbon, which were already fine). Reinstatement
now matches the dense reference's richness. Tanglegram (N=21, rectangular) mildly compressed but
message = crossings, intact; ribbon/morph already log-y.

Nothing committed this session.

---

## Session — controls-ladder same-graph control + descriptor bloom (the "another agent")

**Head.** Built the same-graph control the block above awaited. `14_controls_ladder_mst020.py:82-92`
now reads **ALL descriptors on the mst@0.20 backbone** (was raw_fc/strength/clustering on the
**dense** graph — the confound). Reran → `data/sparsified_arc/controls_ladder_apples/`. On the
corrected data the S4 claim **narrows honestly**: **α is cophenetic-EXCLUSIVE**; **β is a
CONVERGENCE band** — raw *backbone* edges representatively clear β (p=.024), so the old **"β
band-specificity invisible in the raw edges" is a DENSE artifact and is DROPPED** (matches prior
`pairwise_descriptor_ladder` β=convergence). The hierarchy's β value-add is SELECTIVITY (holds
α+β with no false positives), not raw detectability.

**Representativeness gate (new §1 standard, user-mandated — "1-patient-driven ≠ representative"):**
a cell is a "clear" only if cohort Wilcoxon p<.05 AND leave-one-out worst<.05 AND ≥3/10 patients
clear their OWN null. Kills clustering-α (1/10), raw-low_γ / geodesic-δ (LOO-fragile). Apples
representative clears (T_test): raw_fc→β; strength/clustering/resistance→**none**; geodesic→low_γ
(non-carrier); coph_meso→**α,β only** (β p=.001, α p=.007). Impl in `_common.representativeness_gate`.

**Decisions locked:** (1) **only work at scale** — τ_min single-fixed-scale row retired from the
ladder; (2) **representativeness gate** = the §1 bar for any clear; (3) **apples-to-apples** — all
descriptors on the backbone; (4) **Grassmann dropped** from new §1 (dense-only; dense is forbidden).

**Figures (new_results_sec1, shared `_common` gate+glyph layer):**
- `fig_controls_ladder.pdf` — representativeness-gated dot matrix; cophenetic the only row confined to α/β.
- `fig_descriptor_bloom_mst020.pdf` — extended to ALL 6 descriptors (2×3), glow gated on
  representativeness; only cophenetic (f) blooms both carriers. Minor TODO: panel-d γ_low label/p crowding.
- `_common.py` += `representativeness_gate` / `plot_gate_cell` / `gate_area` / `LADDER_DESCS` / `FUNCS` / `FUNC_LABEL`.

**Reconciliation:** consistent with this file's "trace is PLACELESS" verdict (β→OFC dead on backbone).
My ladder/bloom = the same-graph control for the S4 hierarchy-vs-sparsification slot. Nothing committed.

## Session eve — localization cohort-statistic audit (user challenge)

**Context:** User asked whether the scale-dependent framework "washed out" localization via a
test artifact — review completely, don't trust the preemptive analyses, do new controls.
**Done:** It IS partly a test artifact — but β→OFC does NOT come back. mst@0.20 scripts 16/17
used a per-patient Wilcoxon across the 5–8 covered patients (floor 1/2^K → DOA at K=5, cannot
clear BH for OFC/insula/MTL regardless of effect). Built `utils/metrics/cohort_localization.py`
(4 cohort stats: pooled-median / wilcoxon / stouffer / **sign-consistency permutation** + whole-grid
BH + LOO + frac_pos) and the bake-off `23_localization_statistic_bakeoff_mst020.py` (identical
backbone + MS null as script 17; 3 targets × 4 bands × 16 scales × 3 granularities incl
supersystem=limbic K=10). Verified manual==lib. **Every survivor is `low_gamma`; ZERO in β/α/δ**
(floor audit: low_γ 15% of cells beat the whole null vs β/α/δ 2% noise). β genuinely delocalized
(now on a valid powered stat). low_γ localizes to PFC/limbic/MTL/sensorimotor + SOZ (disease band).
C3 duration-clean (NON-DESTRUCTIVE ratio regression — no truncation). C4 (`24_...`) coph localization
value-add is real but low_γ-only. C5 rules out pruning (β 0 survivors at frac 0.20 AND 0.50).
**Issues:** none blocking; low_γ "localization" broadens toward all-systems at frac=0.50 (global
low_γ concordance) — focal reading sharpest at 0.20. frac=0.10 sweep still running (non-decisive).
**Decisions:** LOCKED — never truncate/length-match series (truncation null retired-invalid); use
ratio regression. β→OFC stays retired; state localization PER BAND, never "entirely dead".
**Files touched:** `src/lrg_eegfc/utils/metrics/cohort_localization.py`; scripts
`23_localization_statistic_bakeoff_mst020.py`, `24_localization_rawcoph_bakeoff_mst020.py`,
`07_figures/fig_localization_statistic_bakeoff.py`; report `2026-07-13_localization-cohort-statistic-verdict.md`;
scope `task-persistence-investigation/2026-07-13_localization-cohort-statistic-audit.md`;
`never-always-list.md` (+no-truncation). All UNCOMMITTED.
**Next:** commit; propagate low_γ-localization / β-delocalized-confirmed into the cascade (R1/R2
tables, directives, headlines still carrying dead β→OFC).

---

## Session — §2 dynamics figure (fig_arc_enc_dynamics panel a) rebuilt on mst@0.20 + KEY negative

**Head.** Reviewing the §2 enc/inf directive, the user asked whether the panel-(a) state-space
trajectory ("tracer vs resetter") still holds on the recovered scheme. It does **not** — and the
deeper finding is that the **raw windowed trajectory does not track the matched-strength trace at
all**. The honest reframe is a 4-archetype **dynamics gallery** (anchor/reset/reorganize/trace)
classified by phase-centroid distances, each panel annotated with its real trace. Cool figure,
honest, ready for §2 review. See [[windowed_trajectory_not_trace_2026_07_13]].

**The negative (verified fresh, mst@0.20 mesoscale s=5.6, band-selective patients):**
- Built `16_windowed_trajectory_mst020.py`: 30 s sliding-window |ImCoh| → mst@0.20 → LRG
  cophenetic at scale s → z-scored → PCA-denoised encoding/inference/residual embedding. Caches
  per (patient×band) embedding + discriminants to `data/sparsified_arc/windowed_traj_mst020/`.
- **Trajectory "hold" ⟂ real trace:** Spearman(hold_task, T_test) = **+0.13 (s1) / −0.04 (max)**
  across 18 band-selective cells → ~0. Smoking gun: **Pat_03 α** (cohort's strongest real α-tracer,
  T=+0.38) reads as **RETURNS/resetter** in the trajectory; null low-γ HOLDS. First (wrong) test
  used Pat_06 — the super-responder that traces in every band, so it couldn't show selectivity
  (user caught it).
- **Why:** raw persistence is near-universal — **corr(D_task, D_post)=+0.96** (once the task moves
  the geometry, rest_post follows and stays). The trace is the strength-CORRECTED part; the raw
  trajectory has no null subtraction, so it can't be band-selective. No scale/backbone fixes it.
  ⇒ the old panel-(a) "tracer/resetter" (dense, τ_min) is illustrative-only, NOT evidence.

**The honest reframe (delivered):** `17_dynamics_gallery_mst020.py` — cool KDE-blob + wall-contour +
glowing-flow gallery (reuses the original renderer; see refactor below), 4 archetypes separated by
**centroid distances** d(post,home) & d(post,task) [user's methodological fix — my first `align`-based
reset/reorganize split was fake: Pat_10 β was 0.40/0.42, equidistant]. Picks (each with real T for
honesty; only trace is a real tracer): **anchor** Pat_13 low_γ (d_home .14/d_task .24, T−.05) ·
**reset** Pat_15 α (d_home **.05**, T−.10) · **reorganize** Pat_03 δ (d_home **1.15**/d_task .97, T−.01)
· **trace** Pat_02 β (d_task **.35**, T**+.46**). Output `data/preprint/figures/new_results_sec2/
dynamics_gallery_s5.6.pdf`.

**Library refactor (no fork):** split `fig_arc_attractor_3d.portrait` → `portrait` (loads dense) +
**`render_portrait(ax,P,ph,tc,tag,subtitle=None)`** (pure renderer). Script 17 reuses it (204→149 lines,
duplication removed). `fig_arc_attractor_3d.py` edited behavior-preserving.

**For the §2 rewrite (directive `writing_directive_2026-07-13_sec2-encinf-conceptual-diff.md`):**
panel (a) of Fig 3 cannot claim "tracer vs resetter" as cohort evidence (trajectory ⟂ trace). Options
for §2: (i) keep the gallery as an illustrative "range of consolidation dynamics" showcase (honest,
labels = raw dynamics + real T annotated), NOT as the cohort statistic; (ii) drive panel-(a) endpoints
by the verified gate; (iii) replace with per-scale gate curves. The cohort evidence for §2 stays the
matched-strength decomposition (encoding β 16/16·α 3/16; inference-specific δ/α/β, NOT β-only) + the
localization value-add. **Next (user): compact → review §2 content against the new results.**

Nothing committed this session (scripts 16/17 + gallery + fig_arc_attractor_3d refactor uncommitted).
