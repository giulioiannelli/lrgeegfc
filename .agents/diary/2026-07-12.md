---
name: diary-2026-07-12
type: diary
era: IMCOH_ABS × COHORT_N10 × rho_sym
status: current
created: 2026-07-12
updated: 2026-07-12
---

# 2026-07-12

## Session — Talk emergency: propagator-vs-raw robustness resolved (Option 1), slide built

**Context:** Thursday talk; the "`1/K(τ_min)` ≈ `1/A`" discovery looked like a
crisis (propagator and adjacency distances agree on β but *disagree* on α/low_γ —
"which do I pick?"). Goal: decide the cross-phase-trace distance and have a
presentable, test-passing result set by Monday **without** redoing the pipeline
and **without** pretending multiscale.

**Head (the juice).** The α/low_γ disagreement was an illusion — it only appears
because the propagator was compared against **`1/A`, the one pathological
distance** (reciprocal blows up weak/noisy edges; flips Pat_02's α negative). Swap
in a well-behaved raw distance (`−log A`) and it **agrees with the propagator**:
**α + β clear the matched-strength gate under both; low_γ + all nulls fail under
both.** The trace is a **connectivity property**, the diffusion is not load-bearing
*for the trace* — which *confirms* the brutal review's "not multiscale" and is the
honest single-scale result to present.

**Decision (locked):** **Option 1 — keep the LRG propagator as the spine, add ONE
robustness slide.** Zero pipeline redo (propagator still needed for epi §3 coarse-τ
spreading + β→OFC localization). The "1/K≈1/A" worry is reframed as the talk's
strongest methods point. Withdrew the earlier audit_176 "migrate to −logA"
recommendation (no second functional; `A` = τ→0 limit of the one propagator).
Rejected leading with the raw HAC (would need a β→OFC re-localization on `−log A`,
unnecessary — science identical).

**Verified (R=200, reproduced bit-for-bit, 285 s):**

| band | 1/K prop | −logA | 1−A | 1/A (path.) | verdict |
|---|---|---|---|---|---|
| β | .032 ✓ | .019 ✓ | .042 ✓ | .032 ✓ | ROBUST (all 4) |
| α | .024 ✓ | .0098 ✓ | .032 ✓ | .053 ✗ | robust (fails only 1/A) |
| low_γ | .080 ✗ | .053 ✗ | .019 ✓ | .019 ✓ | form-dependent → drop |
| δ/θ/γ_h | ✗ | ✗ | ✗ | ✗ | null |

**Built:**
- `scripts/01_compute/figures_embedded/fig_rawform_robustness.py` →
  `data/outputs/figures/robustness/fig_robustness_propagator_vs_raw.pdf` (main
  slide: per-band per-patient MS-corrected ρ_sym, propagator vs raw −logA, stars on
  α+β only) and `…/fig_robustness_distance_matrix.pdf` (4-distance gate grid; 1/A
  isolated as pathological).
- `.agents/reports/2026-07-12_rawform-robustness-talk-verdict.md` — decision record
  + verified table + exact talk language + scope caveats.

**Frame for the talk:** ρ^coph = comparing the **hierarchical, nested-community**
organization across phases at a **single scale**; do NOT claim diffusion-multiscale
(degenerate on the dense graph; lives in the sparsification track).

**Next:** user integrates the robustness slide into the deck (Canva). Open (not
blocking): MEMORY.md at 20.1 KB — compact after the talk.

---

## Session — mst@0.20 RECOVERY arc (talk Thursday; user: "recover the multiscale trace")

**Head.** Recovered the whole story on a **minimal-modification density backbone**
`mst_union_top_fraction(W,0.20)`. Old audit_150 α/β cophenetic trace REPRODUCED +
sharpened under matched-strength, now τ-resolved & multiscale, band-selectivity
RESTORED. **Drift discarded** (task directional → trace monotonic → drift≡alt). MEMORY.md
compacted 20.6→18.3 KB. Full report: `.agents/reports/2026-07-12_mst020-recovery-arc.md`;
memory `sparsified_recovery_mst020_2026_07_12.md`.

**Delivered (all under `data/sparsified_arc/`, talk-facing NOT preprint):**
- **Sweep** (`12_…recovery.py`): dense@s=1==audit_150; multiscale turns on <density 0.2
  (C(τ) peaks dense 1.0→sparse 1.4–1.5); β sparsification-invariant, α needs strong-edge
  (mst keeps α .18, percolation kills it .02 at same density).
- **Trace** (`13_matched_strength_mst020.py`, R=200): β 16/16 scales p=.001 (scale-
  invariant); α 12/16 p=.007 (=.024@τ_min=audit_150); θ/low_γ clean null; δ/γ_high
  scale-artifacts (per-scale, NOT scale-max). β all 10 pt positive (7 sig); Pat_15
  β≈0 (right-hemi), Pat_06 super-responder, Pat_13 low.
- **Enc/inf** (`05_…` SA_BACKBONE=mst020): encoding (T_learn) strength-slaved null all
  bands; inference carries α/β; T_infspec β-only fine-scale; T_infspec_pe δ/α/β (β-only
  from full-graph did NOT survive — honest).
- **Epi** (`06_…` SA_BACKBONE=mst020): MARKER AUC δ.83/γl.82/β.745 beat null 7–8/10,
  multiscale>single 7–8/10; DETECTOR prec@5 .40 = single-scale, < audit_117 .60 (τ-sweep
  helps ranking NOT precision — honest). Band dissociation: trace=α/β, epi=δ/γl/β.
- **Controls ladder** (`14_controls_ladder_mst020.py`, RUNNING): raw/strength/clustering/
  geodesic/resistance(spectral)/coph@τmin/coph@meso × {T_test,T_learn,T_infspec,·|e} × MS.

**Figures** (`data/sparsified_arc/figures/{sparsification_recovery,ms_mst020,
enc_inf_arc_mst020,epi_arc_mst020}/`) all PNG-previewed to user on mobile.

**Errors caught:** scale-max gate again flagged δ/γ_high false-CLEAR → retracted, used
per-scale (feedback_report_tau_dependence). BLAS oversubscription (12 workers × LAPACK
threads) → pin OMP/OPENBLAS/MKL=1.

**Next:** controls-ladder verdict + figure; patient-variation~electrodes (pillar 6);
then talk integration.
