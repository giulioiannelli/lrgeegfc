---
name: diary-2026-09-08
kind: diary
era: IMCOH_ABS × COHORT_N10 → PAPER_FINALIZATION (Wave 0/1 closed)
status: current
created: 2026-09-08
scope: Wave 0 + Wave 1 integrated and pushed; state of the three paper results after Lanes W0-A, W0-B, S and E.
pointers:
  - .agents/reports/2026-09-03_paper-finalization-checkpoint.md
  - .agents/preprint/locked/PIPELINE_CONTRACT.md
  - .agents/reports/2026-09-03_lane-s-contrast-scale-structure.md
  - .agents/reports/2026-08-31_lane-s-scale-variability.md
  - data/paper_final/lane_e_encinf/verdict2/
---

# 2026-09-08 — Wave 0/1 integrated and pushed

## Head

Of the three paper results, only the epilepsy/SOZ marker is still standing untested; the other two have each lost a half. Persistence of the task state in `rest_post` is real against every independence null (matched-strength α, β q = 0.032) and directionally clean against the temporal-drift sham (sham reproduces α 12 %, β 25 % of the margin), but it is not yet significant against drift (best q = 0.146, power-limited by two sham realizations per patient). Everything scale-dependent is dead: the readout is flat across four decades of s (n_eff 1.06–1.26 vs a 2.88–3.44 noise floor; q_neff = 1.000 in all 36 contrast cells). The encoding/inference dissociation does not exist in any band (E6 q ≥ 0.66 in all six), `T_infspec` is provably contaminated by temporal adjacency (positive on no-task data, β q = 0.006), and the drift-regressor repair fails for a structural shared-`D_B` reason recorded so it is not retried.

## What was done today

- Merged all five lane branches (W0-A substrate, W0-B null ladder, Lane S parts A and B, Lane E) into `integration/wave0`; the `heat_multiscale.py` signature conflict keeps both `phases=` and `pair_mask=` and was verified numerically.
- Ran Lane E's verdict script, which had been committed but never executed; its permutation gate confirms the quick read.
- Committed the July-era talk/figure/report work that had sat uncommitted on `audit/cohort-n10-diagnostic`, merged it into `integration/wave0`, pushed both branches.

## Where the paper stands

| result | status |
|---|---|
| 1. persistence, scale-dependent | persistence: clears independence, directionally clean vs drift, not yet significant vs drift. Scale-dependence: dead. |
| 2. per-band dissociation | unsupported at n = 10 (β vs low_γ null both ways; no encoding/inference split in any band) |
| 3. epilepsy / SOZ | untested in this wave; structurally independent of every failure above |

## Next

1. Deepen the ordered-sham ensemble (currently two realizations per patient) — the cheapest compute that settles result 1 either way.
2. Launch the epilepsy/SOZ re-derivation (L3) from scratch on the locked substrate.
3. Only then rewrite `results_sec_1/2/3.tex` + `methods.tex`; the 2026-07-13 directives are stale.

## Afternoon — the user's reset, and the first construction that is actually scale-resolved

The user's message: we are nowhere near the objectives (scale-specific on τ across patients; band or spatial specificity; encoding vs inference; SOZ marker), and the four fundamental issues (FC construction, LRG with τ, nulls, cross-patient aggregation) must be re-examined from scratch with new paths and literature. Deliverable: `.agents/plans/active/2026-09-08_new-paths-to-objectives.md` plus the scope report `task-persistence-investigation/2026-09-08_hierarchy-level-decomposition.md`.

Two structural findings from today's feasibility probes (`scripts/01_compute/paper_final/feasibility/`, CSVs in `data/paper_final/feasibility/`): (1) on these graphs the heat kernel has 1.1–1.5 effective independent scales over s ∈ [1, 200], every readout below s ≈ 2 equals the raw FC, the kernel's wavelet above s ≈ 10 is unreliable across split halves, and the single specific-heat scale scatters 3–7× across patients on mst0.14/0.20 — so the whole-vector τ axis could never carry a scale-specific claim; (2) binning pairs by the level of the rest_pre diffusion tree at which they merge gives disjoint pair sets with independent statistics, and there the β trace is absent at k ≤ 4 modules and present at k ≥ 5 (within-patient trend p = 0.010, 9/10; fine-minus-coarse +0.30, p = 0.005; stable across backbone fractions). No null yet. Encoding vs inference does not separate by level (contrast within ±0.05 everywhere); the one construction left is a within-block early/late 2 × 2. Literature briefs from three parallel agents are being appended to the plan §6.

## 2026-09-09 morning — SOZ marker probe: the July 0.85 was label-seeded and mostly shaft adjacency

`feas_soz_marker.py` and `feas_soz_seeded_null.py` (CSVs in `data/paper_final/feasibility/soz_*`): every unsupervised node score (strength, degree, EVC, PageRank, heat-kernel K_ii, cross-shaft row sum at 8 scales, electrode density) sits at median AUC 0.45–0.60. The July 0.85 is the seeded affinity to the clinical SOZ set (uses labels). Shaft-matched random seed sets already score 0.68–0.70 (p95 0.80–0.82); with same-shaft pairs removed the only residual is δ (0.62–0.67 vs null 0.47–0.51, 6–7/10 p<0.05). Objective 4 reframed in the plan §5 as seed extension + β-trace/δ-SOZ-community overlap; §6.4 dataset scoping filled (ds004100 first, ds003029 second). User asked for lower credit burn: one Write + one Bash per script, no subagents.

## 2026-09-09 midday — Lane M1: β trace is level-specific beyond matched-strength and tree-only nulls

`lane_m1_level_decomposition.py` (60 cells, R=300, 24 min/8 workers; CSVs+npz in `data/paper_final/lane_m1_level/`). β heat-kernel readout, cross-shaft: fine(k≥5)−coarse(k≤4) contrast 0.244, 9/10, Wilcoxon 0.004, LOO 0.008; full MS null 0.014±0.015; tree-only null 0.068±0.033 (z 5.4, p<0.003). Step profile (≈0 at k≤4, 0.2–0.3 at k≥5). Stable across frac at s_ref=1, not at s_ref=4.72. rawW shows the same at 0.156 (convergence). θ beats full null but equals tree-only null → tree-only is the null to report. Per-level ordered sham (reliability gradient) NOT run yet; ρ/rel normalisation ill-conditioned. Plan §2 updated.
