---
name: 2026-07-10
type: diary
era: IMCOH_ABS × COHORT_N10
status: current
created: 2026-07-10
pointers:
  - .agents/preprint/supplementary/S2_drift_controls.md
  - scripts/01_compute/audit/audit_167_drift_null_ladder.py
  - .agents/guides/task-persistence-investigation/2026-07-09_pairwise-descriptor-ladder.md
  - scripts/01_compute/figures_embedded/fig_trace_section1_compound.py
  - .agents/preprint/overleaf/results_sec_1.tex
---

# 2026-07-10

## Pairwise ladder → DRIFT NULL: raw traces are drift; α + β-inference-specific drift-unverified

**Head.** Chased the user's "can we really detect the trace at the raw edge level?"
worry to its root. Answer: **raw-FC cross-phase traces are session DRIFT** (user
was right), and the same drift null — stronger than matched-strength and never
previously run — **also erodes the α whole-task trace and the β inference-specific
N2 decomposition**. Only the **β whole-task cophenetic trace → OFC clearly
survives** (p=0.042). Core flagship intact; N2 inference/encoding split + α now
drift-unverified. This is a KC-style "weak null passed, strong null fails."

- **Origin.** Pairwise-blind ladder (audit_166, S1) found raw edges + eigcent +
  closeness *appear* to reproduce the β inference-specific trace. User: "no it
  cannot be… maybe it's drift?" Correct.
- **Drift null (audit_167, S2).** Window a single **rest_pre** recording (pre-task ⇒
  any trace = drift by construction) into 5 ordered segments, run the identical
  ρ_sym arc for raw + cophenetic. Validation Spearman full-vs-cached = 1.000; real
  values reproduce audit_150 (β .198) / audit_152 (β inf .091) exactly.
- **Paired real-vs-drift Wilcoxon:**
  - RAW: nothing survives, any band. Raw β inference drift **+0.141 > real +0.124**.
    → raw = drift entirely.
  - COPH β whole-task: real .198 vs drift .051, **p=0.042 SURVIVES**.
  - COPH α whole-task: .101 vs .092, p=0.19 **fail**.
  - COPH β inference-specific: .091 vs .073, p=0.216 **fail** (indistinguishable).
- **Caveats:** conservative (short windows depress drift floor); "fail"=failed
  control not proof of drift; band-selectivity (real β-specific vs drift γ_low-peak)
  argues β inference isn't PURE drift but magnitude test can't confirm; rest_pre-based
  → **duration-matched drift null is the definitive follow-up** (NOT yet run).
- **First cheap sham was WRONG** (rho_sym(preA,preB,postA,postB) = split-half
  reliability of the pre→post shift, trivially ~1, NOT drift). Discarded; did the
  correct within-single-recording version.
- **Recorded:** preprint `supplementary/` (NEW folder, S1 ladder + S2 drift, per
  user), scope-report drift addendum, N2 warning banner, `locked/CONTROLS.md`
  (drift = new mandatory control), memory [[feedback_drift_null_mandatory]] +
  [[pairwise_descriptor_ladder_2026_07_09]], never-always-list.
- **NEXT (user-gated):** build the **duration-matched drift null** to adjudicate the
  N2 inference-specific claim. β→OFC whole-task core is safe. Runtime warning:
  windowed-FC drift null ~64 min (I under-estimated 2–4 min).

**CORRECTION (same session, before cementing).** I initially said "drift never run"
and alarmed that α whole-task fails drift — **both wrong.** The locked **C2
drift-floor** (`ρ_split>ρ_drift`, `locked/CONTROLS.md`, full-duration halves) already
covers the whole-task trace and **α/β/γ_low PASS** (0.0068/0.0137/0.0098). My
audit_167 is a *second, noisier* construction; where it disagrees (α/γ_low fail), C2
(better-powered, locked) is authoritative. Corrected verdict: **β whole-task
drift-clean under BOTH** constructions (solid); **α/γ_low construction-dependent**
(flag, not overturned); the **only genuine gap** is the **inference-specific
decomposition** (T_infspec_pe) which C2 does not cover and audit_167 shows is
drift-vulnerable (β p=0.22). Raw-FC = entirely drift. Records (S2, memory, diary)
corrected to this framing. Lesson: **grep `locked/CONTROLS.md` before declaring a
control gap.**

---

## 2026-07-10 (pm) — duration-matched drift (audit_168) + rank-size of per-node ρ_sym

**Head.** The duration-matched drift null the gap demanded is now run: it settles the
inference-specific question **decisively AGAINST the claim** — pure pre-task drift
reproduces *and exceeds* the inference-specific trace in every band (β real +0.091 <
drift +0.243; paired real−drift p=0.95; drift arc alone β drift>0 p=0.007). And a
rank-size (Zipf) decomposition of the per-node trace **refutes the user's Zipf guess
in the opposite direction**: a real trace is *delocalised*, not carried by a few nodes.

### audit_168 — duration-matched drift null (inference-specific)
- Five `rest_pre` slots sized ∝ real phase durations `[L_pre/2,L_pre/2,L_TL,L_TT,L_RP]`
  (fixes audit_167's equal-window SNR mismatch that `f=D_TT−D_TL` depends on). Welch
  once/window, sliced over 6 bands → **~10 min** (vs audit_167's 64; 6× win).
- **Inference-specific (coph): drift ≥ real, every band.** β real +0.091 / drift
  +0.243 / real−drift p=0.95 / drift>0 p=0.007. → **drift-confounded**, done.
- **Whole-task (coph): β real +0.198 ≈ drift +0.218 (p=0.54, level).** This is the
  **over-rejection sanity check**: audit_168 packs all 5 slots into ONE short recording
  (shrink s≈0.20) → over-estimates drift (locally-coherent) → *pessimistic upper bound*,
  NOT fair. The fair full-duration **C2** certifies β whole-task drift-clean (0.0137),
  so audit_168 does NOT touch the flagship. No fair full-duration null is constructible
  for the inference-specific split (5 durations sum > rest_pre).
- **Net:** β whole-task → OFC SAFE (C2). N2 inference-specific = **drift-confounded**,
  drop as load-bearing; do not headline "specifically multiscale inference trace".
  Confirms user's drift intuition + the pairwise-ladder "convergence phenomenon" was drift.
- Build `audit_168_duration_matched_drift_infspec.py`; data
  `data/audit/duration_matched_drift_infspec/`; fig `drift_infspec_duration_matched.pdf`.

### Rank-size (Zipf) of per-node ρ_sym — Zipf REFUTED, delocalisation = trace
- Size = additive per-node contribution `T_i = node_incidence_mean(c_sym)` (audit_154,
  cached all 6 bands), `mean_i T_i ∝ ρ_sym` → total trace = Σ node sizes. Pure figure,
  no compute.
- **Strong-trace β (Pat_08/05/03): ~88% of nodes co-move, top-10 carry <20%** →
  delocalised. **Reset β (Pat_15/10): a few idiosyncratic nodes dominate** (concentrated).
- Sign-independent metric (Gini of |T_i|, NOT the naive positive-Gini which is a −0.96
  sign tautology): β strong 0.32 vs reset 0.51; cohort **Spearman(ρ_sym, Gini|T|)=−0.50**
  (p=4e-5). Log-log |T_i| far flatter than Zipf slope −1 → **not a power law**.
- **Verdict:** a heavy tail (few drivers) is the signature of trace ABSENCE (resetters,
  θ/δ null); a real trace is delocalised. Quantifies [[dendrogram_persistence_gate_2026_06_04]]
  "broad = delocalised" + [[figure_trace_heat_brain_2026_07_09]] "β delocalised single-contact".
- Fig `data/outputs/figures/trace_ranksize_nodes.pdf`; build
  `fig_trace_ranksize_nodes.py`; scope
  `task-persistence-investigation/2026-07-10_node-ranksize-concentration.md`.

---

## 2026-07-10 (figures) — §1 Fig 2 rebuilt as a flat matplotlib compound + results_sec_1.tex converted

**Head.** Fig 2 (`fig:trace2`) is now ONE matplotlib compound PDF (**a** OFC brains /
**b** tissue-class / **c** SOZ divergence), replacing the LaTeX-minipage assembly, and
`results_sec_1.tex` points at it. Fig 1 was already a single compound (untouched, locked).
The compound HAD to be a **flat `plt.figure()`** — the locked nilearn-forbids-SubFigures
gotcha ([[figure_compound_matplotlib_mosaic_2026_07_09]]), **re-confirmed empirically this
session**: even a SubFigure built from a custom-margin gridspec is ignored when a nilearn
glass brain sits on the root (its content sprawls across the whole figure; brains stack).
Fully vector (0 rasters — nilearn stayed vector), edge-bleed gate clean.

- **build_fig2 (`fig_trace_section1_compound.py`) — flat rects in figure coords.**
  a = `p_ofc.draw_brain(fig, rect=(0.03,0.665,0.955,0.335))` (top, full width) + compact
  system legend; b = `build_panel(axB)` bottom-left; c = two `scatter_panel` + one
  `forest_panel` bottom-right. b/c are authored for wide standalones ⇒ fonts scaled to
  tile via new `_scale_axes_fonts(axes, factor)` (≈0.60). Band palette + clears/does-not-clear
  became ONE shared bottom legend (was per-panel, spilled). `W,H = 7.2×5.7`.
- **Shared-panel-helper refactors — these touch the standalones too; defaults preserve
  standalone behaviour:**
  - `fig_trace_b_ofc_localization.py`: `draw()` split → `draw_brain(target, rect=…)` +
    `legend_handles(compact=)`. `draw_brain` REQUIRES a real Figure (nilearn root-coords).
  - `fig_trace_d_tissue_class.py`: `build_panel(ax, draw_letter=)`; "non-SOZ ↔ non-SOZ"
    row label now **two lines** (frees left space, plot grows leftward); annotation
    "α recruits the SOZ" → minimal **"α holds SOZ"** with a **black** arrow that lands on
    the dot; standalone left margin 0.26→0.195.
  - `fig_trace_f_soz_divergence.py`: `scatter_panel(ax,band,tag,xlabel=,ylabel=)` — α/β is
    now a **boxed top-left corner** label (was inside, lost in the point cloud); in-figure
    "SOZ ρ_sym=…" **removed** (caption-only). `forest_panel(ax, sym_x=0.03)` — α/β moved to
    the gap **right of the row labels** (was overlapping them); significance ∗ moved to the
    **right of each marker** (was stacked above → overlapped); xlim 0.47→0.53 so the
    rightmost ∗ clears.
  - (Together these implement the user's 5 verbatim Fig-2 fixes.)
- **`results_sec_1.tex`.** Fig 2 float: 3× minipage `\includegraphics` → single
  `\includegraphics[width=\textwidth,height=0.80\textheight,keepaspectratio]{fig_trace2.pdf}`
  (matches the §2 pattern). Caption: **(a)** "rings"→"saturated markers" + pale = 1-patient /
  grey = n.s./WM; **(b)** all-6-bands colour + filled/open encoding (dropped the `\bigstar`
  the forest never draws) + α-SOZ arrow x-ref to (c); **(c)** numbers **re-verified against
  a fresh recompute** — α exemplar +0.428→+0.43, β −0.390→−0.39, cohort α SOZ–SOZ +0.41
  p=0.005, β +0.08 p=0.31 (all match). 0 minipages, balanced floats/table.
- **Loose ends.** Standalone `fig_trace_{b,d,f}_*.pdf` are now stale on disk but **no
  longer referenced by §1** (only the talk might use them — rebuild if so). Fig 1 include
  left as `width=\textwidth` (locked; landscape aspect, no page overflow). See
  [[figure-session-framing]], [[3d-pairglow-ofc]].

---

## 2026-07-10 (pm-2) — §2 Fig 4 landscape rework + inference length/drift framing reconciled

**Head.** Reworked Fig 4 (`fig_arc_enc_anatomy`) to landscape 3-column and, at the
user's insistence, **removed the length-matched "provisional" demotion of the inference
→ cingulate cell** — presenting it on its matched-strength footing. Key call for future
agents: this **deliberately does NOT follow audit_168's "drop inference-specific as
load-bearing" verdict in the main text**, because reading the audit_168 code shows that
drift null is unfair for a *partial* correlation. `results_sec_2.tex` is copy-paste-ready.

### Fig 4 `fig_arc_enc_anatomy.py` — portrait → landscape 3-column
- Was 3 stacked region ROWS (11.6×15.6, aspect 0.74). Now 3 side-by-side COLUMNS
  (18×7.4, aspect ~2.4), each lollipop-over-glass-brain, one shared bottom legend.
  Flat-figure pattern kept (nilearn ignores SubFigures — see [[compound-mosaic]]).
- Band chips added top-left of each column (`β 13-30 Hz` / `γ_l 30-80 Hz`, palette-
  coloured via `band_color`) — the y-axis is anatomical SYSTEMS, users kept reading it
  as bands. Panel **b** recoloured green→**low-γ blue** (`band_color("low_gamma",0.80)`);
  panel b annotation trimmed to `q=0.035` only (dropped "8/8 patients / SOZ-excl ρ").
- Panel **c** cingulate: hatched "provisional" diamond → **normal significant winner**
  (saturated C_INF, ∗); brain markers saturated too. `arc_f.draw_lollipop` lost its `lm`
  arg (lenmatched still computed for console log only). Docstrings/legends updated.

### Inference length/drift framing — the load-bearing decision
- **Removed** from R2.6 + Fig 4 caption + R2.7 synthesis: the length-matched TRUNCATION
  control (q=0.15/0.30 "fails → directional lead / provisional"). User's objection is
  correct — truncation discards data (power loss), a biased control.
- **Kept / now load-bearing for length:** R2.4 (`does not track recording length`) — the
  cross-patient duration-ratio correlation (β inference vs test/learn ratio ρ=+0.10,
  p=0.78; α/high-γ track length but carry no inference). Power-preserving, clean.
- **Drift:** NO drift label in main §2. Reason (verified in `audit_168` code, `_arc`→
  `_prho(f,p,e)`): the duration-matched drift null applies the SAME partial correlation
  to 5 collinear windows of one short `rest_pre`; a partial correlation is inflated under
  collinearity, so drift +0.243 > real +0.091 is largely that artifact. audit_168 itself
  is flagged "pessimistic upper bound, NOT fair; no fair full-duration null constructible"
  for the inference split. Band-selectivity (real β vs drift γ_low-peak) argues against
  pure drift. → inference-specific is **drift-UNVERIFIED (no fair null), not confounded**;
  drift discussion stays in `supplementary/S2_drift_controls.md`.
- **⚠ Tension for future agents:** audit_167/168 diary verdict says "drop inference-
  specific as load-bearing." Main §2 keeps it (matched-strength q=0.010 + duration-ratio
  + β-selectivity). This is DELIBERATE and user-directed — do NOT silently re-add the
  length caveat, and do NOT silently "drop" inference per audit_168 without re-reading
  this. A reviewer applying the locked drift-mandatory rule strictly may still push back.
- Files: `fig_arc_enc_anatomy.py`, `fig_arc_e_lowgamma_cingulate.py`,
  `fig_arc_f_inference_cingulate.py`, `.agents/preprint/overleaf/results_sec_2.tex`.
  Both §2 PDFs landscape + fully vector; all `\FigRef` resolve; braces balanced.

---

## 2026-07-10 (pm-2) — CORRECTION: the "inference-specific is drift-confounded" verdict is WITHDRAWN

**User correction (investigated deeper): the drift-confound conclusion for the
conditional trace was a hallucination — "there is no way to drift-unconfound the
conditional trace; it was an issue of how the drift was conducted."** Accepted, and
my own numbers back it: a windowed drift null is NOT valid for a **partial/conditional**
functional.

- `T_infspec·e = partial_ρ(f, p | e)` already conditions on encoding `e`, so
  drift-removal is entangled with the estimator — no clean external windowed-drift-null
  analog (unlike a simple whole-task correlation, which C2 handles fairly).
- **The tell the NULL (not the trace) is broken:** the sham arc is built entirely from
  pre-task `rest_pre`, where NO consolidation signal can exist, yet audit_167/168 return
  a *significantly positive* conditional trace on their own (β drift>0 p=0.007) and even
  exceed the real value. A valid null must sit ≈0 on a no-signal arc.
- **N2's inference-specific claim stands** on matched-strength (β-only p=0.0098; MS
  returns ≈0 on no-signal) + duration-robustness. **Do NOT drop it.** The whole-task
  drift material (C2, raw-whole-task=drift) is separate and remains valid.
- **Records corrected** (withdrawal propagated): N2 banner, `locked/CONTROLS.md`,
  `supplementary/S2_drift_controls.md` (rewritten to a cautionary methodological record)
  + README, memory [[feedback_drift_null_mandatory]] + [[pairwise_descriptor_ladder_2026_07_09]],
  MEMORY.md. audit_167/168 scripts+data KEPT as the cautionary record (never-delete).
- **Lesson:** whole-task simple correlation → windowed drift null OK; conditional/partial
  trace → NOT OK, validate with matched-strength. (Second overreach this session on the
  same topic — I was too eager to call a control "failed".)

---

## verify-tau (cont.) — removed the stale R2 τ≈2.6 "mesoscale" leftovers from directives/ + headlines/

**Head.** Follow-on to the τ-verification work ([[lrg_tau_choice]] correction, see
2026-07-09 diary): the τ-sweep "mesoscale" R2 result was dropped from the live manuscript
2026-07-09 (PI call; `results_sec_2.tex` header + `results_paragraphs/` archived
`R2.2_mesoscale-integration.tex`, kept the R2.3–R2.7 numbers), but `directives/` and
`headlines/` still described τ≈2.6 as a **current/verified** result. Reconciled every live
doc to the authoritative position: **read at τ=1/λmax only; multiscale via the single-τ
dendrogram hierarchy, not a τ-sweep.**

- **Anchored to ground truth first:** live `overleaf/results_sec_2.tex` (no mesoscale
  paragraph; header documents the drop) + `overleaf/methods.tex` §methods_arc (reads τ_min
  alone). No live doc referenced the stale mentions' sources → safe to reconcile.
- **Edited (current-SOA docs):** `headlines/RESULTS_OUTLINE.md` (R2.2 → retired note; 2
  rationale mentions stripped); `headlines/README.md` (6 mesoscale/τ-sweep strips);
  `headlines/02_encoding_vs_inference.md` (frontmatter scope, the 2026-07-07 banner item 1
  → 2026-07-09 final drop, spectral banner τ-sweep ref, §A scale paragraph, the N2.2 detail
  block, "three legs"→"two legs", 2 dated checklist entries → RETIRED, history pointers
  preserved); methods directive `2026-07-07_...` (Scale-dependence paragraph → live τ_min
  version + 3 header comments); `results_paragraphs/R2.3_...tex` ("Follows R2.2" fixed);
  `2026-07-09_methods-full-review-fixes.tex:9` check-note; `writing_directive_2026-06-25_...md`
  (incidental descriptor); `writing_directive_2026-06-24_results-section-full-draft.{tex,md}`
  (dedicated mesoscale paragraph + 5 summary mentions → retired notes).
- **Did NOT touch** the drift thread (retracted above — inference split stands) or the
  Grassmann hierarchical-vs-eigenmode thread; both are separate and current. Preserved all
  research history (never deleted a dated record — marked superseded + kept pointers).
- **Verified:** zero live docs assert τ≈2.6 as current; all 3 edited `.tex` brace-balanced;
  my flagship edits survived the concurrent drift-banner edit. Guide/memory/methods τ
  correction from the 2026-07-09 session is the parent context.
- **Folder hygiene (user: "archive").** `git mv`'d 8 superseded directives out of
  `directives/` (top-level 32→24): the dead monolithic `2026-06-24_results-section-full-draft.{tex,md}`
  (→archive/2026-06; replaced by per-paragraph `R*.tex` + live `results_sec_*.tex`), plus
  the orphaned (ref=0) pre-ρ_sym May drafting directives (`2026-05-19_beta_red_paragraphs`,
  `_methods_beta_latex_verification`, `2026-05-20_beta_results_application`,
  `_methods_audit_application`, `2026-05-22_alpha_results_drafting` →archive/2026-05) and the
  completed `investigation_directive_2026-06-08_epi-propagator-soz-marker` (→archive/2026-06).
  **Kept** everything with live refs, the current-era 2026-07-* ρ_sym methods directives, and
  the referenced methods-reference notes; restored 2 that a live directive still pointed to;
  repointed one cross-ref to its archive path. Zero dangling references (verified). Nothing deleted.

---

## 2026-07-10 (talk) — R-2 ρ_sym "gate bloom" + R-3 (β→OFC) slide

**Head.** Talk per-slide expansion continued (`.agents/talk/slides/`, one md placeholder
per Canva slide). Two deliverables: a **new radial "gate bloom" figure for R-2** built on
the main cross-phase ρ_sym gate (not the arc functionals), and the **R-3 β→OFC** slide.
Now 16/20 slides done (I-1…7, M-1…4, R-1…3).

- **Bloom generalized to the gate.** `fig_reasoning_bloom.py` had 4 consolidation-arc
  functionals (`T_learn/T_test/T_infspec/T_infspec_pe`), radial value = a derived trace
  correlation, NOT ρ_sym. Added a 5th, **`rho_sym_gate`**: radial value IS per-patient
  ρ_sym (`obs_rho`), cohort glow = `gate_p_sym` (Wilcoxon vs matched-strength). Reads
  `data/audit/rho_sym_gate/{per_patient_per_band,cohort_summary}.csv` (audit_150) via two
  1-line branches in `load_func`/`cohort_p`; reuses every drawing primitive. `__main__` now
  argparse (`--func`, `--no-coverage`); default still builds all. Printed table matches R-2
  exactly (β +0.198 p.032 BLOOM · α +0.101 p.024 BLOOM · γ_l/γ_h/δ p.080 · θ −0.038 p.784).
  Output `data/preprint/figures/_drafts/fig_reasoning_bloom_gate_DRAFT.pdf`.
- **Why it fits R-2:** petals = per-patient spread (no band is trace-free); wedge glow =
  cohort gate (β/α bloom, rest hug the ring); the two longest petals in the whole figure —
  γ_l Pat_05 +0.86 and γ_h Pat_05 +0.92 (rim chevron), in NON-glowing wedges — are
  *peak ≠ verdict* made visual. Palette = turbo band_hue (bloom-family convention, NOT the
  canonical `band_color`; flagged as presenter's aesthetic DOF).
- **R-3 (β→OFC).** Numbers from the 2026-06-10 audited verdict + ρ_sym-era structure file:
  OFC = only robust matched-strength enrichment survivor (R=1000, BH q≈0.010–0.015, all 4
  conditions, LOO-robust); PFC/SM depleted; hotspot-not-container (bilateral, ~59% within-OFC
  pro, 5/10 impl. 4+); strength-independent. Pays off the I-2 Wilson-2014/Schuck-2016 bet.
  Figure = `fig_trace_b_ofc_localization.pdf` (systems-enrichment glass brain, OFC green /
  PFC-SM orange) + optional 3D pairglow talk asset. **Load-bearing honesty encoded:** the
  β→OFC address is system/node enrichment, NOT per-pair arcs (×1.46 β vs ×1.12 α,
  patient-variable → arcs illustrative); Hip/MTL sub-threshold hint only; shaft-collapse
  robust (ring retracted); α does not localize.
- **⚠ Note for talk figures:** §1 Fig 2 was refactored into a compound (`fig_trace2.pdf`)
  earlier today, so standalone `fig_trace_b_ofc_localization.pdf` on disk is pre-refactor
  (message-correct when viewed, but rebuild `fig_trace_b_ofc_localization.py` for a fresh
  cut if the talk uses the standalone).

## 2026-07-10 (figures + editor) — §1 Results mini-review: staleness audit, citations, figure/prose fixes

**Head.** Ran a full publish-readiness pass on `results_sec_1.tex` (numbers →
ledgers, citations → Crossref, figures/captions, Methods consistency). Verdict:
message is strong and the arc is complete; **48/50 numbers were already current
ρ_sym-era values**. Fixed the two real staleness bugs + one figure↔prose conflict,
added four verified citations, tightened two over-claims, and reconciled a
Methods/Results count. One genuine gap remains flagged (windowed-reinstatement has
no Methods home).

**Fixed (all landed + verified):**
- **fig_trace1.pdf rebuilt** — panel-c reinstatement p was `0.003` (normal-approx
  `wilcoxon_z`) vs prose `0.001` (exact signed-rank). Harmonized the figure to the
  exact one-sided test in `fig_trace_e_reinstatement.py` (scipy `wilcoxon(...,
  method="exact")`); now prints `10/10 … p=0.001`, LOO 0.002 (= 1/2^10, 1/2^9). Whole-tree
  0.05→0.58 intact; still vector (only the pre-existing panel-a 3D carrier brains are
  raster, ~750K — the accepted 3D-render exception). fig2 untouched.
- **`results_sec_1.tex:8` stale RAW value** — "ρ^coph = −0.28 in Pat_15" was a
  pre-hierarchy *raw* number; current γ_low cophenetic min is Pat_14 = −0.22 (Pat_15
  ρ_sym = −0.13). → "−0.22 in \patient{14}". Same directive-comment error at
  `R1.1_reasoning-imprint.tex:12` still to fix at source.
- **γ_low peak rounding** — "≈+0.86" → "≈+0.85" (exact 0.853, Pat_05).
- **Citations (+4, all Crossref-verified; bib entries appended to
  `directives/results_paragraphs/references.bib`):** `tambini2010enhanced`
  (already in bib, now deployed — post-rest memory signature) + `ellenbogen2007human`
  (offline relational memory) on the opening paragraph; `wilson2014orbitofrontal`
  (OFC-as-cognitive-map, the foundational ref) alongside schuck2016 on the OFC
  sentence; `schuckniv2019sequential` on the replay-burst alternative being refuted.
  Reworded the OFC sentence so `park2021inferences` (grid code = EC/mPFC, not OFC)
  attaches to inference-over-the-map, not to OFC specifically.
- **Over-claim tightened** — OFC LOO "robust to leaving out any single patient
  (worst-drop q≤0.060)" → "leave-one-out robust in three of the four (worst-case
  q=0.060 in the fourth)" — q=0.060 > 0.05, so the old phrasing over-sold the n=5 case.
- **Table caption** — "the full six-band map is given in Methods" → "…in the
  Supplement" (Methods has no six-band table; it already defers Grassmann detail to
  the Supplement). **A supp six-band probe table must be built to honor this.**
- **methods.tex 9-vs-10 systems** — Methods listed "ten a-priori systems" (incl.
  "remaining subcortex") but the localizer is 9-way over cortical systems
  (README/audit/Results all say nine). → "nine a-priori cortical systems", subcortical
  folded into the V-exclusion. ⚠ confirm subcortical contacts were excluded (not binned).

**Flagged, NOT fixed (need a decision / separate pass):**
- **Methods gap (#1):** the windowed-reinstatement result (N1.6 — 20 s windows, the
  per-window g contrast, 5 Laplacian read-outs, replay/burst negatives, time-shuffle +
  node-permute nulls) is load-bearing in §1 Results but has **no Methods subsection**.
  A referee will look for it. Draft a "Temporal reinstatement" Methods subsection.
- Supplement six-band probe table (see caption edit above).
- Density: a couple of the longest stacked-em-dash sentences (band-gate para;
  multiscale-geometry para) are heavy for NN's broad readership — optional tightening,
  left as-is to preserve the precise voice.

**Provenance:** two parallel verification agents — number-staleness (traced ~50
claims to `rho_sym_gate/`, `epi_wm_stratified_rhosym/`, `grassmann_cluster_extent/`,
`replay_states/`, ANATOMY_LEDGER, VERDICT_LEDGER) and citations (Crossref-verified all
6 existing + 4 proposed bib entries; all real, all attach correctly). Files touched:
`overleaf/results_sec_1.tex`, `overleaf/methods.tex`,
`directives/results_paragraphs/references.bib`,
`figures_embedded/fig_trace_e_reinstatement.py`, `data/.../fig_trace1.pdf`.

### follow-up (same day) — Methods gap filled + two items closed
- **Methods reinstatement subsection DRAFTED** (`methods.tex` new
  `\subsection{Temporal reinstatement of the trace}`, `ssec:methods_reinstatement`,
  before the encoding/inference decomposition). Covers: 20 s windowing (α/β reliable
  only ≥10 s; windowed recipe = full-recording estimator in the limit), the per-window
  task-likeness \(g_w = \rho_S(\Dcoph^w,\Dcoph^{taskt}) - \rho_S(\Dcoph^w,\Dcoph^{rspre})\),
  occupancy shift \(A_1^{post}-A_1^{pre}\) one-sided Wilcoxon (α/β-averaged, LOO), the
  matched-strength inheritance, scale-invariance (τ-sweep) + 5 read-outs (coph/directional/
  raw-prop/subspace/normlap, 8–10/10 p≤0.007), the transient-replay cohort negative
  (burstiness/k-means/sequence × band × scale × target × pair × timescale to 0.2 s floor,
  time-shuffle+node-perm nulls), and the two honest limits (0.2 s floor; A2b p=0.08 → "held"
  not "sharpened"). Provenance: audit_124–141 / replay-states report. Struct verified
  (34/34 eqn envs, labels unique). Added a "(Methods)" cross-ref in the §1 reinstatement para.
- **Six-band-map caption reverted** Supplement→**Methods** (PI: the map exists in
  methods material). NB: methods.tex still has no literal six-band *table* — pointer
  resolves to the per-band gate description, or a small table should be added there.
- **Subcortical/9-systems RESOLVED empirically:** localization CSVs have **no `subcort`
  bin** (systems are all cortical) → the "nine cortical systems, subcortical excluded"
  methods edit is exactly right; no confirmation needed.

### follow-up 2 — six-band Methods table + fig1 lossless shrink
- **Full six-band probe map added to `methods.tex`** (`tab:probe_band_full`, at the end of
  the gates section before Anatomical localization): β✓✓ / α✓— / γl—✓ / δ—(✓ᵂ) / γh—— / θ——,
  cophenetic + Grassmann cohort p per band (from `grassmann_cluster_extent/cohort_summary.csv`
  + `rho_sym_gate`). δ Grassmann footnoted as weak (cohort mass p=0.005 but LOO max p=0.055,
  not LOO-robust). Results caption "given in Methods" now resolves to it. Main-text 4-row
  table (β/α/γl/θ) kept as the crisp illustrative subset.
- **fig1 4.34 → 2.25 MB (−48%, lossless, still fully vector).** Cause diagnosed: panel-b is
  twelve `pcolormesh` fingerprint matrices at `rasterized=False` (~170k vector quads) — the
  no-raster rule's file-size assumption breaks at 12×119². Lossless recompress (mutool/qpdf)
  gained 0 (matplotlib streams already Flate-optimal); **Ghostscript re-emission** (`/prepress`,
  images FlateEncode + no downsampling → brain raster preserved at 300 ppi, vectors kept vector)
  halves it, pixel-identical on visual check. Baked into the build as a guarded `_optimize_pdf()`
  post-step in `fig_trace_section1_compound.py` (no-op if gs absent / no gain); **fig1-only**
  (fig2 is 180 KB, un-gs'd). Rebuilds now stay ~2.25 MB. No rule change — matrices remain vector.

## 2026-07-10 (talk) — R-4 (two probes) + NEW coph×Grassmann confirmation matrix; stale preprint_34 flagged

**Head.** R-4 slide written (17/20). User rejected the existing dissociation figure as
"old" — correctly: `preprint_34_bands_coph_grassmann_dissociation_map.py` builds its
**cophenetic axis from the pre-ρ_sym `ρ_split`** (`matched_strength_surrogate_split_baseline/`),
only the Grassmann axis is current. Rebuilt the double dissociation on the **current ρ_sym
gate** (audit_150) as a fresh **confirmation matrix** (user-picked via previews over
slopegraph / dissociation-bloom).

- **New figure `fig_coph_grassmann_confirmation_matrix.py`** (canonical figures_embedded home).
  6-band × 2-probe truth table: cophenetic (ρ_sym gate_p_sym) | Grassmann (cluster-mass p).
  Cell shade ∝ −log10 p in per-probe ramp (cool coph / warm Grassmann), uniform vector ✓ on
  cells that clear MS gate, right-margin verdict, β row gold-highlighted. Data-driven from the
  two CSVs; `band_color` (canonical palette) for row labels; `--no-annotate` toggles p-text.
  Output `data/outputs/figures/talk/fig_coph_grassmann_confirmation_matrix.pdf`.
- **Verdicts (current ρ_sym × Grassmann):** β coph .032✓/grass .005✓ = **BOTH (only band)**;
  α .024✓/.348 = local-only; γ_low .080/.005✓ = global-only; δ .080/.005✓ = global-only
  (**LOO-fragile** .055, marked crimson); γ_high .080/.060 & θ .784/.159 = neither. Shape
  unchanged from the old ρ_split map, numbers tighter. Matches the independent
  `methods.tex tab:probe_band_full` (β✓✓/α✓—/γl—✓/δ—(✓ᵂ)/γh——/θ——) built same day.
- **⚠ Flagged (not fixed — manuscript scope):** `preprint_34` still emits the stale ρ_split
  cophenetic axis (+ its γ_low epi-X arrow uses ρ_split `epi_stratified/`). Needs a ρ_sym
  migration before the manuscript uses that quadrant map.
- **Layout gotcha (fixed):** equal-aspect + portrait 6×2 data squeezed x-units → column
  headers collided ("copheneticGrassmann"). Dropped `set_aspect("equal")`, enlarged to
  6.9×6.8, added a Grassmann-column x-gap (GX=1.18) that doubles as a local|global separator.

### follow-up 3 — fig1 diffmaps rasterized (authorized rule exception): 4.34 → 1.45 MB
- **PI-authorized override of the locked "FC matrices stay vector" rule**, fig1 panel-b only.
  Switched `fig_trace_c_diffmaps.py` `pcolormesh(rasterized=False)` → `imshow(interpolation=
  "nearest", aspect="auto")` (dropped the now-redundant `invert_yaxis`; imshow origin='upper'
  reproduces M[0,0] top-left). Each N² fingerprint now embeds as a **280×280 image @300 ppi**
  (~5–10 KB) instead of ~N² vector quads. Visually identical (full-fig + 400 ppi zoom checked:
  crisp hard cell edges, green β/α borders + vector text/labels unchanged, orientation matched).
- **Size chain: 4.34 MB (vector) → 2.25 MB (gs lossless) → 1.45 MB (imshow @300 ppi).** With
  imshow the gs step now reports "no gain" (kept as a guarded safety net). fig1 build ~5 s.
- **Rule status:** this is a documented, figure-specific exception (twelve 119² matrices in one
  compound broke the rule's small-file assumption); the vector rule still stands elsewhere. If we
  want it codified, add an exception line to never-always-list.md — NOT done yet (PI said "try").

---

## 2026-07-10 (talk R-4) — GRASSMANN DRIFT NULL (audit_169) + dual-null bloom triptych

**Head.** User rejected the coph×Grassmann confirmation matrix as "horrible" and asked for a
bloom-family triptych (raw · coph · Grassmann) showing per-patient spread against the null, with
"clear = overcome BOTH matched-strength AND drift." Building it forced running the **Grassmann
drift null for the first time** (audit_169) — and the result **cuts against the two-probes story**:
**Grassmann β does NOT clear the windowed drift (real 0.425 vs drift 0.235, p=0.116)**; cophenetic
β is the **sole survivor of the dual gate**. User chose to present the honest triptych as-is.

- **Premise-check first (brutal honesty saved a false figure).** User said "raw drowns in drift,
  coph+Grassmann overcome it." Verified against `drift_null_ladder`: **whole-task** raw drowns
  (β real 0.244/drift 0.139, paired p=0.246; NO band clears) ✓; coph clears β only (0.042), α
  FAILS windowed drift (0.188). NB the earlier "raw=drift" in this diary was the *inference-specific*
  arc — whole-task needed re-deriving; done.
- **audit_169 — Grassmann whole-task windowed drift.** Mirrors audit_167 (window rest_pre into 5,
  pre=w0/tt=w3/post=w4) + audit_66's `t_g_at_all_k` (T_G>0=trace). Welch-once/window (audit_168
  optimization) → **~7.5 min** (timed 1 patient=45s first). Real T_G loaded canonical from
  `grassmann_inference_arc` (onl, median-over-k). **Cohort drift gate (paired Wilcoxon real>drift):
  δ .246 / θ .385 / α .577 / β .116 / γl .188 / γh .065 — NOTHING clears.** Data
  `data/audit/grassmann_drift_null/`.
- **The finding:** matched-strength → coph(β,α) & Grassmann(β,γl,δ) agree on **β** (double
  dissoc holds). Drift → raw ∅, **coph β only**, Grassmann ∅ (β trends p=.116). → **cophenetic β
  = sole dual-gate survivor; the fine multiscale tree is drift-robust, the coarse global subspace
  is NOT** — consistent with [[pairwise_descriptor_ladder_2026_07_09]] "fine-grained not
  coarse-subspace." **Caveat:** windowed drift is conservative (also fails coph α, which fair C2
  rescues 0.0068); NO fair full-duration Grassmann drift exists → Grassmann β = drift-**unverified**,
  trending, NOT "confounded." Flagship β itself drift-clean under BOTH windowed (.042) + C2 (.0137).
- **Dual-null bloom** added to `fig_reasoning_bloom.py` (`--dual raw coph grassmann`): reuses all
  primitives; per-patient triangles + grey MS annulus + **red dashed drift waterline** per wedge +
  **weakest-link glow** (`eff_p=max(p_MS,p_drift)` → glows only if clears both). Data plumbed from
  rho_sym_gate (coph) / pairwise_descriptor_ladder raw_fc T_test (raw) / grassmann_inference_arc +
  grassmann_cluster_extent + grassmann_drift_null (Grassmann). Outputs
  `data/preprint/figures/_drafts/fig_dual_bloom_{raw,coph,grassmann}_DRAFT.pdf`. QA'd all three:
  raw no-glow (petals impaled on drift line), coph β-glow, Grassmann no-glow.
- **R-4 slide reframed** (`.agents/talk/slides/R-4_two-probes.md`): "two strength-immune probes
  corroborate β (MS); only cophenetic β survives drift." Confirmation matrix demoted to MS-only
  alternative. **⚠ affects manuscript:** the "two independent probes" R-4/N1 claim needs the drift
  nuance (Grassmann drift-vulnerable) if it goes to paper. Memory [[grassmann_whole_task_drift_vulnerable_2026_07_10]].
- **Row-overlap fix (imshow):** the filled `imshow` maps butted together at the old
  `hspace=0` (pcolormesh's margin had hidden it). Switched to `imshow(aspect="equal")`
  (drops the redundant `set_box_aspect`) + `hspace=0.10` with the two-row block height
  grown by the same factor so panels stay square and don't overflow into panel c.
  Verified: clean inter-row gap, matrices crisp, still 1.45 MB. `fig_trace_c_diffmaps.py`.

## 2026-07-10 (pm-3) — Drift ladder extended to the full descriptor menu (audit_170); "convergence" reframed

**Head.** User challenged the audit_166 "convergence phenomenon" (raw/eigcent/closeness
appear to detect the β inference-specific trace under matched-strength): *raw can't see it,
it disappears against the drift null*, and *can't accept eigcent/closeness either*. Built
**audit_170** — extends audit_167's within-rest drift null (raw+coph only) to the full
classical menu (strength, clustering, eigcent, pagerank, closeness). Verdict is nuanced and
logged honestly (it did NOT fully confirm the clean hypothesis I'd predicted).

- **CONFIRMED (user's core point):** the within-rest drift arc (pure pre-task rest, no
  consolidation possible) gives a **strongly positive** β `T_infspec·e` for **every**
  low-level rung — raw .141 / strength .186 / clustering .174 / eigcent .167 / pagerank
  .166 / closeness .200, all drift-alone p=0.001 — **≥ the real values**, and **no**
  low-level rung's real value significantly clears its own drift floor (r>d p ≥ .31; raw
  sits *below* it, real .125 < drift .141). Their MS "detection" is **drift-dominated**.
- **Where I over-predicted (corrected):** eigcent/closeness do NOT collapse *below* drift
  the way raw does, and cophenetic does **not** out-filter them on the bias-robust
  differential (coph−raw p=.032 ✓ β-only; coph−eigcent .31, −closeness .14, −pagerank .12,
  n.s.). Cophenetic's own real (.091) does not beat its own drift floor either (r>d p=.216).
- **So drift is NOT the clean "only-multiscale" discriminator for inference.** It cleanly
  demotes RAW, shows the conditional functional is drift-biased for everyone (confirms the
  locked "windowed drift = invalid absolute gate for a conditional trace"
  [[feedback_drift_null_mandatory]]), and shows cophenetic carries the **smallest** drift
  contamination (.073 ≈ half). The clean discriminator remains **band-selectivity**
  (audit_166: only cophenetic recovers α + silent θ; raw + every node scalar fail). The
  inference-β claim stands on **MS (≈0 on no-signal, .0098) + duration**, not drift.
- **Docs corrected** (the "convergence is a clean MS witness" claim was misleading):
  memory `pairwise_descriptor_ladder_2026_07_09.md` (description + part-3 note + DRIFT
  UPDATE rewritten to audit_170), supplementary **S1** (scope/head/table/Reading§3/verdict
  → "drift-contaminated + not band-selective, not a genuine witness"). Whole-task raw=drift
  and C2 unchanged.
- **Net honest frame for §2 method-comparison:** the pairwise/scalar convergence is
  drift-contaminated + not band-selective → not a second witness; multiscale = band-selective
  + least-drift + fine-grained (Grassmann-null). Do NOT write "raw can't see it" as absolute,
  and do NOT claim drift cleanly singles out cophenetic on inference — band-selectivity does.
- **Build:** `scripts/01_compute/audit/audit_170_drift_ladder_descriptors.py` (reuses
  audit_167 windowing + arc; Welch-once-per-window optimization → 6× fewer calls, 242s/10pat
  parallel). Data `data/audit/drift_ladder_descriptors/`.
- **⚠ Still OPEN for §2 (earlier review):** the two Fig-4 defects (4a misplaced PFC
  annotation, 4b winner-not-biggest / null-not-shown), the 9-vs-10-system BH q reconciliation
  + caption SOZ-reversal, and the zero-citations gap remain unaddressed — separate from this
  drift thread.

## Talk deck — merged intro slides I-6 + I-7 → single I-6 (2026-07-10 pm)

- User: "compress I6 and I7 in a same slide, we dont need two for the kick." Done.
- **`.agents/talk/slides/I-6_the-bet.md`** (git mv from `I-6_form-mirrors-form.md`) now holds
  the merged beat: funnel **poetic → precise → methods hand-off** = form-mirrors-form (mirror
  not proof, closes the I-1 race-tree loop) → the bet stated precisely (band-specific / held /
  multiscale trace; trace-anchor-reset-emergent taxonomy said once) → kick to Methods
  ("can we even measure connectivity without volume conduction?"). Figures kept: race-tree +
  circular dendrogram two-panel, minimal taxonomy strip. Ref: Park/Miller/Boorman 2021.
- **`I-7_the-bet.md` git-rm'd** (content fully absorbed; preserved in git history + merged file).
- README index: Intro is now **6 slides** (I-1…I-6), deck 20→19. R-1…R-4 done; **R-5 next.**

## Talk deck — final 5 slides R-5…C-1 drafted; deck first-pass COMPLETE (2026-07-10 pm)

- User: "finish the last 5 slides" (intro restructure coming later, we'll revise then).
  Built all five against the SSOT `2026-07-07_talk-structure-20min.md`, matching the
  R-1…R-4 format (Main point / Concepts / Figures+paths / References + Keep-honest).
- **R-5 held-not-replayed** — reinstatement 10/10 p.001 (HELD not replayed; replay=clean
  neg, N4 closed); 3 tiers 6 trace/2 undet(13,15)/2 reset(10,14); "who carries it"=
  LATERALITY (carriers left L+.34/R+.05, resetters right-only; α symmetric control; stable
  trait ρ.95). Figs: fig_trace_e_reinstatement + per_patient_slopes + laterality-brain HTML.
- **R-6 encoding-vs-inference (setup)** — e/f/p decomposition, T_infspec=partialcorr(f,p|e);
  multi-step chaining=MOTIVATION not a scale. Fig: fig_arc_a_decomposition_forest + build schematic.
- **R-7 inference-persists-β (climax)** — β infspec +0.091 vs .005 p.0098, dur-immune ρ.10/p.78,
  multiscale-robust NOT peak, other bands null; enc→OFC q.010; low-γ→cingulate solid, inf→cingulate
  LEAD only. Keep-honest: CONVERGENCE (raw/eigcent/closeness see it→"fine-grained not exclusively
  multiscale"); Grassmann NO 2nd-witness (audit_165); "conditional=drift" WITHDRAWN, stands on MS+dur.
  Figs: inference reasoning-bloom + fig_encoding_inference_dissociation + per_band_phenomenology.
- **R-8 epi coda (N3, cuttable)** — SOZ diffusion community; co-diff AUC δ.80/γl.74/β.69, det AUC.81
  9/10; β SPARES / α recruits. Keep-honest: SOZ labels NOT surgery; δ clinical vs β cognitive (same
  operator diff band); marker not diagnostic. Figs: fig_trace_f_soz_divergence + fig_soz_diffusion_community.
- **C-1 take-homes** — 4 beats (method emerges / N1 β→OFC / N2 inferred-order / N3 clinical); no-behavior
  end, OFC hotspot-not-container, N1=MS corroboration+coph-β drift-survivor, N2 fine-grained. Fig: per_band_phenomenology.
- Used current `results_section2/` set (regenerated since SSOT) — N2 no longer needs legacy pre-ρ_sym figs.
- **Deck first pass DONE: 19 slides (I-1…6, M-1…4, R-1…8, C-1).** Awaiting user's new intro structure → revise pass.


## audit_171 — raw-vs-coph inference localization: the drift-free separator (pm-4)

User challenge: why do non-multiscale detectors match ρ_coph — they "should fail
somehow we haven't considered." Locked a new rule mid-session
(`feedback_fresh_recompute_not_prior_text`): recompute load-bearing/unwelcome
numbers FRESH, cross-check the cache don't trust it (2 subagents disagreed 6/10 vs
10/10 on coph β-inference breadth — at least one hallucinated).

- **Fresh gate bit-identical** to `cohort_summary.csv` (max|Δ|=0, 60 cells): the
  convergence numbers were REAL, not hallucinated.
- **Whole-task correction (my error, user-caught):** raw T_test "clears" the
  audit_166 MS-median gate at 0.024, but that gate is NOT drift-controlled and raw
  whole-task = entirely drift (audit_167/C2); coph survives MS (ρ_split .005) AND
  drift (C2 .0137). So there is NO convergence problem on whole-task — I invented
  one by quoting an uncontrolled p. Convergence is real ONLY at the inference cell.
- **Inference cell:** raw ties coph under MS (T_infspec_pe 0.0098 both; eigcent
  0.0020) and drift is invalid (conditional). ρ_coph = f(W) ⇒ NO detection test can
  separate them. Separator must be a non-detection axis.
- **audit_171 (the separator):** coph inference_pe → **CINGULATE** BH q **0.030/0.040**
  LOO-robust (both epi); raw **PLACELESS** (no BH system; leads sensorimotor). They
  do NOT co-localize. coph reproduces audit_160 to the decimal (anti-hallucination).
  ⇒ On the inference cell LOCALIZATION, not detection, is the multiscale value-add.
- **CAVEAT / keep out of preprint until closed:** MS-only; duration/length control
  PENDING and its validity for a conditional functional is user-gated (same concern
  as drift).

Build audit_171; data `data/audit/raw_vs_coph_inference_localization.csv`; scope
`.agents/guides/task-persistence-investigation/2026-07-10_raw-vs-coph-inference-localization.md`.
