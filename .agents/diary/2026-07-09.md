---
name: 2026-07-09
type: diary
era: IMCOH_ABS × COHORT_N10
status: current
created: 2026-07-09
pointers:
  - .agents/reports/2026-07-07_talk-structure-20min.md
---

# 2026-07-09

## Session — 20-min talk structure + travel-ready presentation worktree

**Head:** Built and locked the full structure for a 20-minute talk on the
multiscale-network (LRG) results, then set up a parallel `presentation/`
branch + git worktree so the interactive HTML deck can be built on this
laptop and continued on the travel laptop from Sunday.

- **Talk structure crystallized** in
  `.agents/reports/2026-07-07_talk-structure-20min.md` (single source of
  truth): 20 slides (7 intro + 13 methods/results/conclusion), exact ρ_sym
  numbers, verified figure paths, figure gaps, and Crossref-verified intro
  references (DOIs).
- **Intro rebuilt** as a literature-anchored "wonder" arc — dropped the
  generic "brain as a network" opener; cold-open on transitive inference,
  braids *cognition-is-hierarchy* with *method-reads-hierarchy*. Attaches:
  Behrens 2018 / Wilson 2014 / Schuck 2016 / Tambini–Davachi 2010 /
  Betzel–Bassett 2017 / Villegas 2023 / De Domenico 2016 / Nolte 2004.
  Preference saved to memory `feedback_talk_intro_literature_anchored`.
- **Workflow:** committed + pushed all WIP; branch `presentation/talk-html-deck`
  created in a worktree at `.claude/worktree/talk-html-deck` (nested in the
  repo so the `lrgeegfc` env auto-activates) with `data/` symlinked (it is
  gitignored, so absent from the worktree and from a fresh clone).
- **Next (worktree chat):** build the self-contained interactive HTML deck;
  convert headline figures PDF→SVG into a tracked `presentation/assets/` so
  they travel with the branch; regenerate the N2 encoding-vs-inference figure.

## Session — Methods full review + 12 fixes applied to methods.tex

**Head:** Deep-reviewed the whole post-ρ_sym Methods (`.agents/preprint/overleaf/methods.tex`),
wrote a find/replace directive, then applied all of it. Math is sound;
the problems were results leaking into Methods, one stale factual claim,
a notation clash, redundancy, and a section-order break — all now fixed.

- **Directive:** `.agents/preprint/directives/2026-07-09_methods-full-review-fixes.tex`
  (find/replace sheet for the Overleaf body; keep for syncing there).
- **Applied to the local mirror** methods.tex (44 ins / 53 del; 344→335 lines;
  8 subsecs & 33 eqs preserved; anatomy block byte-identical after move):
  - **A (correctness / no-results):** taskl "not used" → now points to arc
    (arc uses taskl); removed in-Methods numbers (one-patient 0.775/0.767/0.008,
    R²=0.06, "positive in every band", "as at β", "β clearing both", "verified
    to 1e-6").
  - **B (notation):** PageRank α / Katz β → η / κ (they clashed the alpha/beta
    bands); "see after~" → "Section~"; kernel list → "full set in Supplement".
  - **C (redundancy):** deleted stale `\paragraph{Anatomical enrichment.}` (anatomy
    subsection is self-contained now); trimmed the 3×-stated continuous-spectrum
    justification in the cophenetic subsection.
  - **D (order):** moved Anatomical-localization subsection above Tissue →
    Comparison → Anatomy → Tissue → Arc → Epi = clean R1→R2→R3.
- **Verified in repo before editing:** k∈{2..112} correct (audit_66, minN=113=Pat_10);
  detector kernels (audit_101), k=3 seeds (audit_117), arc scales 1/2.61/6.81 w/
  6.81 collapse-contaminated (audit_103d). No deprecated tokens (VI(k)/optimal_threshold).
- **Left alone (on purpose):** label `sssec:methods_anatomy` on a `\subsection`
  (opaque string, ~31 refs across preprint tree — not worth breaking).
- **Open (offered, awaiting user):** relocate the 5 note/review .md files out of
  `preprint/methods/` (git mv; two superseded→archive, three→directives/).

## Session — Results §1 (trace) prose compression + probe×band table

**Head:** Tightened Results §1 for NN directly in the Overleaf-canonical
`overleaf/results_sec_1.tex` (mirrored into modular `directives/results_paragraphs/R1.{1,2,3}.tex`).
Closed the R1.3 "setup-without-payoff" gap, de-duplicated the strength-matched-surrogate
definition, and pushed R1.1's magnitude defense onto the figure. Numbers unchanged
(ρ_sym-current); science untouched — this was a writing pass, not a re-analysis.

- **R1.1** ¶2 compressed: one surrogate definition (was 2×), "+0.20 is large" anchored on
  the null value (≈0.01) + `\FigRef{fig:trace_a}` instead of an inline paragraph, cleaner
  low-γ-peaks-but-fails, tighter segue to R1.2; ¶3 split.
- **R1.2** two mega-sentences split (controls enumeration; the two honest limits). Content
  intact (OFC q=0.010–0.015, PFC+sensorimotor depleted, hippocampal hint).
- **R1.3** the Grassmann paragraph was a setup with no payoff (introduced the 2nd probe,
  never gave the dissociation). Compressed it to a brief comment + **moved the band×probe
  dissociation to `Table~\ref{tab:probe_band}`** (β both / α multiscale-only / γ_low
  global-only / θ neither; verified numbers from audit_150 + audit_66/70). Former 4th prose
  ¶ retired. Table needs `booktabs` + `\checkmark` (amssymb) — flagged in a `% Requires:`
  comment, house style. **OPEN:** `\FigRef{fig:trace_d}` label collides with the tissue
  figure slot — reconcile at figure assembly.
- **R1.4/1.5/1.6** left as-is (already tight, defensible, ρ_sym-current).
- **Next (step 2/3):** consolidate `fig_trace_a..f` into a single §1 composite figure, panels
  carrying the quantitative defense the prose shed (esp. panel a = per-patient ρ_sym vs the
  null band). `fig_soz_diffusion_community` dropped from §1 (R3 territory; user: "aesthetic").

## Session (cont.) — preprint reorg: `overleaf/` mirror folder

**Head:** Created a single `overleaf/` mirror holding the manuscript sections as
they live in Overleaf (copy-paste source of truth); dissolved `methods/` and
`results/`. Methods notes went to `directives/` (+archive). All cross-refs fixed.

- **New `.agents/preprint/overleaf/`** (with `README.md` = the copy-paste protocol):
  `methods.tex`, `results_sec_1.tex`, `tables/beta_per_patient.tex`. These are the
  files we edit + you paste to/from Overleaf. `\input` paths inside them mirror
  Overleaf, not the repo (excluded from path rewrites).
- **Dissolved** `methods/` and `results/`. Methods companions →
  `directives/` (grassmann_cluster_extent, neurophysiological_interpretation,
  displacement_taxonomy); superseded reviews → `directives/archive/2026-05/`
  (methods_revision_2026-05-18_cophenet, methods_section_review_2026-05-19).
- **Cross-refs:** mechanical exact-path rewrite across .agents (30 files); ASCII
  folder-trees + routing prose in README/WRITING_GUIDE updated; reorg-history
  entry appended. Zero dangling links to the moved files.
- **methods.tex** = the 12 review fixes from the earlier session, unchanged by the move.
- **Note / open:** a pre-existing `.agents/plans/active/2026-07-04_agentic-folder-reorg.md`
  proposes a broader reorg still referencing `preprint/methods/`; reconcile it with
  this `overleaf/` change when that plan is next picked up.

## Session — Results §1 figures: hero tracer-chord + redo plan (IN PROGRESS)

**Head:** Started §1 figure consolidation → one mosaic (panels hero + a–f), each
`fig_trace_*.py` kept runnable standalone. HERO done (chord network, held β trace as green
"tracer" links); R1.4 prose role-fixed. TODO: R1.6 3D brain, R1.4 tissue redo, mosaic assembler.

- **Prose done** (`.agents/preprint/overleaf/results_sec_1.tex` + mirror `results_paragraphs/R1.4`):
  R1.4 now leads with its ROLE — the pathology confound ("one confound left to clear before it
  can be read as a signature of cognition"); payoff = "healthy cortical coupling, not the
  diseased core." (R1.1/1.2/1.3 compression + probe×band `Table~\ref{tab:probe_band}` logged above.)
- **Library done:** `plot_chord_with_highlight` gained a `vertex_rgba` passthrough
  (`src/lrg_eegfc/visuals/network_chord.py`, 2 edits) → callers can set neutral/custom node
  colors; default None = shaft palette (backward-compatible).
- **HERO done:** `scripts/01_compute/figures_embedded/fig_trace_g_tracer_chord.py` →
  `data/reports/results_section1/fig_trace_g_tracer_chord.pdf` (delivered; user reviewing).
  Exemplar Pat_08 β, **rest_post backbone**. GREEN = top-120 "tracer" pairs = per-pair reading
  of ρ_sym: `score = ½[rankz(D_task−D_preA)·rankz(D_post−D_preB) + (A↔B swap)]`, top positive.
  Neutral grey nodes (no probe color, per user). ILLUSTRATIVE (per-pair noisy; load-bearing =
  node/system β→OFC) — caption must say so. Reuses `plot_chord_with_highlight` + `_load_inputs`;
  halves caches `data/cache/imcoh_lrg_halves/Pat_08`. Raster-embed PDF (graph-tool), 1.8 MB.
  `build_panel(ax)` exposed for the mosaic. Ran in 8 s.
- **Mosaic plan (one Fig 1):** hero (tracer net) + **a** band-forest (R1.1; rebuild so
  per-patient dots sit vs the ~0.01 null band) + **b** OFC glass-brain (keep) + **c**
  raw→multiscale scatter (keep) + **d** tissue (REDO) + **e** reinstatement (keep) + **f**
  β-spares/α-recruits 3D brain (REDO). Architecture: refactor each `fig_trace_*.py` to expose
  `build_panel(ax)`, keep `main()` standalone, new `fig_trace_mosaic.py` imports them.
- **3 user fixes:** #1 tracer network = DONE. #2 R1.6 → 3D brain (nilearn
  `plot_brain_connectome_at_n`, embeddable in axes; SOZ via `build_epi_masks`; exemplar, β+α,
  SOZ ringed) = TODO. #3 R1.4 tissue = drop 36-cell heatmap → compact β-by-tissue forest
  {gray↔gray★, gray↔white, white↔white, SOZ↔SOZ}, full matrix → supp = TODO. NB `fig_trace_d`
  (heatmap) and `fig_trace_f` (slope chart) both "horrible" per user AND overlap on SOZ →
  split cleanly (d = tissue, f = SOZ brain).
- **Data map (Explore agents):** tissue CSV `data/audit/epi_wm_stratified_rhosym/
  pairclass_rhosym_cohort.csv` (band, stratify wm/epi, config, cohort_obs_rho_sym, ms_wilcoxon_p).
  Per-node trace `data/audit/per_node_trace_decomposition_rhosym/per_node.csv` (rho_sym, T_raw,
  polarity carrier/anti/neutral, is_epi, x/y/z). MNI via `load_spatial_metadata` +
  `prepare_spatial_coordinates(to_mni=True)` or `load_channel_regions`.
- **Open decisions (user):** hero backbone rest_post vs rest_pre; green density (120);
  optional OFC/carrier node tint. `\FigRef{fig:trace_d}` label collision (R1.3 → tissue slot) —
  reconcile at mosaic assembly.
- **Run env:** `/home/giulio/Documents/miniconda3/envs/lapbrain/bin/python <script>` (direct;
  conda run fails at the cross-compiler hook).

## Session — R2 figure: consolidation trace as a state-space attractor

**Head:** After the user rejected three flat/1-D attempts, built the R2 encoding/inference
figure as a **live windowed state-space trajectory** — the trace as a dynamical system. The
LRG state is windowed across the whole recording and drawn as a time-colored path through an
encoding×inference×residual cube: `rest_pre` oscillates, `task_learn` jumps (encoding),
`task_test` drifts (inference), then a **tracer stays out / resetter comes home**. Amplitude
of the task excursion stops mattering — the result is the limit set.

- **New tooling:** `audit_164_windowed_attractor_embedding.py` (windowed LRG states → PCA
  embedding per patient/band, ~16 min cohort, saved `data/audit/windowed_attractor/*.npz`).
  Figure `fig_arc_attractor_3d.py` → results_section2/. Scope report updated
  (`.agents/guides/task-persistence-investigation/2026-07-08_windowed-consolidation-flow.md`).
- **Drift control (load-bearing):** rest_pre is earliest ⇒ a slow drift would push every
  rest_post out. Resetters returning to baseline is the built-in control — validated:
  tracers post/task ≈ 1.0 (Pat_06/08), resetters ≈ 0.0 (Pat_10/13).
- **Honest split (brutal-honesty gate):** windowed per-patient separation is a NOISY proxy
  for the verified trace (corr with gate ≈ −0.37; Pat_07 verified-trace but windowed-returns,
  Pat_02 windowed-stays but unverified). So the two trajectories ILLUSTRATE clean exemplars;
  cohort statistic of record stays the verified ρ_sym gate (audit_152/163), 6/10. Cohort strip
  = verified trace, exemplars pinned in. Never color a cohort element by the gate on a
  windowed axis (incoherent).
- **User-driven polish:** tight per-axis cube limits; Gaussian-kernel within-phase smoothing;
  phase clouds as lit 3-D ellipsoids **colored by the colormap's central color at each phase's
  median time** (one unified time language: dark rest_pre → yellow rest_post); colorbar marks
  the phase transitions. Fully vector, PDF-only.
- **Archived** the three rejected attempts → `scripts/archive/2026-07_superseded-arc-figures/`
  (fig_arc_h1/h2 flat panels, fig_arc_ribbons_3d 1-D curves) + README pointer; removed their PDFs.

## Session (cont.) — §1 figures split to TWO figures + panel d redone
- **Mosaic → TWO `figure*`** in `overleaf/fig_trace_mosaic.tex` (user: "break into two"; LaTeX
  single-mosaic proportions are fiddly). **Fig 1** (trace exists + hierarchy) = g hero | a bands |
  c raw-vs-multiscale (row 1) + e reinstatement (banner); **Fig 2** (anatomy) = b OFC brain
  (banner) + d tissue | f SOZ. Proportions via aspect-proportional minipage widths (equal row
  heights, fills line) — "good not pixel-perfect"; pixel-perfect needs a Python composite w/ one
  shared legend (b nilearn + e dendrograms embedded = big refactor, not done). Captions written.
- **Panel d REDONE** (`fig_trace_d_tissue_class.py`): "two blocks" heatmap → compact β tissue
  forest {gray↔gray +0.24★, gray↔white +0.19★, white↔white +0.10, SOZ↔SOZ +0.08}; 6-band matrix
  preserved behind `--style matrix` (→ Supp). `build_panel(ax)` exposed. Numbers verified vs R1.4.
- **STAGED (not done):** (1) per-figure re-lettering — baked letters scattered (Fig 1 g/a/c/e,
  Fig 2 b/d/f); renumber a,b,c,… = one letter constant per script + rerun 6 (~4 min), user-gated.
  (2) f → 3D brain (nilearn redo, the "two points two lines" fix).

### Update — §1 FINALIZED for Overleaf (re-lettered + callouts wired)
- **Re-lettered per figure** (user greenlit): Fig 1 = a hero, b bands, c raw-vs-multiscale, d
  reinstatement; Fig 2 = a OFC, b tissue, c SOZ. Edited the baked `\mathbf{·}` letter in 6
  scripts (hero got a NEW letter added to `build_panel`; c unchanged) + reran all 6 (~40 s);
  hero `a` verified visually (bold, top-left, no clip). Mosaic captions + header comments synced.
- **Callouts wired** into `overleaf/results_sec_1.tex`: all 6 paragraphs now cite their panel
  (¶1 hero Fig1a + bands Fig1b, ¶2 OFC Fig2a, ¶3 raw-vs-multiscale Fig1c, ¶4 tissue Fig2b,
  ¶5 reinstatement Fig1d, ¶6 SOZ Fig2c). Fixed the 2 dead placeholder labels (fig:trace_a,
  fig:trace_d). Mirrored all callouts into `directives/results_paragraphs/R1.{1..6}.tex`.
  Verified: 7 `\FigRef{fig:trace[12]}` calls, mosaic labels fig:trace1/fig:trace2 present, no
  dead labels left (only a resolved-note comment in R1.3).
- **§1 is paste-ready, single file.** The two `figure*` blocks are now EMBEDDED directly in
  `overleaf/results_sec_1.tex` (Fig 1 after ¶1, Fig 2 after ¶2); the separate `fig_trace_mosaic.tex`
  was DELETED (user: figures belong in the results file, not a separate `\input`). Flow: upload
  7 PDFs + paste the one results_sec_1.tex; preamble needs graphicx/booktabs/siunitx/amssymb + macros.
  Still-open polish (non-blocking): f → 3D-brain redo (swap the PDF later, label/callout unchanged).

## Session — talk intro re-weighted: cognition kickstart → network-field spine

**Head:** revised the 20-min talk *structure* (`.agents/reports/2026-07-07_talk-structure-20min.md`)
so cognition is only the KICKSTART and the presenter's network-science expertise is the spine.
Transitive inference compressed to ~1 slide; intro now pivots into the higher-order / multiscale
frontier; the hierarchy parallel demoted to an explicit "reflection, not result." Content-only pass
(deck itself is being built in Canva).

- **Part I rebuilt (I-1…I-7):** I-1 cold-open (kept) → I-2 task+data compressed into one slide
  (TI basics only, sEEG/epilepsy/n=10/4-phase, no-behavior caveat, OFC/rest seeded lightly) →
  **I-3 field is stuck** (pairwise analysis near-exhausted; frontier = hypergraphs) → **I-4 the
  missing middle = multiscale**, and pairwise linear correlators hide higher order because the
  propagator e^{−τL̂}=Σ_n(−τ)^nL̂^n/n! is a sum of higher powers of connectivity → I-5 LRG turn →
  I-6 **form-mirrors-form as a bounded reflection** → I-7 the bet.
- **M-3 augmented:** added the propagator-re-projected-on-links-at-increasing-τ demo beside the
  matrix — HMN lights nested blocks, then a *complete graph with heterogeneous weights* shows the
  same hierarchy (brain-FC regime, Villegas outlier case → justifies running LRG on dense imcoh_abs).
- **New figure gap (needs code, synthetic):** the HMN → complete-graph multiscale-recovery demo.
  Backbone of the I-3→I-4→M-3 pivot. Also added accuracy flag #7 (don't oversell the hierarchy
  parallel) + a "pairwise→multiscale→hypergraph" cartoon to the build list.
- **Refs:** added Battiston 2020/2021 + Santoro higher-order block, all flagged ⚠ unverified
  (Crossref pass still owed before the deck). Saved the framing preference to
  `feedback_talk_intro_literature_anchored` (kickstart-then-pivot + reflection-not-result).

## Session (cont.) — §1 figures moved to preprint output + rebuilds (IN PROGRESS)
- **Figures embedded inline** in `overleaf/results_sec_1.tex` (user: no separate `\input`
  mosaic) — two `figure*` (Fig 1 = g/a/c/e, Fig 2 = b/d/f); deleted `fig_trace_mosaic.tex`.
- **Beta per-patient table ARCHIVED** (orphan + ρ_split, superseded by Fig 1b): git mv .tex+.md
  → `directives/archive/2026-07/`; overleaf/tables/ removed; overleaf/README cleaned.
- **Output path MOVED**: `data/reports/results_section1` → `data/preprint/figures/results_section1`;
  all 11 writer scripts repointed (sed, verified). Future figures write there.
- **Rebuilds done + verified:** (a) band panel → VIOLIN (smooth KDE + crisp outline, gate-tinted);
  results_sec_1 now includes `fig_trace_a_band_violin.pdf`. (c-companion) `fig_trace_c_diffmaps`
  → all 6 bands + title removed. (d) tissue → GENERAL band×tissue dotplot (6 bands × 5 classes,
  filled=clears MS, α SOZ↔SOZ +0.41 flagged; `\(\beta\)` LaTeX bug fixed).
- **REMAINING (big 3D-brain builds):** (f) SOZ divergence → glass-brain (β spares / α recruits SOZ);
  (g) hero chord → **3D-brain trace on all implants** (nodes carrier=green/anti=red/neutral=grey,
  green tracer links, β vs low_γ = cohort-wide vs localized). Data ready: per_node.csv (β+low_γ
  polarity+coords, coords native µm → route via fig_b MNI pipeline); cophenetic halves cached all
  10 patients both bands (green links computable). low_γ carriers concentrate (Pat_05 80/Pat_15 2)
  = the localized-band story. Build next as a focused pass; keep chord script as an alt.

## Session — Results §2 assembled: two inline compound figures, verified & page-fit

**Head.** `results_sec_2.tex` (overleaf mirror) is now a provisional-final,
copy-paste flagship section: all 8 ρ_sym stats independently re-verified against
the CSVs (audit agent matched every one), the dropped **R2.7** cingulate-
multiplexing paragraph restored, the duration range corrected to the honest
**1.35–2.53×** (was 1.4–2.5×, understated the low end), seven `\FigRef` callouts
wired, and the two composite figures embedded inline like §1 — compile-tested
(pdflatex two-column: both `figure*[p]` floats fit a page, all refs resolve).

- **Figures.** Reused the existing `fig_arc_a…g` + `fig_arc_attractor_3d` set
  (1:1 with R2.1–R2.7). Stripped each panel's baked a/b letters (5 scripts
  regenerated in `lapbrain`, all 0-raster) so the LaTeX minipage mosaic supplies
  clean single letters. **Fig `enc_dynamics`** = attractor | decomposition forest
  | multiscale | duration. **Fig `enc_anatomy`** = OFC anchor | low-γ cingulate
  memory | inference→cingulate | multiplexing 2×2. Minipage widths
  aspect-proportional, tuned to fit a float page (nudge-able; §1-style comment).
- **Honest flags surfaced to PI.** (1) Attractor rendered small (0.43\textwidth)
  to keep the 2-figure budget — offered promoting it to its own figure for impact.
  (2) `Z_eff` (72/35/6) traces only to a directive comment; `resolution_diag.py`
  is not committed — hedged with ≈, needs a committed compute for reproducibility.
  (3) "\gls{loo}" in R2.2 is really leave-Pat15-out, not full LOO (value robust;
  kept for §1 idiom consistency, flagged).
- Pre-letter-strip PDFs backed up in scratchpad; README already lists sec_2
  (user edited it concurrently). Kept the R2.2 coarse-graining sentence as
  shipped (assembled file was the later mtime) rather than reverting to directive.

## Session — pairwise-blind program: raw-FC reverify + two negative-control workstreams

**Head:** Opened a program to prove the multiscale trace is *not* reproducible by
simple pairwise / classical-network models. Reverified the raw-FC baseline (the
"all is trace / nothing escapes the null" memory is **half-backwards**), then
scoped two negative-control workstreams. First deliverable landed: a draft scope
report for a **Grassmann inference arc** — a second, strength-independent witness
for the inference-specific β trace, which is currently single-witnessed.

- **Raw-FC reverify (audit_67/146 + errata table):** "all is trace" = TRUE (raw
  `ρ_split^raw` positive in all 6 bands, +0.11..+0.26, floors θ at +0.117, no
  absent-θ contrast). "Nothing escapes the null" = **backwards for raw FC**: raw
  FC *survives* matched-strength broadly (20–88× surrogate median, 6–8/10 above in
  every band). The MS null only discriminates *after* the cophenetic transform
  (kills δ/θ/γ_l/γ_h, leaves α p=0.002 / β p=0.005; only β localizes → OFC). So
  the pairwise-model failure mode is **non-discrimination**, not blindness — the
  ladder's axis is *discriminability*, not "survives the null."
- **Workstream A — classical-descriptor foil (scoped, queued):** turn the standard
  network toolbox (node strength, weighted clustering, betweenness, efficiency,
  modularity, topological overlap, participation, assortativity) into the *same*
  `ρ_sym` rank correlator under the *same* matched-strength null, scored on the
  four discriminating criteria (recovers α / silent on absent-θ / survives MS /
  localizes→OFC). Disaster scenario = any classical descriptor passes all four ⇒
  LRG redundant; committed to report it if it happens. **Key mined fact:** on dense
  FC, weighted clustering tracks node strength at r≈0.96 (participation r≈0.51,
  `fig_F` deleted them for that) — so add a **strength-slaving diagnostic** per
  descriptor; MS likely kills the strength-slaved ones *by mechanism*. Full menu
  (user: "no reason to be cheap"). Reuse: `matched_strength` lib + audit_143
  harness + promote `rho_sym_split` (audit_150) to library on 2nd use. Little
  classical-metric code salvageable (Onnela clustering/participation/betweenness
  from archived `_shared.py`/`fig_F`; efficiency/TO/char-path/Barrat-Zhang/modularity
  = write). No bctpy in repo.
- **Workstream B — Grassmann inference arc (SCOPE DRAFTED, user picked first):**
  `.agents/guides/task-persistence-investigation/2026-07-09_grassmann-inference-arc.md`.
  Gap confirmed: Grassmann only ever computed on whole `task_test` (audit_66/70
  exclude `task_learn`); the encoding-vs-inference split rests on cophenetic
  `ρ_sym` alone (β `T_infspec·e` p=0.0098, audit_152). New statistic: with
  `Δ(r;k)=d_G(r,TL;k)−d_G(r,TT;k)`, the inference-specific relocation is
  `T_G^infspec(k)=Δ(RP;k)−½[Δ(preA;k)+Δ(preB;k)]` = difference-of-triangles
  `T_G^onl−T_G^enc` (encoding controlled *by construction*; verified algebraically).
  Aggregated with the existing `T_G*` cluster-mass gate over k∈{2..112}. **Near-zero
  new compute**: shared surrogate eig-cache already holds `task_learn`, `rest_pre_B`,
  and length-matched `task_test_lm_head` (seed 20260511, all bands R=200). Two locked
  caveats: length-matching is *mandatory* (Δ differences dist-to-TL vs dist-to-TT;
  TT is 1.4–2.5× longer) via `_tt_fc_lenmatched`; scalar-per-patient ⇒ lower power ⇒
  non-corroboration is ambiguous, corroboration is strong. **OPEN (user sign-off):**
  the inference-contrast choice — closeness-difference (default) vs principal-angle
  projection. Cross-check gate: `T_G^onl` must equal audit_66 `T_G` to 1e-10.
- **Roadmap:** tasks #2–#4 = classical foil (scope→lib+audit→run/figure); task #5 =
  Grassmann arm (scope done, build pending contrast sign-off). Session name
  "pairwise-blind-ensure".

## Session — τ-sweep purged from the paper (τ_min-only, PI directive)

**Head:** PI ruled the multiscale-across-τ claim dead — a diffusion-time sweep is
degenerate on a fully-connected, continuous-spectrum |ImCoh| network (coarser τ
relaxes to the uniform mode; no hierarchy to be "robust across"). This only
re-enforces the 2026-05-18 retirement that had leaked back into shipped text; all
LRG cophenetic results now stand on τ = τ_min = 1/λ_max alone. KEEP/DROP split: the
*dendrogram at τ_min* IS multiscale (nested merge-heights) — that sense stays
(framework name, R1.1/R1.3, ρ^coph "multiscale"); only the τ-SWEEP sense dies.

- **§2 (`results_sec_2.tex`):** removed R2.2 ("holds across the hierarchy's scales")
  + the τ panel. Fig 3 (`enc_dynamics`) = attractor hero + decomposition forest (2
  panels; fixes the 0.43-width attractor squeeze). Also dropped the two other weak
  panels the PI culled — duration (kept as R2.4 prose) and cingulate-multiplexing 2×2
  (kept as R2.7 prose) — so Fig 4 (`enc_anatomy`) = OFC | low-γ cingulate |
  inference→cingulate. Resolves 2 old flags (Z_eff 72/35/6 uncommitted; mislabeled
  `\gls{loo}`) — both lived only in R2.2/the panel. Compile-tested: 4 pages, no float
  overflow, refs resolve.
- **Methods (`methods.tex`):** the "Scale dependence" paragraph re-introduced a
  mesoscale c≈2.6 sweep + Z_eff and called it "scale-robustness" — contradicted L80
  ("coarser τ are not used here"). Rewrote to read T_enc/T_inf at τ_min alone + *why*
  a sweep is degenerate. Now consistent with L80 + L153.
- **§1 (`results_sec_1.tex`):** reinstatement paragraph claimed the shift "stays
  positive across ten diffusion scales" — cut it; kept the five-read-out robustness;
  "across every scale" → "across every band." ⚠ Dropped the bundled (8–10/10, p≤0.007)
  stat (it was the τ-sweep's) — restore only if it holds across the five read-outs (PI
  to confirm). Core result untouched (10/10, p=0.001).
- **SOZ marker kept (PI call):** epilepsy 6-τ grid reads raw kernel entries
  [e^{-τL}]_ij as classifier features (AUC/LOSO), not a cophenetic-hierarchy claim —
  out of scope; `methods.tex:294` already firewalls it.
- **Mirrors + figure:** R1.5 directive synced (prose + provenance header, audit_133
  across-τ marked superseded). `fig_trace_e` annotation stripped, PDF refreshed
  (numbers unchanged). 3 dead panel scripts (fig_arc_b/d/g) → archive; PDFs removed;
  R2.2 mirror directive archived. Shipped .tex greps clean of
  mesoscale/ten-diffusion/scale-robust/Z_{.
- **Pre-existing flag (out of scope):** `fig_trace_e`'s colorbar rasterizes
  (`fig.colorbar(ScalarMappable)` → 50×806 strip); one-line fix
  (`cb.solids.set_rasterized(False); set_edgecolor("face")`), likely systemic across
  any fig.colorbar figure — offered a sweep, awaiting PI.
- **Remaining hygiene:** two `headlines/` notes (02_encoding N2.2, 01_trace L81/309)
  still describe the sweep — sync pending. Run env: direct lapbrain binary.

### Update — Grassmann inference arc RAN (audit_165): NEGATIVE, clean, on-thesis
**Head:** Built + ran the Grassmann inference arm. Result: the subspace probe does
**not** second-witness the inference-specific β trace — `T_G^infspec` is null in
all 6 bands. The inference trace stays cophenetic-only (fine-scale); the negative
is informative and *reinforces* multiscale necessity. Scope report → status=result.
- **Verdict:** β `T_G^infspec` cluster_p_mass=**0.408** (no_trace); null in every
  band (δ.756/θ.612/α.821/β.408/γl.378/γh1.000); β per-patient 5/10 pos median,
  medians scatter [−0.20,+0.31] with no direction. **Not a power miss** — `T_G^onl`
  (whole-task) reproduces audit_70 to the decimal (δ38.07/β69.76/γl66.14 strong;
  cross-check vs audit_66 max dev 1.7e-13) and `T_G^enc` clears δ,β. Reading:
  rest_post's leading-subspace relocation is toward the task generally (encoding),
  not inference; the inference refinement lives only in the fine cophenetic
  pair-identity structure. Inference-specific β trace **stays single-witnessed**
  (cophenetic ρ_sym p=0.0098). Memory [[grassmann_inference_arc_negative_2026_07_09]].
- **Method correction (locked):** the scope's "length-matching mandatory" caveat is
  **INVERTED by the data**. `T_G^infspec_lm` fires "strong" (p=0.005) in ALL 6 bands
  incl θ (masses 206–273), even where the observed median is negative — head-truncating
  task_test shifts the matched-strength surrogate baseline negative → a pan-band
  obs>surr artifact. Truncation is NOT a valid control for a subspace distance. Raw
  `T_G^infspec` is the clean arm, length-robust by construction (TT-length bias
  cancels in Δ(RP)−Δ̄(pre)). Also: Grassmann `enc` fails to reproduce the cophenetic
  α-encoding channel (α enc p=0.552) — the subspace probe doesn't resolve the
  encoding/inference dissociation at all.
- **Deliverables:** `scripts/01_compute/audit/audit_165_grassmann_inference_arc.py`;
  `data/audit/grassmann_inference_arc/{cohort_summary,per_patient}.csv + README`.
  Helpers `laplacian_eig`/`topk_basis`/`chordal_distances_for_k_grid` promoted to
  `src/lrg_eegfc/utils/metrics/spectral.py` (2nd caller; audit_66/70 left as-is).
  Observed length-matched TT eigvecs cached `data/cache/lenmatched_tt_obs_eig/`.
  Run ~23 min (10-min lm-eig preflight one-time + 12.6-min compute); surrogate
  ensembles all cache hits seed 20260511 R=200. No figures (deferred). Note: the
  build sub-agent prematurely reported "done" while the run was still going and
  streamed per-band progress via task-notifications rather than a clean final return.
- **OPEN for user:** whether to keep `infspec_lm` in the record as a documented
  artifact (recommend yes — it's the evidence that truncation is invalid for this
  probe). Task #5 CLOSED. Classical-descriptor foil (tasks #2–#4) still queued.
- **FOLDED INTO N2 (user: "fundamental"):** added to `.agents/preprint/headlines/02_encoding_vs_inference.md`
  — a spectral-subspace-control banner, a §A plain-language paragraph, a new **N2.7**
  subheadline, a §C strengths line, a §F provenance entry, and a resolved §G item. Framing
  (honest, N1-consistent): the spectral/Grassmann probe *recovers* the whole-task + encoding
  β reorganization (so it stays a valid N1 second witness) but is blind to the
  inference-SPECIFIC refinement → the inference trace is specifically hierarchical/multiscale,
  not an eigenmode-geometric shift. The N1-both-probes / N2-inference-cophenetic-only
  **dissociation IS the result**, complementing the τ-sweep multiscale argument (N2.2).
  NEXT (unstarted): propagate into manuscript prose `directives/results_paragraphs/R2.*.tex`
  when writing.

## Session (cont.) — LRG diffusion-zoom animation + higher-order refs verified

**Head:** built the I-3→I-4→M-3 visual backbone — an animated τ-sweep of the
heat-kernel propagator on two graphs — and Crossref-verified the new
higher-order references. Both deliverables from the intro re-weighting are now
closed except the N2 figure regeneration.

- **Animation DONE:** `scripts/01_compute/figures_embedded/fig_lrg_diffusion_zoom_demo.py`
  → `data/outputs/figures/lrg_diffusion_zoom/{lrg_diffusion_zoom.gif (7.8 MB,
  60 frames, Canva-ready), lrg_diffusion_zoom_snapshots.pdf (static 3-τ, vector)}`.
  Propagator K(τ)=e^{−τL̂} (combinatorial L̂) re-projected on the links, swept
  over τ, side-by-side with the K(τ) matrix, for an **HMN** (16 fine → 4 mid →
  coarse blocks light up in turn) and a **complete graph with heterogeneous
  weights** (same nested hierarchy emerges with NO sparse blocks — the brain-FC
  / Villegas-outlier regime). One eigendecomp per graph; each frame is
  V·e^{−τΛ}·Vᵀ. Dark-glow theme (magma), fine·local → coarse·global progress bar.
  No ffmpeg in lapbrain → GIF via PillowWriter + shared-96-colour optimize
  (14.4→7.8 MB); MP4 later via `pip install imageio-ffmpeg` if wanted.
- **Refs verified (Crossref):** Battiston 2020 *Phys.Rep.* 874:1–92
  (10.1016/j.physrep.2020.05.004); Battiston 2021 *Nat.Phys.* 17:1093–1098
  (10.1038/s41567-021-01371-4); Santoro 2024 *Nat.Commun.* 15:10244
  (10.1038/s41467-024-54472-y, brain-specific headline) + Santoro 2023
  *Nat.Phys.* 19:221–229 (10.1038/s41567-022-01852-0, method). Talk file
  ⚠-flags removed; I-3 attach + reference block updated.
- **Still open:** the N2 encoding-vs-inference ρ_sym figure regeneration is the
  only remaining "must-produce" for the talk.

## Session (cont.) — diffusion-zoom fix: sparse HMN vs complete graph

**Head:** the two networks both looked complete because the propagator was
drawn on ALL node pairs in both panels. Fixed so each network draws its OWN
edge set — the HMN now reads as genuinely sparse (9% density, 185/2016 edges),
the heterogeneous graph stays complete (100%). The matrices still show full
K(τ). The sparse-vs-complete contrast the talk needs is now visible. GIF
shrank to 5.4 MB (fewer edges). Same asset paths; re-run of the script.

## Session (cont.) — §1 figures: band palette + c/d polish + g rebuilt as trace/no-trace 3D

**Head:** built a canonical swappable band-colour palette in the library,
refactored §1 figures onto it, polished the cophenetic-fingerprint maps,
swapped the ugly raw-vs-multiscale panel out of the paper, and rebuilt the
hero as a single-β "trace / no-trace" 3D brain (per user feedback). f (SOZ
α/β network fusion) still to do.

- **Band palette (locked 2026-07-09):** `lrg_eegfc.visuals.styles` now exports
  `BAND_COLORS` + `band_color(band, shade=)` (re-exported from
  `lrg_eegfc.visuals`). Spectrum slow=red → fast=blue, rainbow between
  (`turbo` reversed; ONE swap point = `BAND_CMAP_NAME`/`BAND_CMAP_SPAN`).
  `shade<1` darkens bright midtones (α/β) for text. Rule → never-always-list +
  `feedback_band_color_palette.md`. Refactored `fig_trace_d_tissue_class` onto
  it (removed the hardcoded BAND_COL).
- **fig_trace_c_diffmaps polished:** killed the box-aspect vertical slack
  (whitespace above/left gone), anchored the panel tag to content, stripped the
  verbose colorbar label → bare −/0/+ symbols (meaning moved to the caption).
- **Tex (`overleaf/results_sec_1.tex`):** removed `fig_trace_c_raw_vs_multiscale`
  (item 4); panel c is now `fig_trace_c_diffmaps.pdf`; panel a is now
  `fig_trace_g_pairglow_3d.pdf`. Fig 1 relaid: row1 a|b (g | violin, both
  squarish), row2 c diffmaps banner, row3 d reinstatement banner. Captions
  (a)+(c) rewritten; LAYOUT flagged as possibly tall for one page.
- **g rebuilt** `fig_trace_g_pairglow_3d.py` (NEW; old `fig_trace_g_tracer_chord`
  kept as alt) from the pairglow talk asset. First tried β vs low_γ
  (consistent vs inconsistent) — user: "cannot work as a distinguisher … balls
  hide links". Redone as a SINGLE β brain, TRACE vs NO-TRACE (amber carrier /
  grey sea + green tracer co-movements), balls shrunk so the green links
  dominate. 3D plotly → high-res raster PDF (user OK'd raster; 3D plotly can't
  be vector). Panels b/c/d stay full vector.
- **Library:** promoted the 3D primitives to `src/lrg_eegfc/visuals/brain3d.py`
  (`pial_mesh`, `spheres_mesh`, `bezier_arcs`, `unit_sphere`) — general names.
- **FOLLOW-UP (deferred):** the talk asset `fig_beta_ofc_pairglow_3d.py` still
  has its own copies of those primitives — migrate it to `brain3d` imports (it's
  a draft; migrating without a full re-render risks a silent break).
- **f DONE (user picked design A):** `fig_trace_f_soz_divergence.py` rebuilt as a
  circular dendrogram-chord (K7 style) for exemplar Pat_05, α | β, highlighting ONLY
  the SOZ-internal strong tracer co-movements (score > 0.4) in green over the diffusion
  dendrogram; SOZ contacts crimson on the rim. α = dense recruited bundle (33 links),
  β = near-empty (3) → "α recruits, β steers around". Pat_05 chosen for SOZ coverage +
  clearest per-patient α≫β gap (symmetric ρ_sym score; Pat_13 bigger raw gap but 30-SOZ
  hairball). Illustrative/exemplar; cohort stat (α epi_epi +0.41 p=0.005) is the tissue
  panel. Fig 2 caption (c) updated to the network view.
- **§1 figure session COMPLETE:** all 5 minor-detail items + g + f done; g raster PDF
  kept (user OK). Only follow-up = migrate the talk asset to `brain3d` (deferred).

## Session (cont.) — diffusion-zoom panel B → REAL brain FC

**Head:** swapped the synthetic complete-graph in the diffusion-zoom demo for
an ACTUAL band network — Pat_05 β rest_pre imcoh_abs (118 nodes, ~100% dense).
Panel A stays the sparse synthetic HMN (known ground-truth → validates
recovery); panel B is now real brain FC (the application). GIF ~6.4 MB.

- `load_fc_matrix(Pat_05, rest_pre, beta, imcoh_abs)` → complete weighted
  connectome. Reordered by its OWN LRG dendrogram (UPGMA on communication
  distance D=1/K at τ=1/λmax) so blocks read on the diagonal; nodes coloured by
  k=6 fcluster communities.
- Layout iteration: spring on a complete graph = hairball → tried ring-of-
  community-blobs (lopsided when one β module dominates) → settled on a
  **circular chord layout in dendrogram order** (contiguous coloured arcs,
  chords glow by propagator; matches repo circular_dendrogram_network template).
- `REAL` dict at top of the script makes patient/band/phase one-line
  configurable. Refactored to fully per-graph (own N, layout, order,
  communities, edge set); `build_fc_heterogeneous` kept only as a load-failure
  fallback. Talk M-3 + figure-gap updated to say real β FC.

## Session (cont.) — second figure: the dendrogram-CUT multiscale

**Head:** built the figure that expresses the multiscale we actually USE — not a
τ-sweep but a dendrogram-cut sweep. At τ=1/λ_max the propagator encodes the whole
tree (D=1/K → UPGMA cophenetic); sliding the cut height h merges nested
communities. New script `fig_lrg_dendrogram_cut_demo.py` →
`lrg_dendrogram_cut.gif` (~2.5 MB) + snapshots PDF.

- **Layout:** left = network (graph-tool sfdp, curved Bézier edges, nodes
  coloured by community at the cut), right = cophenetic dendrogram (log-y) with a
  growing horizontal cut, branches coloured to match nodes (shared leaf-order
  tab20 palette). Two rows: synthetic HMN + real β FC (Pat_05 rest_pre).
- **Iterations:** sfdp on the complete β graph = hairball → kNN backbone (top-6
  per node) for the layout; HMN dendrogram spans ~9 decades → log-y (project
  convention); height-linear sweep stalled at ~107 comm (top-heavy dense tree) →
  drive by TARGET community count 30→1, cut line at the bracketing merge height.
- **τ note:** used τ=1/λ_max (locked canonical; tau_sensitivity vindicated it).
  User phrased it "1/λ_min / τ_max"; flagged for alignment.
- **Honesty:** real β connectome is weakly hierarchical (one dominant core +
  peripheral comms), not deeply nested — present as "same readout on real data".
- Reuses diffusion-zoom primitives (build_hmn/laplacian_eig/propagator/REAL) via
  sibling import — no duplication. Talk M-3 + figure-gaps updated. graph-tool 2.63.

## Session (cont.) — §1 figures g + f REBUILT (both prior versions rejected)

User: g (single-β pooled brain) + f (Pat_05 SOZ chord) both "horrible". Diagnosed
+ rebuilt honestly. Chose via AskUserQuestion: g="θ|α|β spectrum", f="cophenetic
co-movement clustermap" — but clustermap FAILED on contact with data (below).

- **Key data fact (kills naive g):** raw per-pair concordance does NOT separate
  bands (θ 53% ≈ β 61% concordant pairs; `sigma`=sign of prod, not a MS test). Band
  selectivity lives ONLY in per-node ρ_sym-vs-matched-strength (carrier polarity).
  And θ/α/β **carriers are individually identical in strength** (carr ρ_sym
  0.365/0.358/0.375) — the ONLY honest discriminator is carrier **fraction**
  (16/22/33%) + carrier/anti balance (θ 189c/155a→net null; β 382c/100a).
- **audit_154 extended to all 6 bands** (per_node.csv was α/β/lowγ only; MS surrogate
  + half-FC caches exist for ALL 6 → cheap aggregation, no regen). Superset,
  bit-identical for existing bands. θ came out flat: mean π̃ +0.05 (α +0.36, β +0.51),
  carriers balanced by anti. Backup of old CSVs in scratchpad.
- **g rebuilt** = `fig_trace_g_pairglow_3d.py` → 3 pooled MNI brains θ|α|β
  (make_subplots, 3 scenes, shared cam). Sea GHOSTED (opacity .07), amber carriers
  salient, green backbone = **fraction** (FRAC_CC=.045) of carrier↔carrier concordant
  pairs (~carrier² → amplifies 2× fraction gap to ~1:2:3 arc gradient: θ68/α133/β198).
  Reads as gate-crossing. 6.2 MB raster PDF (plotly, kept).
- **f clustermap FAILED (honest pivot, drafted+verified):** per-pair ρ_sym matrix is
  node-polarity row/col STREAKS (≈outer product), NO SOZ block; +0.41 is a
  WITHIN-class rank stat, global-rank-then-subset gives ≈+0.08. Memory
  [[perpair_cophenetic_trace_visualization]] updated with the lesson.
- **f rebuilt** = `fig_trace_f_soz_divergence.py` → **co-movement scatter + cohort
  forest**. Top α|β: x=rankz(D_task−D_pre), y=rankz(D_post−D_pre), SOZ crimson on grey
  sea, green y=x="held". Exemplar **Pat_14** (α SOZ +0.43 on diagonal / β −0.39
  scatter; ≈cohort +0.41). Bottom forest (n=10): ρ_sym per pair-class vs
  matched-strength null — α SOZ +0.41 past NEGATIVE null (p .005 ★), β SOZ +0.08 on
  null (p .31), double dissociation (β traces in cortex not SOZ). **NO threshold
  anywhere** (user's "how do you pick strong tracers" objection killed). 113 KB vector.
- Pat_05 was the wrong exemplar: α≈β≈0.42 (no divergence). Pat_14 has α .43/β −.39
  AND matches cohort α. Switched.
- **tex updated:** Fig1 caption (a)→spectrum; Fig2 caption (c)→geometry+forest,
  Pat_14. Prose para (β/α SOZ diverge) already consistent (p<.001 pairclass, p.005
  MS), no change.

---

## Session — combined multiscale figure (third talk asset)

**Head:** Built `lrg_multiscale_combined.gif` (+ 5-stage vector PDF) — the third
M-3 figure the user asked for: **row 1** = sparse-HMN diffusion-τ sweep (from the
diffusion-zoom demo), **row 2** = real β FC dendrogram-cut sweep (from the
dendrogram-cut demo) fused into one 2×3 slide, plus the two new asks.

- **New ask 1 — blocks on the matrix.** Row-2 connectivity matrix (leaf-ordered
  |imcoh_abs|) gets the cut communities projected on as coloured diagonal blocks
  (`_blocks()` → contiguous runs in leaf order → `Rectangle` pool of k_start=30).
  Blocks appear/merge as the cut fills; palette shared with nodes + dendrogram.
- **New ask 2 — new LRG layout ("non-trivial bubble").** Tried 4 candidates
  (scratch `layout_probe.py`): diffusion φ2,φ3 whiten + whitened-multimode-PCA
  both **collapse to a 1-D spike** (Fiedler localizes on a few low-strength
  nodes); cophenetic-MDS is a stringy ultrametric artifact; **√-compressed
  communication-distance MDS (PCoA on 1/K(τ_max)) WINS** — organic bubble,
  dominant core + peripheral lobes. Adopted as `lrg_communication_layout()`.
- Both rows sweep fine→coarse on one shared vertical scale bar; QA via frame
  dumps (`--check`) before the 14 s full render. GIF ~8 MB, 60 f @ 16 fps.
- Script `fig_lrg_multiscale_combined_demo.py` reuses every sibling primitive
  (build_hmn, laplacian_eig, propagator, tau_*, cophenetic_linkage,
  dendrogram_segments, curved_paths, cut_height, _labels_at, pal, _optimize_gif);
  only the assembly + the two new pieces are original.
- **Honesty note unchanged:** real β connectome is weakly hierarchical (one
  dominant core) — present as "same readout on real data", not deep nesting.
- Talk file `2026-07-07_talk-structure-20min.md` figure-gaps updated with the
  combined-demo bullet. Assets in `data/outputs/figures/lrg_diffusion_zoom/`.

## Session (cont.) — §1 figures g + f: final polish (user edits) → §1 CLOSED

- **g:** carriers recoloured **green** + soft green glow halo, anti/reset nodes **red**
  (theta 187green/155red ~ balanced -> null; beta 381/100 -> green-dominated; the
  red/green balance now IS the net-trace visual). Nodes resized (too-big -> too-small
  -> right). Camera raised+out (eye 1.08/-1.02/0.60), horiz-spacing 0.028, width 2040
  -> panels no longer clip. **Orientation stubs FRONT/BACK/TOP/BOTTOM** (dropped
  LEFT/RIGHT = depth axis, foreshortened+clips) so a static reader can orient.
  Glow+reset per the fig_beta_ofc_pairglow draft; orientation per fig_trace_heat_brain.
- **f:** added **2-sigma covariance ellipse** ("variance surface") per panel -- alpha =
  diagonal cigar hugging the held diagonal (aspect 2.17), beta = near-circular broad
  spread (1.04, beta rho_sym -0.39). Row gap reduced (hspace 0.62->0.30, legend to top).
  Caption (c) updated with the ellipse.
- **Section 1 figure set COMPLETE** (a-g rebuilt/honest); ready to close Section 1.

## Session (cont.2) — g + f second polish pass

- **g:** brain shell darker (C_PIAL #868d97 @0.11, was near-white 0.05); **arcs BLUE**
  (#2e6be6) not green -- green arcs confounded with the now-green carrier nodes; node
  cores shrunk (carrier 0.82+0.85f, was 1.25+1.35f) so links show through the glow;
  arcs increased FRAC_CC 0.045->0.07 (theta 89 / alpha 188 / beta 266) so beta link
  PREVALENCE reads. Arc selection (answering user): per patient, top 7% of significant
  concordant carrier-carrier pairs by |dD_task*dD_rest|, count ~ carriers^2.
- **f:** ellipse 2sigma->1sigma (contained, no longer spills outside; alpha thin diagonal
  cigar aspect 2.17 vs beta near-round 1.04 now clearer); row gap hspace 0.30->0.13,
  fig 8.9->8.1 tall. Caption + legend updated 2sigma->1sigma.

- **g (fix):** removed carrier glow halo -> translucent halos rendered as hollow rings (brighter at silhouette), making dense/low-rho carriers look like a distinct "empty" node class. All carriers now ONE solid green style (luminous via _NODE_LIGHT, size ~ |rho_sym|). No more empty nodes.

---

## Session — combined multiscale figure, rev-2 (4 user fixes + MP4)

**Head:** Reworked `lrg_multiscale_combined` on 4 user notes; key win = **MP4
now the primary asset** (2 MB, sharp true-colour) — GIF palette-quantization was
the "pixelated text = trash" culprit.

- **(1) All links + sharpness.** Row-2 network was silently dropping 90% of edges
  (top-10% quantile filter) — REMOVED; now draws **all** complete-graph links
  (per-edge alpha ∝ weight; within-community glow vs faint grey wash). Text
  sharpness fixed by rendering **MP4** (H.264, true-colour, no palette): installed
  `imageio-ffmpeg` (static ffmpeg, no system dep) → `FFMpegWriter`, crf 18,
  yuv420p, dpi 150. MP4 2 MB & crisp vs old GIF 8 MB & pixelated. GIF kept as
  fallback but with per-frame adaptive 256-colour (`_sharpen_gif`), 10 MB.
- **(2) Start from smallest merge.** Cut sweep k_start was 30 (skipped the bottom
  ~88 merges); now k_start = n (118) → begins at all-singletons / smallest merge,
  driven by merge **rank** (even progression; dense β tree is height-top-heavy).
- **(3) Too fast.** 60 f @ 16 fps (3.75 s) → 96 f @ 12 fps + 16 held frames ≈
  9.3 s + hold.
- **(4) Two bars.** Replaced the single fine→coarse bar with **two independent
  vertical bars**: τ (diffusion, amber) and h (dendrogram cut by rank, pink),
  each fine↕coarse, live community-count readout.
- Verified by extracting a real MP4 frame via ffmpeg (sharp) + dpi-150 check
  PNGs (frame 1 = 116 communities confirms smallest-merge start). ~35 s total
  render. Talk file rev-2 bullet + this diary updated. Layout memory
  [[figure_dense_connectome_bubble_layout]] still valid (MDS bubble unchanged).

---

## Session — combined multiscale figure, rev-3 (dendrogram-per-τ correction)

**Head:** User caught a real pedagogical error: the figure implied "sparse → no
dendrogram, dense → dendrogram." Truth is the opposite structure — the *sparse
topological* graph has a **τ-dependent family of trees** (one per diffusion
time), while the *fully-connected weighted* graph has **one precise hierarchy at
τ_max**. Fixed by giving row 1 its own dendrogram that MORPHS with τ.

- Grid 2×3 → **2×4** (network │ matrix │ dendrogram │ bar), both rows symmetric.
- **Row 1 dendrogram recomputed every frame**: D(τ)=1/K(τ) → UPGMA, drawn with a
  FIXED leaf x-order (reference τ) via new `_tree_geometry_fixed_order()` so it
  morphs smoothly; fixed global log-y range. Scratch `morph_probe.py` confirmed
  the morph is clean + dramatic: deep differentiated tree at fine τ → flat comb
  at coarse τ (heights span ~10 decades as far-pair K→0 at small τ). Branches
  coloured by the 4 ground-truth mid-blocks (matches row-1 network nodes).
- **Row 2 unchanged**: single cophenetic tree at τ_max, moving cut.
- Captions "a new tree for every τ" (amber) / "one tree at τ_max · cut ↑" (pink)
  top-left of each dendrogram; explicitly encode the τ≠cut distinction
  ([[feedback_lrg_optimal_threshold_is_not_tau]] — τ is not a cut height).
- Two bars kept (τ row1, h row2), now one per row in the thin 4th column.
- make_static → 6 rows (added HMN τ-dendrogram row). Verified via MP4 frame
  extraction (fine=tall tree, coarse=flat comb). MP4 2.1 MB, GIF 10.7 MB, ~35 s.
- Talk file bullet → rev-3. Memory [[figure_dense_connectome_bubble_layout]]
  augmented with the τ-tree-per-τ vs one-tree-at-τmax framing.

---

## Session — combined figure: y-rescale + transparent/light variant

**Head:** Two more user asks on the combined multiscale figure: (1) rescale the
row-1 (τ) dendrogram so it always fills the y-axis; (2) a transparent-background,
black-text version for a light Canva slide.

- **(1) Rescale.** Row-1 τ-dendrogram now sets `ax_hd.set_ylim` per frame to the
  current tree's [min·0.4, max·1.15] (leaf feet just below the first merge → root
  near top) so the morphing tree ALWAYS fills the panel; y-tick labels carry the
  absolute scale (10⁹ fine τ → 10² coarse τ). Same per-column in the static PDF.
- **(2) Transparent/light theme.** Added `_set_theme(light)` + `THEME` dict
  routing all fg/muted/label/spine/node-edge/edge-cmap colours; `LIGHT` rc.
  Light mode = transparent fig+axes bg, **black text**, dark spines, and network
  edges use **cividis_r** (dark-on-light — magma's bright "strong flow" is
  invisible on white). Networks & dendrograms render as pure ink (see-through
  panels); the two heat-matrices stay opaque magma rasters.
- **MP4 can't be transparent** (H.264 opaque) → transparent *animated* asset is a
  **GIF** with 1-bit alpha (`render_transparent_gif` → savefig(transparent) per
  frame → `_transparent_gif` reserves palette idx 255, disposal=2). Verified by
  compositing a frame on light (#f5f4f0 → crisp) and teal (dark → low-contrast,
  confirms light-slide use). 77.8% transparent pixels.
- **Deliverable set** (data/outputs/figures/lrg_diffusion_zoom/):
  `…combined.mp4` (dark, sharp, dark slides) · `…combined_transparent.gif`
  (light, transparent, animated) · `…combined_transparent.pdf` +
  `…combined_snapshots.pdf` (vector stills). Dropped the redundant dark GIF.
- Talk file bullet → formats matrix. Memory
  [[figure_dense_connectome_bubble_layout]] += transparent/MP4-format note.

## Session — figure-maker: "why the variance" laterality brain (β left-lateralised)

**Head:** Built a new dense cohort figure that answers *why some patients
carry the β→OFC trace and others reset* — all 10 patients' contacts pooled
into one MNI brain, each tinted by its **patient's** β trace net. Left/bilateral
clouds are warm carriers; the two cool resetter clouds (Pat_10, Pat_15) are the
patients implanted **purely on the right**. β is left-lateralised at the contact
level, **α is bilaterally symmetric** (the control that makes it credible).

- New script `scripts/01_compute/figures_embedded/fig_trace_laterality_brain.py`
  → `data/preprint/figures/_drafts/fig_trace_laterality_brain_{beta,alpha,low_gamma}_DRAFT.html`
  (HTML only, rotatable). Companion `fig_trace_laterality_brain_README.md` with the
  honest numbers + bounds.
- Honest split (β): per-contact **L +0.34 vs R +0.05** (~7×); BUT pooled-right is
  low mostly because the two right-only reset patients drag it — right *contacts*
  in bilateral patients carry fine (**+0.36** excl. Pat_10/15). Plus a milder
  **within-patient** left-lean (Pat_05 +0.45, Pat_06 +0.21). Control: **α symmetric**
  (L +0.145 ≈ R +0.144). Do NOT read as "right hemisphere inert" — it's coverage/
  laterality at the *patient* level.
- Not a new test: visualises the audit_148 stable-trait (ρ=.95) result. β→OFC stays
  the system-scale MS-gated result (bilateral OFC) — left-lateralisation is a
  compatible contact-level trait, not a claim about OFC laterality.
- **Library:** promoted `orientation_traces(lo,hi)` (FRONT/BACK/LEFT/RIGHT/TOP/BOTTOM
  axis labels) from the heat script into `visuals.brain3d` (2nd caller); heat script
  now imports it.

## Session — §2 figures re-compounded in matplotlib (single PDF per float, PI directive)

**Head:** §2's two floats are now ONE matplotlib compound PDF each — the mosaic is built
in matplotlib, not a LaTeX minipage grid. Fig 3 `fig_arc_enc_dynamics.pdf` = **a** attractor
hero over **b** decomposition forest; Fig 4 `fig_arc_enc_anatomy.pdf` = 3 region rows
(**a** OFC / **b** low-γ cingulate / **c** inference→cingulate) + one shared legend. Both
fully vector, §2 compiles clean (4 pp, no float-overflow).

- New compound scripts, **import-and-compose** (touch none of the 5 single scripts):
  `figures_embedded/fig_arc_enc_dynamics.py` (SubFigures — 3D + 2D, no nilearn) and
  `fig_arc_enc_anatomy.py` (**FLAT** figure — nilearn forces it, see gotcha). Each imports
  the single-panel scripts' draw helpers (`portrait`/`cohort_strip`, `draw_panel`,
  `draw_lollipop`/`draw_brain`/`load_*`) as namespaces and lays them onto one figure;
  legends rebuilt inline from the modules' own colour constants (no drift).
- **Gotcha (locked):** `nilearn.plot_glass_brain(figure=subfig)` IGNORES the SubFigure and
  adds its brain axes to the ROOT figure in ROOT coords → all 3 brains stacked at one rect
  (only the middle rendered). Diagnosed via per-subfig `len(sf.axes)`==1 (brain axes absent).
  Fix: **glass-brain compounds MUST be a flat `plt.figure()` with rects in figure coords**
  (the proven standalone pattern). Pure 3D + 2D compounds are fine in SubFigures.
- `results_sec_2.tex`: both `figure*[p]` blocks are now a single
  `\includegraphics[width=\textwidth,height=0.80–0.84\textheight,keepaspectratio]`; minipage
  tiles + LaTeX `(a)/(b)/(c)` letters removed (letters baked in matplotlib). Header manifest
  updated → upload only the **2** compound PDFs to Overleaf.
- 5 single-panel scripts + their PDFs KEPT (source of the imported helpers; the single PDFs
  are now orphaned from §2 but may still serve the talk). Sent both compounds to the PI.

---

## Session — combined figure: direct matrix colouring (squares → cells)

**Head:** User: "instead of a square becoming bigger i would like direct
colouring of the matrix" (like the diffusion snapshots colour the matrix
directly). Replaced the row-2 growing-Rectangle overlay with DIRECT cell
colouring.

- New helper `_community_matrix_rgb(labels, leaves, Mbright)` → (n,n,3): within-
  community cells take the community hue (tab20) with brightness ∝ |FC|
  (`Mbright = clip(M/mvmax)**0.6`), between-community cells a faint grey.
  Blocks now grow in the PIXELS as the cut merges; hues match dendrogram+bubble.
- build_figure: dropped the Rectangle pool + magma imshow → single recolourable
  RGB `rm_im` (set_data per frame). make_static row-4 same. Removed dead
  `_blocks()` + `Rectangle` import.
- Verified: animation frame (grey/blue/orange/green blocks) + snapshots PDF
  (39→8→1 comm., blocks = coloured cells, no outlines). Re-rendered all 4 assets
  (mp4/transparent gif/2 pdfs). Talk bullet + memory updated.

---

## Session — talk: per-slide expansion files + I-1 race-bracket figure

**Head:** Started expanding the 20-slide talk structure into one build-ready
**md spec per slide** (Canva is the deck; repo keeps md only). Wrote slide I-1
(cold open) and rendered its figure.

- New folder `.agents/talk/slides/` + `README.md` (index + 9-section per-slide
  template: on-slide content · narration · delivery · visual · transition ·
  guardrails · refs · open decisions). Single source of truth stays
  `.agents/reports/2026-07-07_talk-structure-20min.md`.
- `I-1_cold-open.md` — cold open personalised to four Lipari guests
  (Battiston/Gabrielli/Garlaschelli/Meloni). Order FB>AG>DG>SM shown as
  **neighbour premises only**; a **race bracket** pits the NON-adjacent
  (inferred) pairs FB-vs-DG & AG-vs-SM → champion Battiston, Meloni out R1
  (inside joke). Narration reoriented to "which relational structures store this
  order — and do they hold it after you stop?" Title = "The race you were never
  shown" (talk title "The shape of an inference" reserved). Wink: Battiston (I-3)
  & Gabrielli (I-5) authored the methods.
- Figure: `scripts/01_compute/figures_embedded/fig_ti_race_bracket_demo.py` →
  `data/outputs/figures/talk/ti_race_bracket.pdf` (vector, transparent, light
  ink for dark slide; photo-placeholder leaf boxes; FB-vs-DG hero node accented
  amber + "★ inferred"; Battiston starred). Honesty note in the spec: this is a
  race bracket (pairs non-adjacent to stage inference), NOT the UPGMA dendrogram
  (which merges neighbours) — only "order → tree" carries to I-6.
- Env note: `conda run -n lapbrain` fails on a cross-compiler activation hook;
  call `/home/giulio/Documents/miniconda3/envs/lapbrain/bin/python` directly.
- OPEN: I-2 … C-1 still to expand (one file each). Cadence TBD (batch intro vs
  one-at-a-time).

---

## Session — §2 Fig 3 visual overhaul (attractor + decomposition forest)

**Head:** Iterated the §2 dynamics compound (`fig_arc_enc_dynamics.pdf`) hard on PI
direction. The 3-D attractor is no longer a basic ellipsoid plot, and panel b is now
violins. All edits live in the shared single-panel helpers so the standalone/talk
versions inherit them.

- **Attractor (`fig_arc_attractor_3d.py::portrait`)** — reworked:
  - ghost ellipsoids → **wiggly KDE density isosurfaces** (`_spread_surface`: radial
    ray-cast, per-direction radius where KDE drops to a low quantile; `scipy.stats.
    gaussian_kde` + `gaussian_filter1d`; no skimage → no marching_cubes needed).
  - wall projections → **concentric per-phase KDE level-curves** on all 3 walls
    (`_plane_contours` via `contourpy.contour_generator`, line deepens with density) +
    trajectory floor shadow. (First tried single covariance ellipses — PI: "still
    circles", wanted nested level curves.)
  - points bigger/opaquer; **median markers forced on top** via `ax.computed_zorder =
    False` + high zorder + `depthshade=False` (3-D depth-sort otherwise buries them).
  - tinted panes, thicker glowing trajectory, bigger axis labels; titles trimmed to
    "Pat_06 · tracer" / "Pat_10 · resetter".
- **Forest (`fig_arc_a_decomposition_forest.py::draw_panel`)** — per-band dot rows →
  **horizontal violins** (per-patient T distribution) + jittered points + cohort-median
  **diamond**. Recolour: `_verdict_color(v, p)` now sign-aware → **red = reset (T<0)**,
  green = clears own null (trace), grey = undetermined (was significance-only). Legends
  updated (compound + standalone).
- **Compound layout (`fig_arc_enc_dynamics.py`)** — attractor gridspec gains a **spacer
  row** so the time-colorbar sits clear of the 3-D plots and the strip (PI: colorbar
  overlapped); bigger colorbar; more gap to panel b. figsize 13.2×16.4.
- All renders fully vector (`pdfimages` → 0). Still mid-iteration; PI reviewing.

## Pairwise-blind ladder — do simple pairwise scalars reproduce the trace? (audit_166)

**Head.** User asked to close the "is our multiscale trace already recoverable from
node strength / clustering — that'd be a disaster" question. Built the **ladder**:
classical low-level graph descriptors run through the IDENTICAL ρ_sym estimator +
matched-strength null as the cophenetic trace, structural order raw edges → node
scalars → cophenetic. Verdict = **disaster averted, but not a clean sweep.**

- **Scope** `.agents/guides/task-persistence-investigation/2026-07-09_pairwise-descriptor-ladder.md`
  (status=result). **Library** `src/lrg_eegfc/utils/metrics/graph_descriptors.py`
  (node_strength + Onnela clustering + eigcent + pagerank + closeness + betweenness,
  pure numpy/scipy, general names — no classical descriptor existed before; strength
  was inline `W.sum(1)`). **Driver** `audit_166_pairwise_descriptor_ladder.py`,
  **Mode B** (exact flagship seed-20260511 ensemble; `adjacency_from_laplacian_eigs`
  reconstructs each surrogate W → every rung sees identical MS draws). Results
  `data/audit/pairwise_descriptor_ladder/`. ~1 min run.
- **Cross-check PASS**: cophenetic rung reproduces audit_150/152 exactly (α/β T_test
  .032/.032, θ silent .784, β-only T_infspec_pe .0098) → harness validated.
- **(1) Named disaster AVERTED.** `strength` p=1.000 all bands (degenerate under MS
  BY CONSTRUCTION); `clustering` (0.99 slaved) + `pagerank` (1.00) clear nothing
  (β inference .161 / .920). Local scalars blind.
- **(2) Band-selectivity UNIQUE to cophenetic.** Only cophenetic recovers α (.032)
  AND silent on θ (.784). Raw FC broad (≈.05 every band, θ not silent). No pairwise
  scalar reproduces α-recovery + θ-silence.
- **(3) HONESTY FLAG.** β inference-specific trace = CONVERGENCE phenom, NOT
  exclusively multiscale: raw edges (.010, 8/10), eigcent (.002, thin 3/10),
  closeness (.024) all reproduce it (β=convergence cell). ⚠ **partly walks back the
  N2.7 "specifically multiscale" framing** folded in earlier from the Grassmann
  negative → reframe to "specifically fine-grained (edge+hierarchy), NOT
  coarse-subspace." **FLAGGED for user, not silently rewritten.**
- Strength-slaving diagnostic: clustering 0.99 / pagerank 1.00 / eigcent 0.99 /
  closeness 0.90 (even higher than r≈0.96 predicted). Memory
  [[pairwise_descriptor_ladder_2026_07_09]].
- **NEXT (user-gated):** (a) N2 reconciliation — reframe N2.7 to "fine-grained not
  coarse-spectral; multiscale value-add = band-selectivity + β→OFC"; (b) ladder
  verdict figure (heatmap rows=rungs × cols=criteria + strength-slaving scatter);
  (c) optional Tier B global scalars (efficiency/path-length/assortativity) +
  betweenness rung.

---

## verify-tau — C(τ) DOES have a peak; §3 coarse-τ justified; methods.tex mislabel fixed

**Head.** Chasing "why does §3 (epi marker) read a different τ than the rest of the
paper" surfaced a false long-standing claim: our "C(τ) is smooth / no peak / τ*
undefined" (guide §5.2, memory `lrg_tau_choice`, `methods.tex:80`) is **wrong**. C(τ)
has a **single clean unimodal peak** in 30/30 (pat×band) cells (+ full cohort), matching
the pipeline's cached `entropy_C` to ~1e-15; τ* = argmax C(τ) ≈ 10/λ_max (~4× coarser
than Fiedler 1/λ_gap) and coincides with audit_121's collapse onset (α≈10–15). Absent is
a *multi-peak* mesoscale ladder — NOT the single collapse peak.

- **Keystone bug:** `methods.tex:80` mislabelled the window ceiling `τ*≈1/λ_gap` and said
  "specific-heat reading does not apply / coarser τ not used here" — both wrong, and the
  second **contradicts §3** (epi), which reads the SOZ marker at `τ_5 = 10/λ_max`. Fixed:
  τ* = C-peak (≈10/λ_max, collapse scale); Fiedler = interior scale; the cross-phase trace
  is anchored at τ_min for a **cross-phase-collapse** reason (audit_121), not because C(τ)
  has no peak.
- **§3 justification added** (`ssec:methods_epi`): the marker reads coarse because seizure
  spread is mesoscale reachability AND, being **within-phase**, it is immune to the
  cross-phase collapse artifact that bars coarse τ for the trace; `τ_5=10/λ_max ≈ τ*`
  (verified below the peak in ~80–90 % of cohort for epi bands δ/γ_low/β). Governing
  principle: **"scale follows the question"** — trace at the floor τ_min, marker at the
  ceiling τ*.
- **Corrected:** guide `lrg-framework-guide.md` §5.2/§6/§9/§10; memory `lrg_tau_choice`
  (+ MEMORY.md pointer). **On record:** `data/reports/2026-07-09_ctau-peak-verification/`
  (README + `ctau_curves.pdf` + 3 reproducers). No `src/` or `scripts/` changes.
- **Left as-is (correct):** `methods.tex:279` (N2 scale-dependence) and `:153` — they scope
  the τ-sweep degeneracy to the *mesoscale-ladder / cross-phase* reading, which stays true.
- **Open for user:** whether to also state τ* ≈ 10/λ_max as an explicit numeric in Methods
  (currently "of order 10/λ_max") and whether a supplement figure of C(τ) is wanted.
