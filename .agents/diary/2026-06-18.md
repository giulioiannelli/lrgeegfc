---
name: diary-2026-06-18
era: IMCOH_ABS_COHORT_N10
date: 2026-06-18
kind: diary
---

# 2026-06-18

## Committed the n10-diagnostic backlog (8 commits) + PI decision: C5 epi-exclusion demoted to secondary

**Head.** Cleared two long-standing items. (1) The whole
`audit/cohort-n10-diagnostic` branch was uncommitted — ~80 untracked scripts/reports
plus the brief/ledger cascade — now committed in **8 semantic commits** (lib /
epi-marker / controls / taxonomy / preprint-figs / null-archive+linearity-note /
docs-cascade / epi-precision). (2) **PI decision on the open C5 re-anchor: epi-zone
exclusion is DEMOTED from "primary interpretive lens" (2026-06-05 directive) to a
SECONDARY control**, parallel to C6 — and that decision was cascaded into the locked
records. No verdict tags change.

### Why demote (the honest call)

C5's original elevation to *primary* (2026-06-05) rested on "exclude epi → the cophenet
trace *strengthens*". The node-count decimation control (audit_85 `--stratify epi`,
2026-06-12) showed that strengthening is **generic node-count**, not epi-specific
(cohort `p_dec` α 0.135 / β 0.390 / γ_l 0.130; epi drops only ~9.5 % of nodes). The
rationale collapsed. Re-elevating C5 to primary on a *different* post-hoc finding from
the same retracted thread would be goalpost-moving — exactly what brutal-honesty mode
should resist. C6 (WM-exclusion) was demoted for the identical node-count reason, so
symmetric treatment puts C5 and C6 at the same secondary tier.

### What is RETAINED (genuine, same-graph, reported as secondary)

The pair-class + pair-count control (audit_85 `--mode pairclass`, R=1000, n=10) gives a
real tissue dissociation with no node-count confound:
- **α RECRUITS the diseased core** — epi↔epi cophenet obs **+0.423** vs pair-count null
  +0.129, `p_pair<0.001` (concentrated); matched-strength persist p=0.003.
- **β SPARES the core** — epi↔epi `weaken` (+0.142, p=0.16), trends depleted
  (`p_pair=0.945`, n.s.); β trace is **gray↔gray-dominant** (concentrated
  `p_pair<0.001`), WM↔WM depleted.

### Cascade (all done, consistent)

`02_alpha.md` (frontmatter + Head + §3.1 status + §9 + 2026-06-18 changelog),
`03_gammalow.md` (Head + cross-probe table + changelog — cophenet "emergence" was
already withdrawn 2026-06-12 as node-count, so γ_l is doubly settled, verdict stays
`strong, only Grassmann`), `01_beta.md` (harmonized "major component / not a side
control" → "secondary, parallel to C6"), `CONTROLS.md` §C5 (PI-resolution reworded to
"demoted to secondary, cohort-wide") + §C6 (parity note), `VERDICT_LEDGER.md` (head +
locked verdict table α/γ_l rows + γ_l C5 interp + 2026-06-18 amendment reworded).

### Also today (earlier in session)

Archived the rejected opaque matrix nulls (CRC + SB-CRC) with a post-mortem; wrote the
methods note `linearity-and-higher-order-structure.md` (ImCoh second-order / lagged≠
nonlinear; LRG linear dynamics but nonlinear map → higher-order *graph* not *signal*
features). Both committed.

### Next

Point 3 of the user's plan: **verify the cross-phase reset/anchor dissociation**
(audit_105-108 taxonomy) — trace is already VERIFIED; reset/anchor are NEW + UNVERIFIED
and need R=1000 matched-strength + leave-one-out before they can be claimed.
