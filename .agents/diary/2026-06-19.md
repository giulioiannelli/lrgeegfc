---
name: diary-2026-06-19
era: IMCOH_ABS_COHORT_N10
date: 2026-06-19
kind: diary
---

# 2026-06-19

## Duration control closes the β cingulate inference localization — content, not data-amount

**Head.** Resumed the inference-mark thread after compact. The handoff listed a
length-equalized control as "optional/moot"; I checked the actual recording lengths
and it was **not moot** — `task_test` is **1.35–2.53× longer than `task_learn` in
all 10 patients** (median 1.57×), so the inference vector `f = D_taskTest −
D_taskLearn` could in principle reflect `task_test` simply having ~1.6× more data,
not inference content — and this sits directly under the marginal β cingulate result
(q=0.050). Built `audit_113` to test it. **Verdict: the β cingulate inference hotspot
(and the OFC memorizing hotspots) survive length-matching → content-driven, not a
duration artifact.** Confound excluded.

## What the control does

Truncate `task_test` to `task_learn`'s sample count (3 window placements:
head/center/tail), recompute ONLY that phase's canonical cophenetic, rerun the exact
audit_110 localization with `D_task_test` swapped. Reuses the audit_110 / audit_83
machinery verbatim (no fork); recomputes the imcoh_abs FC from the truncated
timeseries via the same `compute_msc_welch(metric="imcoh")` + `_band_abs` path the
cache uses. Two built-in correctness checks, both pass:
- **Full-length recompute == cached cophenetic** (Spearman ρ=1.000000, max|Δ|≈1.5e-4
  = float32 rounding) on all 3 validation patients (Pat_02/06/15, spanning the ratio
  range) → my recompute pipeline IS the canonical cache.
- **Encoding target is bit-identical full-vs-matched** (`e = D_TL − D_preA` has no
  `task_test` dependence) → the swap logic is correct; any change would be a bug.

## Numbers (β, epi-include)

- Cingulate inference: of 8 cingulate-sampling patients, **8/8 keep the sign** of
  their per-patient demeaned value; cohort median **0.61× full** (+4.36e5→+2.67e5),
  per-patient full-vs-matched ρ=0.74; paired Wilcoxon matched-vs-full **p≈0.95
  (indistinguishable)**. The 5/8-positive cohort split is reproduced exactly.
- OFC standard survives (0.83–0.87× full, sign 5/5, ρ=0.90–1.0); OFC encoding
  identical by construction.
- The ~40% attenuation is the **expected, statistically non-significant** noise
  penalty of discarding data (truncation → noisier `D_TT'` → biases concordance
  toward zero = conservative). Survival under the noisier estimate is the point.

## Stage limit (honest)

Observed-statistic robustness only — it does NOT regenerate the matched-strength
surrogate on truncated `task_test`, so no new BH q at matched length. Given how
little the observed value moves (ρ_fm=0.74, paired p≈0.95, 8/8 sign), a
surrogate-on-truncated stage-2 would near-certainly reproduce q≈0.05; left as an
optional rigor add if a referee insists.

## Lesson

**Check phase durations before dismissing a duration confound as "moot."** The prior
handoff's "argued moot by band/target-specificity" hand-wave was wrong on the facts —
the durations differ a lot and systematically. The band/target-specificity argument
turned out to be *correct* (the hotspot is content), but only the explicit control
established it.

## Files

- `scripts/01_compute/audit/audit_113_inference_length_control.py` (5-point preamble
  in the docstring).
- `data/audit/inference_localization/length_control.csv` (540 rows).
- Cascaded into `.agents/reports/2026-06-18_inference-mark-localization.md`
  ("Duration / length-equalized control" section + honest-limits + next-steps),
  the handoff, memory [[inference_mark_localization_2026_06_18]], and the MEMORY.md
  ⏯ line.

## Open

Behavioral TC1 (blocked — no accuracy/RT scores in repo, confirmed by grep); low-γ
focal cingulate-encoding trace standalone writeup (data ready, framing TBD with PI);
optional audit_113 stage-2 (surrogate-on-truncated).
