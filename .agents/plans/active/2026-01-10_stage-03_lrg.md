# Plan (2026-01-10): Stage 03 - LRG analysis

## Goals
- Compute LRG ultrametric distances, dendrograms, and entropy curves from FC.
- Keep analysis consistent across MSC and correlation FC.

## Inputs
- Cached FC matrices from Stage 02
- `lrg_eegfc.workflow.lrg`
  `visuals/lrg.py`, `visuals/lrg_revised.py` (post-refactor: `lrg_eegfc.visuals.lrg`)
- Legacy notebooks (FIGMNTGN series) for parameter defaults

## Current status
- LRG workflow lives in `lrg_eegfc.workflow.lrg` with caching to `data/lrg_cache/`.
- Visualization helpers exist under `visuals/lrg.py`.
- No consolidated LRG parameter sheet yet.

## Tasks
1) LRG parameter consolidation
- Extract entropy steps, tau range, and dendrogram threshold settings from
  notebooks.
- Record defaults for dev vs full runs.
- Store defaults in a short reference note under `.agents/plans/active/`.

2) LRG computation workflow
- Ensure `compute_lrg_analysis` runs for both MSC and correlation matrices.
- Cache outputs under `data/lrg_cache/Pat_XX/`.
- Add a single-patient runner notebook in `ipynb/03_lrg/` for quick checks.
- Implementation targets:
  - `ipynb/03_lrg/01_lrg_singlepat.ipynb`
  - `ipynb/03_lrg/02_lrg_entropy_psi_singlepat.ipynb`
  - (optional) `src/compute_lrg_analysis.py` extension for batch grids

3) Diagnostic plots
- Generate single-case LRG figures (entropy, dendrogram, PSI, ultrametric
  heatmaps) from cached results.
- Use `visuals/lrg.py` functions to avoid duplicate code.

## Compute vs visualize
- Compute: `compute_lrg_analysis` (or batch wrapper) writes to `data/lrg_cache/`.
- Visualize: use `load_lrg_result` and `visuals/lrg.py` to render plots.
- Do not recompute LRG inside visualization scripts.

## Deliverables
- Parameter summary for LRG settings.
- Cached LRG results for dev scope.
- Single-case diagnostic notebooks to validate outputs.
- LRG parameter note: `.agents/plans/active/2026-01-10_lrg_params.md`

## Exit criteria
- LRG cache exists and downstream reorganization metrics can consume it.
