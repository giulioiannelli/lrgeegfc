# FC Parameters (2026-01-10)

This file records parameter choices and where they came from.
Defaults below are the **proposed standards** pending the scaling test
(`scripts/msc_surrogate_scaling.py`).

## Sources reviewed
- Legacy notebooks: `ipynb/90_archive/.old/2025-12-11/UTILS-COHERENCE_NETWORKS.ipynb`
  - `nperseg`: 256, 512
  - `n_surrogates`: 50
- Legacy notebooks: `ipynb/90_archive/.old/2025-12-11/UTILS-FC_COMPARISON_PIPELINE.ipynb`
  - `nperseg`: 256, 512
  - `n_surrogates`: 1 (testing only)
- Current defaults:
  - `workflow.msc`: `nperseg=1024`, `n_surrogates=0` (dense)
  - `compute_msc_matrices.py`: `nperseg=1024`, `n_surrogates=0`
  - `config/const.py`: `DEFAULT_N_SURROGATES=200`
- Sanity run caches: `*_msc_sparsify-none_nperseg-1024.npy` exists.

## MSC (coherence) parameters
| Parameter | Dev default | Full default | Notes |
| --- | --- | --- | --- |
| `sparsify` | `none` | `soft` | Dense for dev; validated for full run. |
| `n_surrogates` | `0` or `50` | `200` | Raise to 500 only if scaling test shows instability at 200. |
| `nperseg` | `512` | `1024` | Legacy notebooks used 256/512; keep 1024 for final resolution. |
| `noverlap` | `nperseg // 2` | `nperseg // 2` | Standard Welch overlap. |
| `batch_size` | `64` | `64` | Current default in `compute_msc_matrices.py`. |
| `sample_rate` | `2048` | `2048` | Fallback if missing in MAT metadata. |

## Correlation parameters
| Parameter | Dev default | Full default | Notes |
| --- | --- | --- | --- |
| `filter_type` | `abs` | `abs` | Used in `workflow.corr`. |
| `zero_diagonal` | `True` | `True` | No self-loops. |
| `filter_order` | `4` | `4` | Bandpass order. |
| `sample_rate` | `2048` | `2048` | Fallback if missing in MAT metadata. |

## Data inventory notes
- Observed `fs`: 2048 Hz (Pat_02), 1024 Hz (Pat_03); missing for Pat_05/06/07/08.
- Default `sample_rate` is used only when `fs` is missing; decide whether to
  override per patient or update metadata.

## Cleaning parameters (correlation)
| Parameter | Dev default | Full default | Notes |
| --- | --- | --- | --- |
| MP cleaning | on | on | Uses Marchenko–Pastur bounds. |
| Percolation threshold | first jump | first jump | Existing logic in `workflow.cleaning`. |

## Open decisions
- Confirm the final `n_surrogates` after the scaling test.
- Revisit `nperseg=1024` only if low-band resolution is insufficient.
