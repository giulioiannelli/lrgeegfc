---
name: talk-slide-14-lasting-trace
type: report
era: IMCOH_ABS × COHORT_N10 (mst@0.20 recovery)
slide: 14
status: draft
updated: 2026-07-14
canva: page 13  (⚠ 2026-07-13 RESTRUCTURED: absorbed old 14 nulls + 15 survivors → ONE comprehensive trace result. Two BUILT talk figures: trace_scale_map (band×scale gate map) + trace_band_scale_violin (gate-ranked split violins, ●/○ verdict). Scalar-vs-structured-object frame RETIRED; matched-strength sole null. RE-RENDER on deck.)
---

# Slide 14 — A lasting, multiscale trace

1. TITLE
A lasting, multiscale trace

TITLE CHOSEN 2026-07-13. The old "a scalar verdict, or a structured object?" is RETIRED as a title
— too method-y for the headline-1 punchline. The title pays off slide 13's "we hunt traces": we
found one, it lasts, and it lives across scales.

⚑ RESTRUCTURE 2026-07-13 (user directive — fewer, denser slides). This slide is now the ONE
comprehensive headline-1 RESULT: it shows **plots of the measure** — the cohort ρ_sym(τ) curves —
delivering **multiscale + per-band + the null + survivors ALL TOGETHER**. It ABSORBS the old
slide 14 (the nulls) and old slide 15 (survivors table); those are no longer separate slides.
- The scale-resolved **reinstatement** figure moved to slide 12 (the measure slide); it is NOT the hero
  here. 13 is quantitative (curves), not the colour-on-tree demo.
- New **slide 15 = comparison with the other metrics** (does raw connectivity / simpler descriptors
  see this? the raw-vs-coph / descriptor-ladder story) — TENTATIVE, "at most one more slide".
- Downstream slides (old 16 β-anatomy … 22 thanks) shift up by the two folded slots — a mechanical
  renumber to do LATER, not yet done.
- The three questions the title implies — which bands? at what scale? vs the null? — are now ALL
  answered ON THIS SLIDE by the two figures (the gate map + the gate-ranked violins), not deferred.

2. MAIN CONCEPT
- **HEAD (the juice):** The task leaves a trace that outlasts it — and read multiscale, that trace is **band-selective, scale-structured, and cohort-CONSISTENT**. Post-task rest keeps the task's hierarchy; the cross-phase similarity ρ_sym clears a matched-strength null **consistently across the whole cohort** along the entire diffusion-scale axis for **β (every one of 16 scales — scale-invariant)** and across most of the axis, peaked at mesoscale, for **α (12/16 — scale-tuned, not mesoscale-only)**. The other bands are **not trace-free**: individual patients carry strong traces — **γ_low especially, with some patients far above their own null** — but those traces **don't align across the cohort**, so no cohort gate clears (θ / low-γ 0/16, δ / high-γ patchy). The gate is a **cohort-consistency** test, not a presence/absence one.
- **The trace holds.** Read through the resting hierarchy, the network afterwards does not snap back — its tree still looks like the task's. (The visceral "it holds" image is the reinstatement grid on the measure slide, slide 12; here we quantify it.)
- **WHERE the trace is cohort-consistent — the gate map (Fig 1).** −log₁₀ of the matched-strength gate p over 6 bands × 16 scales; sub-0.05 cells greyed so the lit region is **where the whole cohort agrees**. β = a solid bright block spanning the **whole** scale axis (scale-invariant); α = a broad **mesoscale** band; δ / high-γ = scattered patches (fine / coarse only); θ / low-γ = grey. Grey means **no cohort consensus at that scale — NOT that no patient traces** (this is a cohort statistic; the individual traces live in Fig 2).
- **HOW IT VARIES across patients — the split violins (Fig 2).** Per band a split violin over the τ-sweep, all **10 patients** as Δ = ρ_sym − null (0 = the null); filled half = the band's weakest-consensus scale, outline half = its strongest. The violin BODIES are the story: **wide = strong patient-to-patient variation**, and even the grey bands push patients well above their null (γ_low reaches Δ≈0.6). Each median is a **filled ● if the cohort holds at that scale, an open ○ if it doesn't**. So **β** = two ● (cohort holds even at its weakest scale → scale-invariant); **α / δ / high-γ** = one ● (holds only at their best scale); **θ / low-γ** = two ○ — **traces present per patient, but they don't cohere**. Read both ways: a median a hair above the line is still an open ring (**above the null ≠ a cohort trace**), and an open ring is **not** "no trace".
- **One referee: matched-strength, read as cohort consistency.** Rewire preserving each contact's total connection strength; the per-scale Wilcoxon asks whether the cohort **consistently** beats that null — the ●/○ and the greying ARE the gate. Never best-scale, never a patient count.
- **Implication → next.** A band-selective, scale-structured, offline trace, β the primary carrier and the only scale-invariant one. Where in the brain, and whether simpler metrics see it, come next. (A whole-graph Grassmann companion independently confirms β — mention only if asked; not on-slide.)

3. ON-SLIDE TEXT   (the MESSAGE — not a figure legend; each line is a speech anchor the figures don't state)

HEAD (top of slide)
The reorganization outlives the task — a lasting, offline trace.

band-specific & multiscale
β — the primary carrier: scale-invariant → architectural, shared
α — a scale-tuned trace: broad support (12/16), peaked at mesoscale, fading at the coarsest scales

no band is silent
δ, γ_high, θ, γ_low reorganize per patient — idiosyncratic, not shared
the trace = CONSISTENCY (shared), not mere presence

---

4. SPEECH  (~110 s — the load-bearing trace RESULT; three figures, each with ONE job)

Two slides ago we built one number — rho — how similar two phases' trees are at a given scale, read against a matched-strength null. Now we spend it, on the question this whole talk is about: after the task, does its reorganization survive in the resting tree? The short answer is yes — read the resting hierarchy afterwards and it doesn't snap back; its tree still looks like the task's. Here's that made quantitative, three ways: how strong the trace is, where it's shared across patients, and how it varies from patient to patient.

[Fig 3 — the curves: HOW STRONG.] This is rho itself — for every band, how much the rest tree still resembles the task tree, swept from fine to coarse scale. Read two things. The height: beta rides highest, its rest tree is the most task-like at every scale; alpha next; the others lower, some sitting right on the grey null floor where there is nothing above chance. And the dots: filled when the whole cohort clears the matched-strength null at that scale, open when it doesn't. Beta is filled all the way across; alpha across most of the range.

[Fig 1 — the map: WHERE IT'S SHARED.] Compress those dots into a map — band down the side, scale along the bottom, colour is how consistently the cohort agrees, grey is below the null — and the shape of the trace jumps out. Beta is a solid block across the entire width: the cohort agrees at every single scale, so the trace is scale-invariant — it doesn't live at one resolution, it's architectural. Alpha is broad too, strongest through the mesoscale. Delta and high-gamma clear only in patches at the extremes — the fine, near-raw end and the coarse-collapse tail, the least trustworthy ends of the axis. Theta and low-gamma stay grey. So the consistent, cohort-wide trace is selective — and, crucially, grey does not mean nothing happened.

[Fig 2 — the violins: HOW IT VARIES.] Here's what the grey hides. For each band, all ten patients, plotted as distance above their own null. Look at the widths — they're huge. Even in the grey bands individual patients sit far above their null; low-gamma reaches point-six, a very strong trace. The traces are there — they just don't point the same way from patient to patient, so the cohort median doesn't hold. That's the open ring: the patients disagree, never "nobody traced". Beta is the exception — filled even at its weakest scale. And the ring reads both ways: a median a hair above the line is still open, because being above your own null is not the same as the whole cohort agreeing.

So — the result, and what it means. The task leaves a lasting, offline trace in the tree: the reorganization outlives the task. And it is band-selective. Beta carries the shared, cohort-wide trace at every scale; alpha across most of them; the other bands reorganize too, sometimes strongly, but each patient in their own way, so they don't cohere into a cohort trace. The trace we can stand behind is consistency — the same reshaping across patients — not mere presence. What drives that per-patient heterogeneity, and where in the brain the trace sits, come next.

**Careful:**
- **Three figures, three jobs, in this speech order — recall the measure FIRST, then:** **Fig 3 (curves) = HOW STRONG** — ρ magnitude + scale profile; introduce the filled/open (●/○) key HERE, on its first appearance. **Fig 1 (map) = WHERE IT'S SHARED** — the band×scale consistency map, selectivity + β scale-invariance. **Fig 2 (violins) = HOW IT VARIES** — per-patient spread, "grey ≠ silent". Do NOT re-explain filled/open on Fig 1/2 — introduced once on Fig 3, reused. The speech opens by spending the ρ measure built on slide 12 (two slides back) — that hand-off is load-bearing: this slide is where the measure PAYS OFF. Eye-path note: speech points bottom-left (curves) → top-left (map) → bottom-right (violins); if that bothers the presenter, swap the Fig 3 and Fig 1 beats (magnitude-first vs map-first — content is modular), but keep filled/open introduced on whichever curve/map comes first.
- Do NOT resurrect "read three ways / who's right / scalar vs structured object" — RETIRED framings. 13 is now the comprehensive trace RESULT.
- Do NOT say "only multiscale sees the trace" / "invisible to simple methods" — raw connectivity DOES detect it (raw β clears matched-strength at p=.024). The edge is selectivity + scale-structure, not exclusive detection. (The raw-vs-coph comparison is the tentative new slide 15.)
- Do NOT name β's anatomical home here. β is DELOCALIZED; there is no "β → OFC" (OFC is the *encoding* anchor only). Anatomy/laterality is a later slide.
- Null = **matched-strength ONLY** (drift retired 2026-07-12). The gate is the per-scale Wilcoxon read as **cohort consistency** — NOT the curve/violin height, NOT a ×-null ratio, NOT a patient count (house rules: no-scalar-collapse, counts-never-the-gate). Name β/α as the consistent carriers; θ hugs the floor more closely than the rest, but **do NOT call any band "trace-free" / "built-in control that never traces"** (house rule no-band-is-trace-free).
- δ / γ_high = the **partial, scale-confined** tier: δ 8/16 patchy toward the fine (near-raw) end, γ_high 4/16 in the coarse-collapse tail — the **artifact-prone extremes** of the scale axis (fine ≈ single raw edges, coarse ≈ collapse). Present them as the intermediate / scale-localized tier that shows consistency has a scale structure; do NOT elevate them to carriers (that's α/β), do NOT drop them to "no trace" (memory: δ/γ_h flagged as scale-artifacts — the honest read is "partial + at the untrustworthy ends").
- Fig 2 honesty runs BOTH ways, and this is the load-bearing correction: the ●/○ is cohort consensus, height is NOT. (i) A median a hair above the line is still an open ring — **above the null ≠ a cohort trace** (θ best median > 0, gate p=.28). (ii) An open ring is **NOT** "no trace" — **γ_low rides two open rings yet has patients at Δ≈0.6, far above their own null**; 0/16 = **no cohort consensus**, never "no trace". γ_low is heterogeneous (strong but inconsistent), NOT null-hugging. The per-patient spread and what drives it (laterality, patient-specific / global structure) is **slide 15** — point at it here, don't unpack it.
- Halves are ranked by the GATE (strongest = min gate-p, weakest = max gate-p), NOT by argmax-Δ (which is upward-biased and drifts to the coarse-collapse tail — that was the bug that made every band peak at s≈16). β's *weakest* scale plots slightly higher than its *strongest* (ranking is by p, not Δ; β is so uniform its least-significant scale still has large Δ) — both ● filled, message intact.
- Grassmann is a whole-graph companion, NOT attributed to the mst@0.20 backbone; keep it off-slide unless asked.

---

5. FIGURES  (talk PNGs in data/outputs/figures/talk/)

FIGURE PLAN (BUILT 2026-07-13). 13 is the comprehensive result — TWO talk-clean figures, both from
the settled mst@0.20 matched-strength cache `data/sparsified_arc/ms_mst020/`
(gen: `scripts/07_figures/talk_fig_trace_multiscale.py`; numbers recomputed fresh from the CSVs).

- **Fig 1 — the (band × scale) trace map.** `data/outputs/figures/talk/trace_scale_map.{png,pdf}`.
  −log₁₀ of the **matched-strength gate p** over 6 bands × all 16 diffusion scales (s = τ·λmax, fine→coarse);
  sub-0.05 cells **greyed** so the lit region *is* the trace zone; cyan dashed line = s_report ≈ 5.6.
  Answers WHERE the trace is COHORT-CONSISTENT: β = a solid bright block spanning the **whole** scale axis (16/16,
  scale-invariant); α = a broad **mesoscale** band (12/16); δ = scattered patches (8/16), γ_high = coarse
  corner only (4/16); θ / γ_low = grey (0/16 = **no cohort consensus**, NOT absence of trace — individual
  patients still trace, γ_low strongly; see Fig 2). NO N/16 side-column (removed on
  user request — the map carries breadth visually).
- **Fig 3 — per-band ρ_sym(τ) curves (the magnitude companion, full/empty gate).**
  `data/outputs/figures/talk/trace_band_scale_curves.{png,pdf}`. One curve per band of the
  cohort-median **ρ_sym** across all 16 diffusion scales (s = τ·λmax, fine→coarse, log-x), with the
  **same full/empty marker grammar as the encoding/inference scale-payoff figure**
  (`slide15_encinf_scale_payoff.png`): **filled ● = the cohort clears the matched-strength gate at
  that scale (p<0.05), open ○ = ns**. Where Fig 1 shows −log₁₀(p), this shows the **actual measure
  value** and its scale profile, so band-selectivity reads at a glance: β rides high and **all-filled**
  (16/16, scale-invariant), α high with a filled **mesoscale** run (12/16), δ / γ_high partial, θ /
  γ_low low and **all-open** (0/16 = **no cohort consensus**, NOT trace-free — the per-patient spread
  is Fig 2). Grey floor = matched-strength null envelope; grey dashed vertical = s_report ≈ 5.6.
  Band legend (right) ranked by breadth N/16, carriers (β, α) first. Same house rules: the **●/○ IS
  the per-scale gate** — never the curve height, never a patient count.
- **Fig 2 — the gate-ranked split violins.** `data/outputs/figures/talk/trace_band_scale_violin.{png,pdf}`.
  Per band a split violin over the τ-sweep: filled half = the band's **weakest**-trace scale (max gate-p),
  outline half = its **strongest** (min gate-p); y = **Δ = ρ_sym − matched-strength null** (0 = null);
  median = **filled ● if the cohort holds at that scale, open ○ if not**; scale label inside each half.
  The violin BODIES carry the message — wide = strong patient-to-patient variation; even the grey bands
  push patients well above their null (γ_low reaches Δ≈0.6). Dots: **β = 2 ●** (cohort holds even at its
  weakest scale → scale-invariant), α / δ / γ_high = 1 ● (holds only at their best scale), **θ / γ_low = 0 ●**
  (**two open rings = no cohort consensus, NOT "no trace"** — traces present per patient, don't cohere).
  Halves ranked by the GATE, not argmax-Δ (that bug made every band peak at s≈16). Embodies "show spread
  not one number" + "counts never the gate" + BOTH readings of the ring (above-the-null ≠ cohort trace;
  open ring ≠ no trace).
- **Settled numbers behind the labels** (source: `.agents/preprint/established_results/2026-07-13_settled-three-results.md`;
  `cohort_gate.csv`; `hardening/scale_flatness.csv`): β 16/16, strongest s≈5.6 gate_p=.001, weakest s=1 p=.014
  (both clear → scale-invariant; Friedman p=.17 flat); α 12/16, strongest s≈5.6 p=.007 (Friedman p=.004,
  scale-tuned mesoscale); δ 8/16 strongest s≈2.8 p=.014 (fine, patchy); γ_high 4/16 strongest s≈64 p=.014
  (coarse only); θ 0/16 (best s=16 p=.28), γ_low 0/16 (best s=16 p=.12). Gate = matched-strength Wilcoxon.
- **RETIRED on 13:** `trace_multiscale_curve.png` + `trace_cohort_spread.png` (the earlier curve/single-scale
  spread — superseded by the two gate-honest figures above); `fig_rhocoph_tau_bands.png` (busy diagnostic);
  `trace_reinstatement_coph.png` (→ slide 12 scale grid); `fig_trace_c_raw_vs_multiscale.pdf` (raw-vs-coph → the
  tentative NEW slide 15); `fig_before_nulls_three_measure.png` (killed "who's-right").

Generator (single source for both figures): `scripts/07_figures/talk_fig_trace_multiscale.py`
→ `trace_scale_map.{png,pdf}` + `trace_band_scale_violin.{png,pdf}`, both from
`data/sparsified_arc/ms_mst020/{cohort_gate,per_patient_scale}.csv`. Transparent, Canva-ready.

Canva build: drop **Fig 1 (the gate map)** left/large and **Fig 2 (the violins)** right/beside (or stacked);
title "A lasting, multiscale trace"; paste the 4 on-slide lines; speech → presenter notes.
No β→OFC, no exclusive-detection language, no drift, no reinstatement panel (that's slide 12).

---

6. REFERENCES
- All read-outs are ours: raw \(|\mathrm{ImCoh}|\) edges · Grassmann chordal (subspace) distance · cophenetic \(\rho_{\mathrm{sym}}\) from \(e^{-\tau\hat L}\) on the sparsified (mst@0.20) backbone. No external citation on this slide.

7. CANVA STATUS
⚠ 2026-07-13 RESTRUCTURED (user directive, fewer denser slides): 13 ABSORBED old 14 (nulls) + old 15
(survivors) → ONE comprehensive trace result. The two figures are BUILT and delivered
(`trace_scale_map` = band×scale gate map + `trace_band_scale_violin` = gate-ranked split violins with
●/○ verdict). Scalar-vs-structured-object frame RETIRED; the earlier curve/spread pair is superseded.

On-slide text = the MESSAGE, not a figure legend (user, 2026-07-16): the figures carry where/how-much
(which band×scale clears, the per-patient spread); the on-slide lines carry the INTERPRETATION the figures
can't state, each line a speech anchor. Beats: (1) the reorganization OUTLIVES the task — a lasting, offline
trace; (2) band-specific & multiscale — β the primary carrier (scale-invariant → architectural, shared),
α scale-tuned (broad 12/16, mesoscale-peaked, fades at the coarse tail — NOT mesoscale-only); (3) NO band is silent — δ/γ_high/θ/γ_low reorganize per patient,
idiosyncratic not shared → the trace is CONSISTENCY (shared), not mere presence. Do NOT put "β 16/16 /
α mesoscale / θ none" style legend text on-slide (that's what the map already shows). The gate is a
cohort-consistency test, NOT presence/absence; "never trace / built-in control" is RETIRED (no-band-is-
trace-free). Per-patient variability unpacked on slide 15 (pointed at here, not cluttered).

Page 13. Canva job: (1) place Fig 1 (the gate map) large; (2) place Fig 2 (the violins) beside/under it;
(3) title "A lasting, multiscale trace"; (4) paste the HEAD + the two figure-side clusters, speech → presenter notes.
New slide 15 (tentative) = comparison with the other metrics (raw-vs-coph). Downstream slides renumber
later. No β→OFC, no exclusive-detection language, no drift, no reinstatement (that's slide 12).
