---
name: n2-flagship-handoff
era: IMCOH_ABS_COHORT_N10
status: current
kind: report
scope: Pre-/compact handoff (2026-06-23). Carries the session's done work (spectral head-to-head cascade, entropy dead-end, doc fixes) + THE next goal — elevate N2 to the "offline abstraction of a learned structure" flagship, kept in slot 2. Resume here after /compact.
updated: 2026-06-23
---

# Handoff — N2 as the "offline abstraction" flagship (post-/compact resume)

> **EXECUTED 2026-06-23.** The N2-elevation plan below has LANDED (framing/rewrite
> only, no new compute): N2 title + §A rewritten to lead with *offline abstraction of
> a learned structure* (ceiling first, decomposition demoted to evidence, mesoscale
> promoted as the multi-step-integration scale-signature, OFC anchor, literature tie
> to Behrens / Schuck / Bernardi marked *verify-at-write-up*); §C adds the
> three-legs framing; N1 §A "Why it matters" reframed to *method reveals a held,
> non-ergodic structure* + explicit forward-bridge to the flagship; `RESULTS_OUTLINE`
> R2 reframed + reordered (R2.1 abstraction, R2.2 mesoscale lead) + NN-fit foot;
> `README` head + architecture box + narrative-spine pts 2–3 + headline-count +
> file-index all reframed. Files still **UNCOMMITTED** (branch
> audit/cohort-n10-diagnostic). Open decisions below unchanged.

**Head.** The 2026-06-22/23 session closed the method-superiority question
(cophenetic beats spectral clustering on α + selectivity; `audit_143`) and killed
the entropy-milestone idea (dead end). **Next goal, PI-agreed: make N2 the paper's
flagship — "the brain keeps the relationships it reasoned out = offline abstraction
of a learned structure" — kept in slot 2 (after N1 trace, before N3 epilepsy).**
Resume here.

## The arc (PI-agreed spine)
- **N1 (trace)** = the method *reveals a held structure*: non-ergodicity /
  persistence / sustained reinstatement the resting brain holds, invisible to
  edge-wise + spectral-clustering analysis. Sets up *what the method can see*.
- **N2 (slot 2, made the flagship)** = *what the held structure IS*: the
  **abstraction** — relationships the brain *reasoned out*, not just the pairs it
  saw. **"Offline abstraction of a learned structure."** The center /
  headline-of-headlines. Built on EXISTING verified results — no new data needed.
- **N3 (epilepsy)** = unchanged, after.

## N2-elevation plan (execute after /compact)
1. Rewrite N2 §A head + title to LEAD with "offline abstraction of a learned
   structure" (the brain consolidates what it *inferred*, not just what it saw).
2. Re-order N2 so the abstraction claim is the lead; the encoding-vs-inference
   decomposition becomes the *evidence* for it (inference-specific consolidation:
   β, mesoscale, survives matched-strength + duration; OFC anchored by encoding).
3. Tie to cognitive-map / schema / "geometry of abstraction" literature (Behrens,
   Schuck, Bernardi) — the NN-shaped framing.
4. Keep the honest ceiling FIRST (no behavior, no outcome; "consolidation" is
   inferential) — frame as *abstraction-specific reorganization that persists*,
   never a behavioral or mechanistic proof.
5. Reframe N1 to foreground non-ergodicity / persistence / sustained reinstatement
   as the "method reveals a held structure" setup for N2.

## Done this session (files changed — UNCOMMITTED, branch audit/cohort-n10-diagnostic)
- **Count fix:** `headlines/02_encoding_vs_inference.md` §D +
  `headlines/archive/2026-06/04_replay_states.md` → "3 neuro headlines (N1–N3) +
  methods core."
- **N3 reframe:** `headlines/03_epileptogenic_markers.md` §A leads with relational
  (not nodal) organization, detector demoted to application.
- **Spectral head-to-head cascade (`audit_143`):** `headlines/00_CORE_methodology.md`
  Claim 1 (head-to-head block + §D to-dos done + §E figure + §F provenance + §G
  closed) · `headlines/01_trace.md` (§A pt2 extended, new **N1.2b**, §F provenance) ·
  `headlines/RESULTS_OUTLINE.md` R1.2.
- **Entropy scope (dead end):**
  `.agents/guides/task-persistence-investigation/2026-06-22_intrinsic-scale-reorganization.md`
  — status `executed_mostly_negative` + VERDICT banner. Do NOT revisit.

## Two findings to carry forward
- **Method-superiority (SOLID, written in):** cophenetic beats spectral clustering
  on **α** (cophenetic-only across BOTH spectral read-outs — per-pair embedding +
  subspace Grassmann) and on **selectivity** (fires only on α,β; the textbook
  embedding leaks into the null band θ). **β is a TIE** — make the claim on α +
  selectivity, NEVER β magnitude. Source: `audit_143_spectral_distance_swap_headtohead.py`,
  `data/audit/spectral_distance_swap/cohort_summary.csv` (cophenetic arm reproduces
  locked audit_63 α/β bit-exact). v1 `audit_141` superseded (wrong statistic:
  triangle + ARI/NMI).
- **Entropy / specific-heat thermodynamics = DEAD END** — re-confirmed the
  2026-04-29 retirement (von Neumann entropy saturated, single-peaked). The lone
  signal (transient task_test scale-coarsening) is band-nonspecific + duration-
  confounded = generic engagement, not a milestone. Closed.

## Open decisions (NOT started — for after N2)
- **Directed-flow / Hodge bet** — the one new-observable gamble: TI is directed
  (A→B→C); does a directed flow persist offline? Scope before compute; could be
  another dead end like entropy. (Sketched as §10 of the entropy scope doc.)
- **Literature triangulation** (deep-research, token-heavy) — to back the
  abstraction story. On PI go.
- **Coordinated cross-phase null** (audit_63 per-phase-independence gap) — referee
  pre-empt.
- **Commit** the uncommitted edits above once the N2 elevation lands.
